\documentclass[a4paper,12pt]{article}

\usepackage[T1]{fontenc}
\usepackage[italian]{babel}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{tabularx}
\usepackage[table]{xcolor}
\usepackage[most]{tcolorbox}
\usepackage{float}
\usepackage{fancyhdr}
\usepackage[hidelinks]{hyperref}
\usepackage{xurl}
\usepackage{amsmath}
\usepackage{longtable}
\usepackage{eurosym}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{patterns}
\usepackage{listings}
\usepackage{titlesec}
\graphicspath{{./}{../../}}
\titleformat{\paragraph}
    {\normalfont\bfseries}
    {\theparagraph}
    {1em}
    {}
\titlespacing*{\paragraph}
    {0pt}{2.5ex plus 1ex minus .2ex}{0.75em}

\lstdefinestyle{codelisting}{
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    breakatwhitespace=false,
    backgroundcolor=\color{gray!6},
    frame=single,
    rulecolor=\color{gray!45},
    framesep=4pt,
    numbers=left,
    numberstyle=\tiny\color{gray!60},
    numbersep=8pt,
    keywordstyle=\color{blue!60!black}\bfseries,
    commentstyle=\color{green!35!black}\itshape,
    stringstyle=\color{orange!65!black},
    showstringspaces=false,
    tabsize=2,
    captionpos=b,
    xleftmargin=4pt,
    xrightmargin=4pt,
    aboveskip=8pt,
    belowskip=4pt
}
\lstset{style=codelisting}

\lstdefinelanguage{TypeScriptMvp}{
    keywords={class, constructor, private, public, protected, readonly, interface, type, export, import, return, if, else, for, while, new, this, void, extends, implements, static, get, set, function, const, let, var, from, as, typeof, instanceof, in, of, try, catch, finally, throw, async, await, yield, default, case, switch, break, continue, do, delete, null, undefined, true, false},
    keywordstyle=\color{blue!60!black}\bfseries,
    sensitive=true,
    comment=[l]{//},
    morecomment=[s]{/*}{*/},
    morecomment=[s]{/**}{*/},
    string=[b]",
    morestring=[b]',
    morestring=[b]`,
}
\lstdefinestyle{phplisting}{style=codelisting, language=PHP}
\lstdefinestyle{tslisting}{style=codelisting, language=TypeScriptMvp}

\newcommand{\groupmail}{alittlebyte.swe@gmail.com}
\newcommand{\grouplogo}{../../.github/site-src/assets/logo_alittlebyte.png}
\newcommand{\groupsymbol}{../../.github/site-src/assets/solo_logo.png}

\geometry{
    left=2.5cm,
    right=2.5cm,
    top=2.5cm,
    bottom=2.5cm,
    headheight=331pt,
    includeheadfoot
}

\pagestyle{fancy}
\fancyhf{}

\renewcommand{\headrulewidth}{0pt}
\fancyhead{}

\renewcommand{\footrulewidth}{0.4pt}
\fancyfoot[L]{\includegraphics[height=0.6cm]{\groupsymbol}\hspace{0.45em}aLittleByte}
\fancyfoot[R]{Specifica Tecnica}

\fancypagestyle{firstpage}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0.4pt}
    \fancyhead[C]{%
        \makebox[\textwidth][c]{%
            \begin{tabular}{c}
                \includegraphics[height=10cm]{\grouplogo}\\[1ex]
                \small\texttt{\groupmail}
            \end{tabular}%
        }
    }
    \fancyfoot[L]{\includegraphics[height=0.6cm]{\groupsymbol}\hspace{0.45em}aLittleByte}
    \fancyfoot[R]{Specifica Tecnica}

    \setcounter{secnumdepth}{4}
    \setcounter{tocdepth}{4}
}

\providecommand{\glslink}[2]{\href{https://alittlebyte-19.github.io/Documentazione/glossario.html\#gls-#1}{\underline{#2}\textsuperscript{\scriptsize G}}}

\begin{document}

\hypersetup{pageanchor=false}

\thispagestyle{firstpage}

\vspace*{0.5cm}

\begin{center}
    {\LARGE \textbf{Specifica Tecnica}}
\end{center}

\vspace{1.5cm}

\begin{center}
    \renewcommand{\arraystretch}{1.3}
    \begin{tcolorbox}[
        width=0.92\textwidth,
        colback=gray!6,
        colframe=gray!45,
        boxrule=0.5pt,
        arc=2mm,
        boxsep=0pt,
        left=8pt, right=8pt, top=8pt, bottom=8pt
    ]
        \begin{tabularx}{\linewidth}{>{\bfseries}p{3.5cm} X}
            Gruppo      & aLittleByte (gruppo 19) \\
            Versione    & 1.0.0 \\
            Redattori   & -- \\
            Verificatori& --\\
            Destinatari & Prof. Tullio Vardanega, Prof. Riccardo Cardin \\
            Data        & -- \\
        \end{tabularx}
    \end{tcolorbox}
\end{center}

\newpage

\newgeometry{left=2.5cm,right=2.5cm,top=2.5cm,bottom=2.5cm,includefoot}

\begin{center}
    {\Large \textbf{Registro delle Modifiche}}
\end{center}

\vspace{0.35cm}

\renewcommand{\arraystretch}{1.35}
\rowcolors{2}{gray!8}{white}
\begin{center}
    {\footnotesize
    \begin{longtable}{|p{1.4cm}|p{1.8cm}|p{5cm}|p{2.4cm}|p{2.3cm}|p{2.3cm}|}
        \hline
        \rowcolor{gray!20}
        \textbf{Versione} & \textbf{Data} & \textbf{Descrizione} & \textbf{Redattore} & \textbf{Verificatore} & \textbf{Approvatore} \\
        \hline
        \endfirsthead
        \hline
        \rowcolor{gray!20}
        \textbf{Versione} & \textbf{Data} & \textbf{Descrizione} & \textbf{Redattore} & \textbf{Verificatore} & \textbf{Approvatore} \\
        \hline
        \endhead
        1.0.0 & 03/09/2026 & Approvazione manuale utente. & - & - &  V. Y. K. Fernandes \\
        \hline
        0.18.1 & 20/08/2026 & Fix & Eleonora Bellet & Diego Belluz & - \\
        \hline
        0.18.0 & 19/08/2026 & Aggiunta diagrammi & Angela Maule & Angelica Gastaldello & - \\
        \hline
        0.17.0 & 18/08/2026 & Database (schema ER, schema relazionale e descrizione tabelle) & Eleonora Bellet & Leonardo Soligo & - \\
        \hline
        0.16.0 & 17/08/2026 & Design Pattern (backend e frontend) & Angela Maule & Diego Belluz & - \\
        \hline
        0.15.0 & 16/08/2026 & Architettura Frontend (modelli di dati, servizi e componenti UI) & Eleonora Bellet & Leonardo Soligo & - \\
        \hline
        0.14.0 & 15/08/2026 & Architettura Frontend (principi di progettazione e pattern MVVM) & Angela Maule & Leonardo Soligo & - \\
        \hline
        0.13.0 & 14/08/2026 & Descrizione architettura Backend (Modulo Communications e classi condivise) & Eleonora Bellet & Diego Belluz & - \\
        \hline
        0.12.0 & 13/08/2026 & Descrizione architettura Backend (Modulo Documents) & Angela Maule & Angelica Gastaldello & - \\
        \hline
        0.11.0 & 12/08/2026 & Architettura del Sistema (deployment, containerizzazione e stile architetturale) & Eleonora Bellet & Leonardo Soligo & - \\
        \hline
        0.10.0 & 11/08/2026 & Architettura del Sistema (architettura logica interna e componenti) & Angela Maule & Diego Belluz & - \\
        \hline
        0.9.0 & 10/08/2026 & Architettura del Sistema (contesto e architettura applicativa) & Angela Maule & Leonardo Soligo & - \\
        \hline
        0.8.0 & 09/08/2026 & API (State API, Communications API, Documents API) & Angela Maule & Leonardo Soligo & - \\
        \hline
        0.7.0 & 08/08/2026 & Tecnologie (osservabilità, CI e quality gate) & Angela Maule & Diego Belluz & - \\
        \hline
        0.6.0 & 07/08/2026 & Tecnologie (edge e web server, servizi cloud AWS, persistenza dei dati) & Angela Maule & Diego Belluz & - \\
        \hline
        0.5.0 & 06/08/2026 & Tecnologie (contratto API, ambiente locale e provisioning) & Eleonora Bellet & Leonardo Soligo & - \\
        \hline
        0.4.0 & 05/08/2026 & Tecnologie (linguaggi di programmazione, framework e librerie) & Angela Maule & Diego Belluz & - \\
        \hline
        0.3.0 & 04/08/2026 & Introduzione (glossario e riferimenti) & Eleonora Bellet& Angelica Gastaldello & - \\
        \hline
        0.2.0 & 03/08/2026 & Introduzione (contenuto, scopo e struttura del documento) & Angela Maule & Leonardo Soligo & - \\
        \hline
        0.1.0 & 02/08/2026 & Stesura prima versione & Angela Maule & Diego Belluz & - \\
        \hline

    \end{longtable}
    }
\end{center}
\newpage

\begin{center}
    {\Large \textbf{Indice}}
\end{center}

\vspace{0.6cm}

\makeatletter
\@starttoc{toc}
\makeatother

\newpage

\begin{center}
    {\Large \textbf{Indice delle Figure}}
\end{center}

\vspace{0.6cm}

\makeatletter
\@starttoc{lof}
\makeatother

\newpage

\begin{center}
    {\Large \textbf{Indice delle Tabelle}}
\end{center}

\vspace{0.6cm}

\makeatletter
\@starttoc{lot}
\makeatother

\newpage

\begin{center}
    {\Large \textbf{Indice dei Listing}}
\end{center}

\vspace{0.6cm}

\makeatletter
\@starttoc{lol}
\makeatother

\newpage
\pagenumbering{arabic}
\hypersetup{pageanchor=true}
\setcounter{page}{7}
\fancyfoot[R]{Specifica Tecnica\hspace{0.8em}\thepage}

% ─────────────────────────────────────────────────────────────────────────────
\section{Introduzione}

\subsection{Contenuto del documento}

Il presente documento costituisce la Specifica Tecnica del sistema sviluppato dal gruppo \textit{aLittleByte} per il proponente Eggon S.r.l. nell'ambito del progetto Nexum.

Al suo interno vengono illustrate le tecnologie selezionate, il contratto delle API REST esposte dal backend e le scelte architetturali adottate nella realizzazione del prodotto. Il documento ha lo scopo di fornire una visione organica e dettagliata della struttura del sistema, delle sue componenti e delle interazioni tra esse.

Deve essere inteso come riferimento tecnico per le attività di progettazione e codifica, garantendo la coerenza tra le decisioni implementative adottate e quanto emerso nella fase di Proof of Concept.

\subsection{Scopo del prodotto}

Nexum è la piattaforma sviluppata per Eggon S.r.l. a supporto degli operatori che gestiscono la documentazione del personale e le comunicazioni interne aziendali: i consulenti del lavoro da un lato, i redattori aziendali dall'altro.

L'AI Co-Pilot assiste i consulenti del lavoro nella gestione della documentazione HR: un file caricato viene automaticamente suddiviso, riconosciuto tramite OCR e associato al destinatario corretto, lasciando all'operatore solo la revisione dei casi a bassa confidenza invece della classificazione manuale di ogni documento.

L'AI Assistant Generativo assiste i redattori aziendali nella produzione di comunicazioni interne, dal titolo al testo fino all'immagine di copertina, a partire da un semplice prompt: riduce il tempo dedicato alla scrittura senza sottrarre all'utente il controllo editoriale, poiché ogni bozza generata resta modificabile, valutabile e rigenerabile finché non viene scartata.

\subsection{Struttura del documento}

La struttura del presente documento è articolata nelle seguenti sezioni principali:
\begin{itemize}
    \item \textbf{Sezione 1 - Introduzione:} Presenta il contenuto e gli obiettivi del documento, il glossario dei termini e i riferimenti normativi e informativi adottati.
    \item \textbf{Sezione 2 - Tecnologie:} Descrive lo stack tecnologico adottato, suddiviso per categoria: linguaggi, framework, librerie, ambiente locale, edge, servizi cloud AWS, persistenza dei dati, osservabilità e CI/quality gate.
    \item \textbf{Sezione 3 - API:} Descrive il contratto REST esposto dal backend, organizzato per area funzionale (stato applicativo, AI Assistant Generativo, AI Co-Pilot).
    \item \textbf{Sezione 4 - Architettura del Sistema:} Descrive il contesto di sistema, l'architettura applicativa e logica interna del backend, il dettaglio dei componenti, la distribuzione, la containerizzazione e lo stile architetturale complessivo.
    \item \textbf{Sezione 5 - Descrizione architettura (Backend):} Scende al livello delle singole classi dei due domini di prodotto (Documents, Communications): adapter primari, entità di dominio, casi d'uso applicativi e adapter secondari.
    \item \textbf{Sezione 6 - Architettura Frontend:} Descrive i principi di progettazione della SPA Angular, il pattern MVVM adottato, i modelli di dati, i servizi di comunicazione col backend e i componenti UI, micro e macro.
    \item \textbf{Sezione 7 - Design Pattern:} Documenta i design pattern applicati esplicitamente nel backend e nel frontend, con l'intento di ciascuno, dove si trova nel codice e cosa succederebbe senza.
    \item \textbf{Sezione 8 - Database:} Descrive il modello dei dati del sistema: schema ER, schema relazionale e la descrizione di ciascuna tabella.
    \item \textbf{Sezione 9 - Requisiti Funzionali Soddisfatti:} Riporta lo stato di implementazione di ciascun requisito funzionale identificato nell'Analisi dei Requisiti, verificato sul codice sorgente del sistema.
\end{itemize}

\subsection{Glossario}

Al fine di evitare ambiguità e garantire una comprensione uniforme della terminologia utilizzata, i termini tecnici, gli acronimi e le parole con un significato specifico nel contesto del progetto sono contrassegnati nel testo con una ``G'' ad apice (es.\ parola\textsuperscript{\scriptsize G}). La loro definizione completa è consultabile nel documento esterno \textit{Glossario}.

\subsection{Riferimenti}

\subsubsection{Riferimenti normativi}
\begin{itemize}
    \item \textbf{Capitolato d'appalto C5 - NEXUM (Eggon S.r.l.):}\\
    \url{https://www.math.unipd.it/~tullio/IS-1/2025/Progetto/C5.pdf}\\
    \textit{Data Ultimo accesso: 09/07/2026}

    \item \textbf{Norme di Progetto v1.0.0:}\\
    Documento interno del gruppo \textit{aLittleByte} che definisce le regole, i ruoli e le procedure operative adottate nel progetto.\\
    \url{https://alittlebyte-19.github.io/Documentazione/RTB/Norme\%20di\%20Progetto/Norme\%20di\%20Progetto.pdf}\\
    \textit{Data Ultimo accesso: 09/07/2026}

    \item \textbf{Regolamento del Progetto Didattico:}\\
    Anno accademico 2025/2026, Corso di Ingegneria del Software, Università di Padova.\\
    \url{https://www.math.unipd.it/~tullio/IS-1/2025/Dispense/PD1.pdf}\\
    \textit{Data Ultimo accesso: 09/07/2026}
\end{itemize}

\subsubsection{Riferimenti informativi}
\begin{itemize}
    \item \textbf{Glossario v2.0.0:}\\
    Documento interno del gruppo \textit{aLittleByte} contenente le definizioni dei termini tecnici e degli acronimi utilizzati nella documentazione.\\
    \url{https://alittlebyte-19.github.io/Documentazione/RTB/Glossario/Glossario.pdf}\\
    \textit{Data Ultimo accesso: 09/07/2026}

    \item \textbf{Analisi dei Requisiti v2.0.0:}\\
    Documento interno del gruppo \textit{aLittleByte} contenente la descrizione dei requisiti funzionali e non funzionali del sistema.\\
    \url{https://alittlebyte-19.github.io/Documentazione/RTB/Analisi\%20dei\%20Requisiti/Analisi\%20dei\%20Requisiti.pdf}\\
    \textit{Data Ultimo accesso: 09/07/2026}

\end{itemize}

% ─────────────────────────────────────────────────────────────────────────────
\section{Tecnologie}

Il presente capitolo illustra lo stack tecnologico adottato per la progettazione e la realizzazione del sistema Nexum. Le scelte implementative sono state guidate dai requisiti di vincolo definiti nell'Analisi dei Requisiti e dalle indicazioni del proponente Eggon S.r.l. Lo stack è organizzato attorno a cinque macro-aree principali: il runtime applicativo (backend Laravel su PHP e frontend Angular in TypeScript), l'infrastruttura cloud AWS per l'elaborazione intelligente dei documenti e la gestione asincrona dei job, la persistenza dei dati, lo stack di osservabilità e la pipeline di integrazione continua con quality gate automatici. L'ambiente locale riproduce la logica applicativa e l'orchestrazione asincrona di produzione attraverso la containerizzazione con Docker e l'emulazione dei servizi cloud con LocalStack, garantendo riproducibilità senza credenziali reali; fanno eccezione Bedrock e Textract, che parlano sempre con AWS reale, e l'infrastruttura di produzione vera e propria (RDS, ElastiCache, CloudFront), fuori dal perimetro dell'MVP.

% ── 2.1 ──────────────────────────────────────────────────────────────────────
\subsection{Linguaggi di programmazione}

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{9.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    PHP & 8.4 & Linguaggio di scripting server-side utilizzato per la realizzazione del backend tramite il framework Laravel. La versione 8.4 introduce miglioramenti significativi alle performance e nuove funzionalità del linguaggio. \\
    \hline
    TypeScript & 5.9 & Superset tipizzato di JavaScript sviluppato da Microsoft, impiegato per la realizzazione del frontend Angular. La tipizzazione statica riduce gli errori a tempo di compilazione e migliora la manutenibilità del codice. \\
    \hline
    SQL & SQL:2023 & Linguaggio dichiarativo per la definizione e la manipolazione dei dati nel database relazionale PostgreSQL, utilizzato per la gestione della persistenza strutturata del sistema. \\
    \hline
    \caption{Linguaggi di programmazione}
\end{longtable}
}

% ── 2.2 ──────────────────────────────────────────────────────────────────────
\subsection{Framework}

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{9.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    Laravel & 12 & Framework PHP open source per lo sviluppo di applicazioni web server-side. Fornisce routing, un ORM (Eloquent) e strumenti integrati per la gestione dei job asincroni; il prodotto non ne adotta il tipico MVC applicativo, ma li usa come infrastruttura sotto un'architettura esagonale a porte e adapter (Sezione 4.3). È stato preferito a Symfony (di cui riusa diversi componenti sotto il cofano, come HttpFoundation e Routing) per le convenzioni integrate — ORM, code asincrone, CLI Artisan — che riducono la configurazione esplicita richiesta per un MVP con team ridotto, a fronte della maggiore modularità che Symfony offrirebbe. \\
    \hline
    Angular & 21 & Framework open source sviluppato da Google per la creazione di Single Page Application (SPA). Utilizza TypeScript e adotta un'architettura basata su componenti con supporto nativo per dependency injection, routing, reactive forms e signals. È stato preferito per la struttura integrata e opinionata che garantisce coerenza e manutenibilità nel tempo. \\
    \hline
    \caption{Framework}
\end{longtable}
}

% ── 2.3 ──────────────────────────────────────────────────────────────────────
\subsection{Librerie}

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{2cm}|p{7.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Ambito} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Ambito} & \textbf{Descrizione} \\
    \hline
    \endhead
    RxJS & 7.8 & Frontend & Libreria per la programmazione reattiva basata su Observable. Utilizzata per la gestione dello stato asincrono e degli stream di eventi all'interno della SPA Angular, in combinazione con i signals nativi di Angular. \\
    \hline
    @lucide/angular & 1.28 & Frontend & Libreria di icone open source integrata nel frontend Angular. Fornisce un set di icone SVG consistente e accessibile per l'interfaccia utente. \\
    \hline
    AWS SDK for PHP & 3.380 & Backend & SDK ufficiale AWS per PHP. Utilizzata dal backend Laravel per invocare Bedrock, Textract, S3, SQS, Step Functions, SSM Parameter Store e Secrets Manager tramite un'unica libreria client. \\
    \hline
    Flysystem S3 (league/flysystem-aws-s3-v3) & 3.32 & Backend & Adapter S3 per il filesystem astratto di Laravel. Consente di trattare i bucket S3 (o LocalStack) come un disco Laravel standard, cambiando solo la configurazione tra ambienti. \\
    \hline
    Opis JSON Schema & 2.6 & Backend & Libreria per la validazione di payload JSON contro uno schema. Utilizzata per validare la struttura dei dati restituiti dai modelli Bedrock, sia nella pipeline di Documents (split e estrazione campi) sia nella generazione testuale di Communications. \\
    \hline
    FPDI & 2.6 & Backend & Libreria PHP per l'importazione di pagine da documenti PDF esistenti. Utilizzata nella pipeline documentale per estrarre pagine specifiche da PDF massivi prima dello split. \\
    \hline
    FPDF & 1.8 & Backend & Libreria PHP per la generazione di documenti PDF. Utilizzata in combinazione con FPDI per produrre i sotto-documenti PDF per range di pagine a partire dai file originali. \\
    \hline
    Dompdf & 3.1 & Backend & Libreria PHP per il rendering di HTML/Blade in PDF. Genera il PDF del messaggio di invio (Co-Pilot) e l'esportazione della comunicazione (Assistant), entrambi con lo stesso timbro di piè di pagina condiviso. \\
    \hline
    qpdf & --- & Backend & Binario di sistema per l'ispezione strutturale dei PDF (non una libreria Composer). Verifica in upload che il file non sia cifrato e che la struttura interna sia valida prima di accettarlo nella pipeline. \\
    \hline
    \caption{Librerie}
\end{longtable}
}

% ── 2.4 ──────────────────────────────────────────────────────────────────────
\subsection{Contratto API}

Il frontend non conosce gli endpoint del backend in modo statico: il contratto OpenAPI 3.1.1 costituisce la fonte di verità condivisa da cui viene generato automaticamente il client TypeScript tipizzato, mantenendo frontend e backend allineati durante l'evoluzione del sistema.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{9.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    OpenAPI & 3.1.1 & Specifica standard per la descrizione delle API REST. Costituisce il contratto condiviso tra frontend e backend e rappresenta la fonte di verità da cui vengono derivati client e documentazione. \\
    \hline
    Orval & 8.23 & Strumento di code generation che, a partire dalla specifica OpenAPI 3.1.1, genera automaticamente il client TypeScript tipizzato per il frontend Angular, eliminando la scrittura manuale delle chiamate HTTP. \\
    \hline
    Redocly CLI & latest & Strumento per la validazione e il linting del contratto OpenAPI, eseguito in pipeline sempre nell'ultima versione disponibile. Verifica la conformità della specifica agli standard e individua inconsistenze prima che vengano propagate al codice generato. \\
    \hline
    \caption{Strumenti per il contratto API}
\end{longtable}
}

% ── 2.5 ──────────────────────────────────────────────────────────────────────
\subsection{Ambiente locale e provisioning}

L'ambiente locale è progettato in modo che ogni membro del team lavori sulla stessa identica configurazione, vicina a quella di produzione, senza dover creare account cloud o installare servizi manualmente. I servizi AWS vengono emulati in locale tramite LocalStack, mentre l'infrastruttura è descritta come codice con Terraform, rendendola versionata e ricreabile.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3.5cm}|p{1.8cm}|p{9.2cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    Docker & --- & Piattaforma di containerizzazione utilizzata per costruire le immagini dell'applicazione e del web server. Garantisce che l'ambiente di esecuzione sia identico per tutti i membri del team e coerente con la produzione. \\
    \hline
    Docker Compose & --- & Strumento per l'orchestrazione dei container locali. Avvia l'intera infrastruttura (applicazione, database, servizi di supporto) su reti separate con un singolo comando. \\
    \hline
    Make & --- & Automazione dei comandi ricorrenti tramite Makefile. Garantisce che le procedure di setup, avvio e build siano identiche per tutti i membri del team. \\
    \hline
    Composer & --- & Gestore delle dipendenze PHP. Gestisce le librerie del backend e le relative versioni, garantendo riproducibilità delle build. \\
    \hline
    Node.js & 22 & Runtime JavaScript utilizzato dagli strumenti di build e sviluppo del frontend (Angular CLI, npm, Orval, ESLint, Jest). Eseguito in un container dedicato, non richiede un'installazione locale sulla macchina dello sviluppatore. \\
    \hline
    LocalStack & 4.5 & Emulatore locale dei servizi AWS. Riproduce in locale S3, SQS, Step Functions, SSM, Secrets Manager, KMS, IAM, EventBridge e SES, consentendo di testare la pipeline asincrona senza credenziali reali. Bedrock e Textract restano eccezione deliberata: parlano sempre con AWS reale (Sezione 2.7). \\
    \hline
    Terraform & 1.10.5 & Strumento di Infrastructure as Code (IaC) per il provisioning dell'infrastruttura. Sostituisce gli script manuali con una descrizione dichiarativa e versionata, ricreabile a comando tramite \texttt{terraform apply}. \\
    \hline
    AWS SSM Parameter Store & --- & Servizio AWS per la gestione della configurazione runtime non sensibile. I parametri vengono letti dall'applicazione all'avvio, mantenendo la configurazione fuori dal codice sorgente. \\
    \hline
    AWS Secrets Manager & --- & Servizio AWS per la gestione dei segreti applicativi (credenziali, chiavi API). Mantiene i dati sensibili separati dal codice e dalla configurazione. \\
    \hline
    \caption{Strumenti per l'ambiente locale e il provisioning}
\end{longtable}
}

% ── 2.6 ──────────────────────────────────────────────────────────────────────
\subsection{Edge e web server}

Una richiesta HTTPS attraversa diversi livelli prima di raggiungere il codice applicativo. Traefik opera come entry point TLS esterno; in locale un secondo Nginx (\textit{edge-cdn}) emula il ruolo di una CDN, servendo la SPA Angular direttamente da S3/LocalStack e inoltrando le richieste \texttt{/api}, \texttt{/health} e \texttt{/ready} all'Nginx applicativo, che a sua volta fa da proxy verso PHP-FPM. In produzione il ruolo di CDN è coperto da AWS CloudFront, mentre l'Nginx applicativo resta un'immagine dedicata esclusivamente al proxy verso PHP-FPM.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{9.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    Traefik & 3.4 & Reverse proxy posizionato davanti a tutti i servizi. Si occupa della terminazione TLS e dello smistamento delle richieste in base all'host. \\
    \hline
    Nginx (applicativo) & 1.27 & Web server utilizzato come proxy per le chiamate \texttt{/api} verso PHP-FPM tramite FastCGI. Immagine di produzione, buildata e scansionata, che non conosce LocalStack. \\
    \hline
    Nginx (edge-cdn) & 1.29 & Secondo Nginx, presente solo in locale, che emula il ruolo di una CDN/edge davanti a S3-LocalStack per servire gli asset statici della SPA Angular. In produzione questo ruolo è sostituito da AWS CloudFront. \\
    \hline
    PHP-FPM & 8.4 & Runtime PHP con processo FastCGI. Esegue il codice dell'applicazione Laravel. È stato preferito a FrankenPHP e Laravel Octane per un comportamento più prevedibile e una gestione dei processi più semplice in fase di PoC. \\
    \hline
    \caption{Edge e web server}
\end{longtable}
}

% ── 2.7 ──────────────────────────────────────────────────────────────────────
\subsection{Servizi cloud AWS}

Le funzionalità di intelligenza artificiale, l'elaborazione asincrona dei documenti e la protezione dei dati si basano su servizi gestiti AWS. L'adozione di servizi gestiti elimina la necessità di infrastrutture dedicate, garantendo scalabilità automatica e alta disponibilità.

In locale, S3, SQS, Step Functions, SSM, Secrets Manager, KMS, IAM, EventBridge e SES sono emulati da LocalStack (Sezione 2.5). Bedrock e Textract restano invece un'eccezione deliberata: parlano sempre con AWS reale, anche in locale, perché LocalStack Community non offre un'emulazione dei modelli generativi utile ai fini del progetto. Textract è disattivato di default (\texttt{TEXTRACT\_ENABLED=false}): l'OCR reale va abilitato esplicitamente fornendo credenziali AWS vere, mentre Bedrock resta sempre attivo perché è il cuore delle due funzionalità AI del prodotto.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3.5cm}|p{1.8cm}|p{9.2cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    Amazon Bedrock & --- & Servizio AWS per l'accesso a modelli linguistici di grandi dimensioni (LLM). Di default utilizza Amazon Nova Lite per la generazione dei testi e Stability SD3.5 Large per le immagini di copertina nell'AI Assistant Generativo, oltre che per la classificazione e lo split automatico dei documenti nell'AI Co-Pilot. \\
    \hline
    Amazon Textract & --- & Servizio AWS di riconoscimento ottico dei caratteri (OCR). Estrae testo e dati strutturati da documenti scansionati o immagini, leggendo i file direttamente da S3. \\
    \hline
    AWS Step Functions & --- & Servizio di orchestrazione delle pipeline asincrone. Due state machine distinte coordinano rispettivamente la pipeline documentale (OCR, estrazione AI, persistenza risultati) e la pipeline delle comunicazioni (generazione testo/copertina), gestendo tentativi, timeout e stato tramite task token. È stato preferito a soluzioni self-hosted come Temporal o Camunda. \\
    \hline
    Amazon SQS & --- & Servizio di code di messaggi gestite. Sono presenti due code separate, una per la pipeline documentale e una per quella delle comunicazioni, scelte per la loro durabilità e per la gestione automatica dei messaggi falliti. Un Worker dedicato per pipeline consuma i messaggi dalla propria coda e risponde all'orchestratore Step Functions con un token di callback. \\
    \hline
    Amazon SQS DLQ & --- & Dead Letter Queue: una coda secondaria SQS per pipeline che raccoglie automaticamente i messaggi che hanno superato il numero massimo di tentativi (\texttt{maxReceiveCount}), consentendo l'analisi e il reindirizzamento dei task falliti. \\
    \hline
    Amazon S3 & --- & Servizio di object storage. Un bucket dedicato archivia i documenti originali, i sotto-documenti generati dalla pipeline e le copertine delle comunicazioni, con oggetti cifrati at-rest tramite KMS; un secondo bucket ospita gli asset statici della SPA Angular, serviti dall'edge locale o da CloudFront in produzione. L'accesso a entrambi è regolato da policy IAM. \\
    \hline
    AWS KMS & --- & Servizio di gestione delle chiavi crittografiche. Utilizzato per la cifratura at-rest degli oggetti del bucket documentale, garantendo la protezione dei documenti sensibili. \\
    \hline
    AWS IAM & --- & Servizio di gestione delle identità e degli accessi. Definisce i ruoli di esecuzione per Step Functions e le policy di accesso alle risorse, seguendo il principio del minimo privilegio. \\
    \hline
    \caption{Servizi cloud AWS}
\end{longtable}
}

% ── 2.8 ──────────────────────────────────────────────────────────────────────
\subsection{Tecnologie per la persistenza dei dati}

I dati del sistema hanno nature diverse e vengono gestiti con strumenti dedicati: PostgreSQL per la persistenza strutturata e transazionale, Redis per lo stato volatile ad accesso rapido.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3cm}|p{1.8cm}|p{9.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Descrizione} \\
    \hline
    \endhead
    PostgreSQL & 16 & Sistema di gestione di database relazionali open source. Conserva i dati strutturati dell'applicazione (documenti, estratti, audit, stato dei task) e, grazie al supporto JSONB, gestisce anche payload semi-strutturati senza cambiare database. È stato preferito a MySQL e a soluzioni documentali come MongoDB per affidabilità e flessibilità. Nel repository gira come container (\texttt{postgres:16-alpine}); un servizio gestito in cloud (RDS) è l'evoluzione naturale per una distribuzione reale, ma resta fuori dal perimetro dell'MVP. \\
    \hline
    Redis & 7 & Data store in-memory open source. Gestisce la cache applicativa, le sessioni utente e il rate limiting, sempre con scadenza. Non viene utilizzato come coda della pipeline, ruolo affidato ad Amazon SQS. Nel repository gira come container (\texttt{redis:7-alpine}); un servizio gestito in cloud (ElastiCache) è ugualmente fuori dal perimetro dell'MVP. \\
    \hline
    \caption{Tecnologie per la persistenza dei dati}
\end{longtable}
}

% ── 2.9 ──────────────────────────────────────────────────────────────────────
\subsection{Osservabilità}

Il sistema affida l'osservabilità operativa a tre meccanismi applicativi, senza uno stack di monitoraggio dedicato né un collector:

\begin{itemize}
    \item \textbf{Audit trail di compliance}: \texttt{AuditLogger} (Sezione 5.3) registra ogni evento rilevante (upload, elaborazione, revisione, generazione, valutazione...) sulla tabella \texttt{audit\_events}, append-only (Sezione 8). Non è un sistema di monitoraggio operativo ma un registro di conformità: risponde a "chi ha fatto cosa, quando", non a "il sistema sta bene adesso?".
    \item \textbf{Correlation/request ID}: il middleware \texttt{CorrelateRequests} lato backend, con l'equivalente lato frontend nella cartella \texttt{core} (Sezione 6.1), taggano ogni richiesta HTTP e ogni messaggio SQS con un identificativo di correlazione, propagato nei log strutturati per ricostruire il percorso di una richiesta attraverso i tre processi backend (API, due Worker).
    \item \textbf{Metriche operative verso CloudWatch}: \texttt{EmfMetricsRecorder} scrive righe in CloudWatch Embedded Metric Format (EMF) su un canale di log dedicato (\texttt{metrics}); CloudWatch le interpreta automaticamente come dato di metrica quando la riga arriva da CloudWatch Logs, senza endpoint di scrape né SDK applicativo verso \texttt{PutMetricData}. Il listener \texttt{RecordRequestMetrics} lo invoca a ogni richiesta HTTP (evento nativo \texttt{RequestHandled}, non un middleware: un'eccezione che risale la pipeline farebbe comunque terminare la richiesta prima che il codice dopo \texttt{\$next()} di un middleware venga eseguito) per \texttt{RequestCount}, \texttt{Latency} ed \texttt{Errors}; il comando \texttt{php artisan mvp:metrics:dlq-depth} lo invoca per \texttt{DlqDepth}, una per pipeline.
\end{itemize}

I log strutturati sul canale \texttt{stderr} sono già in formato JSON con i campi di correlazione iniettati da \texttt{CorrelateRequests}: instradabili verso CloudWatch Logs tramite il log driver del container host, senza chiamate SDK applicative dedicate. La diagnosi di un incidente passa da questi log e da due comandi diagnostici manuali: \texttt{php artisan mvp:dlq:list}, che interroga direttamente SQS per ispezionare il contenuto di una DLQ, e il già citato \texttt{mvp:metrics:dlq-depth} per la sola profondità.

Le metriche mostrate agli utenti nei pannelli Angular (Assistant, Co-Pilot, Overview — Sezione 6.2, 6.6) sono un sistema distinto dai precedenti: \texttt{MvpStateService} le calcola sull'intero tenant a ogni \texttt{GET /api/v1/state}, e la SPA le legge da lì.

% ── 2.10 ─────────────────────────────────────────────────────────────────────
\subsection{CI e quality gate}

Ogni modifica inviata al repository attiva la pipeline di integrazione continua su GitHub Actions, che ricostruisce le immagini e le pubblica su GHCR solo dopo che tutti i controlli automatici sono stati superati. I quality gate coprono stile del codice, analisi statica, test, sicurezza delle dipendenze e accessibilità.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{3.5cm}|p{1.8cm}|p{2.5cm}|p{6.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Categoria} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Nome} & \textbf{Versione} & \textbf{Categoria} & \textbf{Descrizione} \\
    \hline
    \endhead
    GitHub Actions & --- & CI/CD & Orchestratore della pipeline CI. Attivato da push e pull request, esegue in parallelo quattro job: \texttt{backend}, \texttt{frontend}, \texttt{coverage-diff} e \texttt{stack}. \\
    \hline
    GHCR & --- & CI/CD & GitHub Container Registry: registro in cui vengono pubblicate le immagini Docker solo dopo il superamento di tutti i quality gate. \\
    \hline
    Docker Hub & --- & CI/CD & Utilizzato esclusivamente per il pull autenticato delle immagini base, al fine di evitare i limiti di rate anonimo. Non è il registro di pubblicazione delle immagini prodotte. \\
    \hline
    Trivy & 0.66.0 & Sicurezza & Scanner di vulnerabilità per immagini Docker. Analizza le immagini costruite (vulnerabilità, secret, configurazione) prima della pubblicazione su GHCR. \\
    \hline
    npm audit & --- & Sicurezza & Analisi delle vulnerabilità nelle dipendenze npm di produzione del frontend Angular. \\
    \hline
    Pint & 1.29 & Qualità backend & Formattatore e linter per il codice PHP, basato sulle convenzioni di Laravel. \\
    \hline
    PHPStan 2.2 / Larastan 3.10 & --- & Qualità backend & Strumento di analisi statica per PHP (PHPStan) con estensione specifica per Laravel (Larastan). Individua errori di tipo e problemi di logica senza eseguire il codice. \\
    \hline
    Pest & 4.7 & Qualità backend & Framework di testing per PHP utilizzato per i test unitari e di integrazione del backend Laravel. \\
    \hline
    ESLint & 9.39 & Qualità frontend & Linter per il codice TypeScript della SPA Angular. Verifica la conformità alle regole di stile e individua potenziali errori. \\
    \hline
    Jest & 30.4 & Qualità frontend & Framework di testing JavaScript utilizzato per i test unitari del frontend Angular, in combinazione con TypeScript. \\
    \hline
    Redocly + Orval diff & Orval 8.23 / Redocly latest & Contratto API & Redocly (sempre ultima versione) valida il contratto OpenAPI e Orval rigenera il client tipizzato; un diff verifica che il client generato sia allineato con il contratto aggiornato. \\
    \hline
    terraform validate & 1.10.5 & IaC & Valida formato e correttezza sintattica/semantica della configurazione Terraform prima di qualsiasi applicazione. \\
    \hline
    axe & 4.13 & Accessibilità & Strumento di testing automatico per l'accessibilità (a11y) dell'interfaccia utente, integrato nella pipeline CI. \\
    \hline
    pa11y & 9.1 & Accessibilità & Strumento complementare ad axe per il testing dell'accessibilità. Esegue test a11y sulle pagine dell'applicazione. \\
    \hline
    Playwright & 1.60 & Accessibilità & Framework per l'automazione di un browser reale (Chromium). Utilizzato per eseguire gli audit di accessibilità con axe e Pa11y e per verificare che la SPA funzioni correttamente con la Content Security Policy applicata. \\
    \hline
    \caption{Strumenti per CI e quality gate}
\end{longtable}
}

% ─────────────────────────────────────────────────────────────────────────────
\section{API}

Il sistema espone un contratto REST versionato (\texttt{/api/v1}), definito tramite la specifica OpenAPI 3.1.1 in \texttt{openapi/v1/alittlebyte-mvp-api.yaml} e consumato dalla SPA Angular tramite il client generato con Orval. L'identità (\texttt{MvpIdentity}: attore e tenant) è risolta in due modalità alternative, selezionate da \texttt{MVP\_IDENTITY\_MODE}: in modalità \textit{local} (default, usata in sviluppo e nella MVP) l'identità è iniettata da configurazione e nessun header è richiesto; in modalità \textit{trusted\_headers} la richiesta deve portare \texttt{X-Mvp-User-Id} insieme a \texttt{X-Mvp-Tenant-Id} e agli altri header fidati, iniettati dal confine aziendale a monte dell'applicazione.

Il rate limiting è granulare per gruppo di endpoint. Il limite di default è 60 richieste al minuto; scende a 20/minuto per le operazioni di generazione e scrittura più onerose (creazione bozze, rigenerazione, upload OCR, aggiornamento copertina, salvataggio configurazioni di prompt, salvataggio e scarto della bozza) e a 30/minuto per anteprima, export e valutazione delle comunicazioni (le rotte equivalenti sui documenti restano al limite di default). Sale invece a 120/minuto per la sola lettura della copertina già generata. Gli endpoint che restituiscono un PDF (anteprima ed export delle comunicazioni) supportano la cache HTTP tramite \texttt{ETag}/\texttt{If-None-Match}, evitando di rigenerare il documento se il client possiede già l'ultima versione.

L'avanzamento delle elaborazioni asincrone (generazione contenuti, pipeline documentale) è notificato alla SPA tramite un endpoint Server-Sent Events (SSE), senza dover interrogare ripetutamente il server. Le liste di comunicazioni e documenti supportano filtri lato server e paginazione.

Le API sono organizzate in tre aree funzionali, corrispondenti ai tag della specifica OpenAPI: lo stato applicativo, l'AI Assistant Generativo (comunicazioni) e l'AI Co-Pilot (documenti).

% ── 3.1 ──────────────────────────────────────────────────────────────────────
\subsection{State API}

Espone lo stato corrente dei due moduli applicativi, utilizzato dalla SPA per popolare le viste principali senza richieste multiple.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{6cm}|p{1.8cm}|p{6.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endhead
    \path|/api/v1/state| & GET & Restituisce in un'unica risposta le metriche e lo storico delle comunicazioni dell'AI Assistant Generativo, insieme alle metriche e ai sotto-documenti dell'AI Co-Pilot. Accetta quattro parametri di query opzionali (\texttt{tone}, \texttt{style}, \texttt{dateFrom}, \texttt{dateTo}) che filtrano solo la sezione Metriche dell'AI Assistant, senza incidere sui numeri del Co-Pilot. \\
    \hline
    \caption{State API}
\end{longtable}
}

% ── 3.2 ──────────────────────────────────────────────────────────────────────
\subsection{Communications API}

Gestione del ciclo di vita completo di una comunicazione generata dall'AI Assistant Generativo: richiesta della bozza, revisione e modifica, rigenerazione, salvataggio o scarto, valutazione, esportazione e gestione della copertina. Include inoltre la gestione delle configurazioni di prompt salvate per il riuso.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{6cm}|p{1.8cm}|p{6.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endhead
    \path|/api/v1/communications| & GET & Lista delle bozze approvate del tenant (\texttt{status = approved}), con filtri opzionali (parola chiave nel prompt, tono, stile, data) e paginazione; le bozze non ancora salvate e quelle scartate non compaiono. \\
    \hline
    \path|/api/v1/communications| & POST & Richiede una bozza di comunicazione a partire da prompt, tono e stile; avvia in modo asincrono la pipeline delle comunicazioni e restituisce l'identificativo della comunicazione e l'URL di streaming. \\
    \hline
    \path|/api/v1/communications/{communication}| & PUT & Aggiorna titolo e corpo di una bozza di comunicazione. \\
    \hline
    \path|/api/v1/communications/{communication}| & DELETE & Elimina definitivamente una comunicazione dallo storico. \\
    \hline
    \path|/api/v1/communications/{communication}/regenerate| & POST & Rigenera la bozza di comunicazione riutilizzando prompt, tono e stile già persistiti; la richiesta non accetta un body. \\
    \hline
    \path|/api/v1/communications/{communication}/save| & POST & Salva la bozza nello storico, lasciandola comunque modificabile e rigenerabile. \\
    \hline
    \path|/api/v1/communications/{communication}/discard| & POST & Scarta la bozza di comunicazione. \\
    \hline
    \path|/api/v1/communications/{communication}/favorite| & POST & Aggiunge la comunicazione ai preferiti. \\
    \hline
    \path|/api/v1/communications/{communication}/favorite| & DELETE & Rimuove la comunicazione dai preferiti. \\
    \hline
    \path|/api/v1/communications/{communication}/rating| & POST & Assegna una valutazione da 1 a 5 stelle alla bozza generata; valutazioni successive per la stessa generazione vengono rifiutate. \\
    \hline
    \path|/api/v1/communications/{communication}/stream| & GET & Endpoint SSE che notifica alla SPA l'avanzamento della generazione (testo e copertina) per una comunicazione specifica. \\
    \hline
    \path|/api/v1/communications/{communication}/cover-image| & GET & Download dell'immagine di copertina generata per la comunicazione (PNG, JPEG o WebP). \\
    \hline
    \path|/api/v1/communications/{communication}/cover-image| & POST & Sostituisce manualmente l'immagine di copertina generata dall'AI, caricando un file. \\
    \hline
    \path|/api/v1/communications/{communication}/cover-image| & DELETE & Rimuove l'immagine di copertina associata alla comunicazione. \\
    \hline
    \path|/api/v1/communications/{communication}/preview| & GET & Anteprima del PDF impaginato della comunicazione, con supporto \texttt{ETag} per evitare rigenerazioni non necessarie. \\
    \hline
    \path|/api/v1/communications/{communication}/export| & GET & Download del PDF finale della comunicazione come allegato, con lo stesso supporto \texttt{ETag} dell'anteprima. \\
    \hline
    \path|/api/v1/prompt-configurations| & POST & Salva la configurazione corrente di prompt (tono, stile, testo) per un riutilizzo successivo. \\
    \hline
    \path|/api/v1/prompt-configurations/{promptConfiguration}| & DELETE & Elimina definitivamente una configurazione di prompt salvata. \\
    \hline
    \path|/api/v1/assistant/metrics/export| & GET & Download del report riepilogativo delle metriche dell'AI Assistant come PDF allegato. \\
    \hline
    \caption{Communications API}
\end{longtable}
}

% ── 3.3 ──────────────────────────────────────────────────────────────────────
\subsection{Documents API}

Gestione del ciclo di vita dei documenti caricati dall'AI Co-Pilot, della relativa pipeline di elaborazione asincrona (OCR, estrazione, revisione) e della composizione del messaggio di invio verso il destinatario individuato.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\begin{longtable}{|p{6cm}|p{1.8cm}|p{6.7cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Endpoint} & \textbf{Metodo} & \textbf{Descrizione} \\
    \hline
    \endhead
    \path|/api/v1/documents| & GET & Lista dei sotto-documenti del tenant, con filtri opzionali (ricerca su nome/cognome/azienda, stato di invio, soglia e criterio di confidenza, mese/anno) e paginazione. \\
    \hline
    \path|/api/v1/documents/ocr| & POST & Carica un documento e avvia in modo asincrono la pipeline documentale (OCR tramite Textract se abilitato, altrimenti un passo vuoto; estrazione e classificazione tramite Bedrock); restituisce l'URL di streaming per seguirne l'avanzamento. \\
    \hline
    \path|/api/v1/documents/{originalDocument}/stream| & GET & Endpoint SSE che notifica alla SPA l'avanzamento dell'elaborazione per un documento originale specifico. \\
    \hline
    \path|/api/v1/documents/{subDocument}| & DELETE & Elimina un sotto-documento già elaborato dalla pipeline. \\
    \hline
    \path|/api/v1/documents/{subDocument}/extracted-data| & PUT & Corregge i dati estratti automaticamente per un sotto-documento (nominativo, azienda, email destinatario, codice fiscale, id dipendente, tipo documento, data, descrizione, punteggio di confidenza), con un flag opzionale per marcarlo contestualmente come validato manualmente. \\
    \hline
    \path|/api/v1/documents/{subDocument}/review| & POST & Segna un sotto-documento come validato manualmente, ad esempio a valle di una revisione human-in-the-loop per i casi a bassa confidenza. \\
    \hline
    \path|/api/v1/documents/{subDocument}/preview| & GET & Restituisce l'anteprima in formato PDF del sotto-documento. \\
    \hline
    \path|/api/v1/documents/{subDocument}/original-preview| & GET & Restituisce l'anteprima in formato PDF del documento originale completo, non splittato, alternativa indipendente quando quella del sotto-documento non è disponibile. \\
    \hline
    \path|/api/v1/documents/{subDocument}/send-preview| & GET & Anteprima PDF del messaggio di invio precompilato per il sotto-documento. \\
    \hline
    \path|/api/v1/documents/{subDocument}/send-export| & GET & Download del messaggio di invio precompilato come PDF allegato. \\
    \hline
    \path|/api/v1/documents/{subDocument}/send-message| & PUT & Corregge i campi del messaggio di invio precompilato per il sotto-documento. \\
    \hline
    \caption{Documents API}
\end{longtable}
}

% ─────────────────────────────────────────────────────────────────────────────
\section{Architettura del Sistema}

Questo capitolo descrive l'architettura del sistema, dal generale al particolare: contesto, architettura applicativa, architettura logica interna del backend, dettaglio dei componenti, distribuzione, containerizzazione e stile architetturale complessivo.

% ── 4.1 ──────────────────────────────────────────────────────────────────────
\subsection{Contesto di Sistema}

L'utente finale interagisce con la piattaforma tramite browser, via HTTPS. Il sistema delega l'elaborazione intelligente dei contenuti a due servizi AWS esterni: l'AI Co-Pilot invia i documenti caricati ad AWS Textract per il riconoscimento ottico dei caratteri; l'AI Assistant Generativo invia i prompt ad AWS Bedrock per la generazione di testi e immagini. Le integrazioni con AWS avvengono esclusivamente lato backend; il browser comunica solo con il sistema.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/Esagonale/context.png}
    \caption{System Context Diagram}
    \label{fig:system-context}
\end{figure}

% ── 4.2 ──────────────────────────────────────────────────────────────────────
\subsection{Architettura Applicativa}

Il sistema si compone dei seguenti container principali. Il frontend è una Single Page Application in Angular 21. Il backend è un'API in Laravel 12 su PHP 8.4, servita da PHP-FPM e Nginx, che espone esclusivamente un contratto REST in formato JSON, senza rendering lato server. Le operazioni pesanti (OCR e invocazione dei modelli generativi) sono delegate in modo asincrono ad AWS Step Functions e Amazon SQS; due processi Worker separati dall'API, uno per pipeline, consumano i messaggi e coordinano l'esecuzione: la coda documentale è servita da un container dedicato, quella delle comunicazioni da un secondo container, così i due flussi non si contendono gli stessi consumatori. La persistenza è affidata a PostgreSQL per i dati applicativi e a Redis per cache, sessioni e rate limiting; nessuno dei due è utilizzato come coda, ruolo riservato a SQS.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{ImmaginiSpecificaTecnica/Esagonale/container.png}
    \caption{Container Diagram}
    \label{fig:container-diagram}
\end{figure}

% ── 4.3 ──────────────────────────────────────────────────────────────────────
\subsection{Architettura Logica Interna}

Il frontend è dedicato all'interfaccia utente. Il backend è organizzato per dominio applicativo secondo i principi dell'architettura esagonale.

\subsubsection{Frontend: Interfaccia Utente}

Sviluppato in Angular come Single Page Application, gestisce l'interfaccia e l'interazione con l'utente finale.

\begin{itemize}
    \item Gestisce gli input dell'utente (caricamento documenti, richieste di generazione, revisione dei dati estratti).
    \item Comunica con il backend tramite chiamate API RESTful, generate a partire dal contratto OpenAPI condiviso.
    \item Riceve l'avanzamento delle elaborazioni asincrone tramite Server-Sent Events, senza interrogare ripetutamente il server.
\end{itemize}

\subsubsection{Backend: Architettura Esagonale}

Il backend adotta un'architettura esagonale, applicata ai due domini di prodotto: \texttt{Documents} (AI Co-Pilot) e \texttt{Communications} (AI Assistant Generativo). La logica di business è disaccoppiata dai dettagli tecnici (framework, database, SDK esterni) per garantire manutenibilità, testabilità e sostituibilità dei singoli componenti. Il backend si articola in tre parti.

\paragraph{Adapter primari}
Ricevono uno stimolo esterno e lo traducono in una chiamata a un caso d'uso, senza contenere regole di business. Nella forma più comune sono i Controller HTTP. Il gestore dei task di workflow è un adapter primario equivalente, guidato da un messaggio SQS o da una callback di Step Functions invece che da una richiesta HTTP.

\paragraph{Dominio e applicazione}
Il dominio contiene le regole di business e le porte che definiscono le capacità del sistema, senza dipendenze da Eloquent, da un client AWS o da HTTP. Le tre aggregazioni principali (\texttt{SubDocument}, \texttt{OriginalDocument}, \texttt{Communication}) sono entità di dominio a sé stanti, distinte dai model Eloquent: incapsulano le proprie transizioni di stato e le relative regole (es. una copertina non può essere generata prima del testo) invece di lasciarle implicite nei singoli servizi applicativi. L'applicazione implementa le porte primarie orchestrando dominio ed entità esclusivamente attraverso le porte secondarie.

\paragraph{Adapter secondari}
Implementano la comunicazione con i sistemi esterni: il database PostgreSQL tramite repository Eloquent, i modelli AI tramite gli adapter di Bedrock e Textract, lo storage documentale tramite Flysystem e S3.

\paragraph{Esempio: il dominio Documents}
Un Controller riceve l'upload, lo valida e delega l'esecuzione al caso d'uso corrispondente, che salva il file e avvia la pipeline asincrona senza conoscere i dettagli di Step Functions. Il Worker raggiunge lo stesso dominio applicativo tramite un adapter primario diverso, guidato dal workflow, e delega ai casi d'uso di OCR, estrazione dei campi e finalizzazione della pipeline. La revisione umana delle estrazioni a bassa confidenza avviene invece in modo sincrono, tramite un Controller HTTP dedicato invocato dall'operatore, non dal Worker. Il risultato è persistito tramite il repository del dominio; un evento dedicato notifica l'audit. Il dominio Communications segue lo stesso schema.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documentExemple.png}
    \caption{Architettura Esagonale --- Esempio Dominio Documents}
    \label{fig:documents-hexagonal-example}
\end{figure}

% ── 4.4 ──────────────────────────────────────────────────────────────────────
\subsection{Dettaglio dei Componenti}

La tabella seguente quantifica la composizione interna dei due domini: casi d'uso, porte verso l'esterno, eventi di dominio e adapter primari.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\small
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{4pt}
\begin{longtable}{|p{2.3cm}|p{1.6cm}|p{2.4cm}|p{1.6cm}|p{5.6cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Dominio} & \textbf{Casi d'uso} & \textbf{Porte secondarie} & \textbf{Eventi} & \textbf{Adapter primario} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Dominio} & \textbf{Casi d'uso} & \textbf{Porte secondarie} & \textbf{Eventi} & \textbf{Adapter primario} \\
    \hline
    \endhead
    Documents & 12 & 6 & 14 & 4 Controller HTTP + \texttt{WorkflowTaskHandler} \\
    \hline
    Communications & 14 & 6 & 21 & 6 Controller HTTP + \texttt{WorkflowTaskHandler} \\
    \hline
    \caption{Metriche strutturali dei due domini esagonali}
    \label{tab:component-metrics}
\end{longtable}
}

I due domini condividono un piccolo numero di porte secondarie puramente infrastrutturali, comuni a entrambi perché non riguardano la logica di business: l'avvio di un'esecuzione Step Functions, la generazione di identificativi univoci, i confini transazionali sulle scritture pure sul database e la segnalazione di attività verso Step Functions durante le chiamate AI più lunghe. Ciascuna ha un solo adapter condiviso.

La separazione fra i due domini è verificata automaticamente a ogni build della pipeline CI: uno script analizza il codice e blocca la build se un dominio referenzia direttamente il framework, l'SDK AWS o l'altro dominio, così i due moduli restano davvero indipendenti.

\subsubsection{Modulo Documents}

Copre l'intera pipeline dell'AI Co-Pilot: dall'ingresso del documento caricato dall'utente, attraverso l'orchestrazione asincrona di riconoscimento ottico, classificazione ed estrazione dei campi, fino alla consultazione, alla revisione umana dei dati estratti e alla composizione del documento precompilato per il destinatario, che l'operatore scarica ed invia fuori piattaforma. Le porte secondarie isolano il dominio dalla persistenza, dai gateway AI di riconoscimento ottico e classificazione, dallo storage documentale, dal rendering del documento di invio e dalla pubblicazione degli eventi di dominio.

Il diagramma seguente si concentra sulla pipeline centrale (upload, orchestrazione asincrona, revisione); i controller e i casi d'uso di sola consultazione (\texttt{DocumentPreviewController}, \allowbreak\texttt{SendMessageController} e i rispettivi casi d'uso di lista, cancellazione e polling) non compaiono qui: il conteggio completo dei 4 Controller HTTP e dei 12 casi d'uso è nella Tabella~\ref{tab:component-metrics} della Sezione 4.4, il dettaglio di ciascuno nella Sezione 5.1.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents.png}
    \caption{Component Diagram --- Pipeline centrale del modulo Documents}
    \label{fig:documents-component-diagram}
\end{figure}

\subsubsection{Modulo Communications}

Copre l'intero ciclo di vita di una bozza generata dall'AI Assistant Generativo: dalla richiesta di generazione (testo e copertina), attraverso l'orchestrazione asincrona della pipeline, fino alla gestione editoriale della bozza (preferiti, modifica, approvazione, scarto), alla sua consultazione, esportazione e valutazione, e alla gestione delle configurazioni di prompt riutilizzabili. Le porte secondarie isolano il dominio dalla persistenza, dal gateway AI generativo, dallo storage delle copertine, dal rendering PDF e dalla pubblicazione degli eventi di dominio.

Il diagramma seguente si concentra sulla pipeline centrale (generazione, orchestrazione asincrona, valutazione); i controller e i casi d'uso di gestione editoriale, consultazione e configurazione (\texttt{CommunicationCoverController}, \texttt{CommunicationExportController}, \texttt{CommunicationStreamController}, \texttt{PromptConfigurationController} e i rispettivi casi d'uso) non compaiono qui: il conteggio completo dei 6 Controller HTTP e dei 14 casi d'uso è nella Tabella~\ref{tab:component-metrics} della Sezione 4.4, il dettaglio di ciascuno nella Sezione 5.2.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/Esagonale/comunication.png}
    \caption{Component Diagram --- Pipeline centrale del modulo Communications}
    \label{fig:communications-component-diagram}
\end{figure}

% ── 4.5 ──────────────────────────────────────────────────────────────────────
\subsection{Architettura di Deployment}

Il backend e la SPA frontend sono distribuiti come container separati, raggiunti da un unico entry point TLS (la catena completa di proxy è descritta in Sezione 2.6). Il backend gira su tre processi che condividono lo stesso nucleo applicativo: l'API, raggiunta via HTTP, e due Worker, uno per la coda documentale e uno per quella delle comunicazioni, che eseguono lo stesso dominio tramite i messaggi SQS e le callback di Step Functions — tre adapter primari sullo stesso sistema, non sistemi separati.

In locale l'intera topologia, orchestrazione Step Functions/SQS inclusa, è riprodotta con Docker Compose, LocalStack e Terraform: stessa API applicativa, stessi due Worker, stessa orchestrazione Step Functions/SQS emulata. Bedrock e Textract restano un'eccezione deliberata (Sezione 2.7), e un deploy di produzione reale (RDS, ElastiCache, CloudFront) resta fuori dal perimetro dell'MVP (Sezioni 2.6, 2.8).

% ── 4.6 ──────────────────────────────────────────────────────────────────────
\subsection{Containerizzazione con Docker}

L'intero stack è containerizzato; i dettagli di ciascun servizio sono descritti nella Sezione 2. I container principali sono:

\begin{itemize}
    \item \textbf{Frontend (SPA):} esegue la build statica Angular, servita dall'edge; nessuna logica di business.
    \item \textbf{Backend (API):} esegue PHP-FPM dietro Nginx, gestisce le richieste HTTP verso il dominio applicativo.
    \item \textbf{Worker documenti e Worker comunicazioni:} due container separati che eseguono lo stesso dominio applicativo del backend, in ascolto rispettivamente sulla coda SQS documentale e su quella delle comunicazioni, anziché su HTTP.
    \item \textbf{Database (PostgreSQL) e Cache (Redis):} persistenza applicativa e stato volatile, su volumi Docker dedicati.
    \item \textbf{Emulazione AWS (LocalStack) e provisioning (Terraform):} riproducono in locale, senza credenziali reali, i servizi AWS emulabili (Sezione 2.7); Bedrock e Textract restano un'eccezione e parlano sempre con AWS reale.
\end{itemize}

Il pool PHP-FPM del container Backend è dimensionato esplicitamente (\texttt{docker/\allowbreak php/\allowbreak www-pool.conf}), invece di restare sul default dell'immagine base (\texttt{pm.max\_children=5}). Il rischio non era il timeout di Traefik o di Nginx verso lo stream SSE (l'heartbeat inviato ogni secondo lo tiene vivo), ma il pool PHP-FPM stesso: ogni connessione SSE occupa un worker per l'intera durata dello stream (fino a 1800 secondi per i documenti), quindi pochi upload concorrenti avrebbero esaurito l'intero pool, bloccando anche l'endpoint \texttt{/health} per tutte le richieste in arrivo.

% ── 4.7 ──────────────────────────────────────────────────────────────────────
\subsection{Stile architetturale}

Lo stile architetturale del backend è un monolite modulare organizzato per dominio: un nucleo esagonale limitato ai due domini di prodotto, circondato da un'infrastruttura condivisa che non vi è assoggettata. Tre caratteristiche lo definiscono:

\begin{itemize}
    \item \textbf{Testabilità del dominio senza framework:} i casi d'uso di Documents e Communications sono istanziabili con \texttt{new}, senza container, database o LocalStack; la suite di test dedicata usa implementazioni in memoria delle porte secondarie al posto di quelle reali.
    \item \textbf{Orchestrazione asincrona esternalizzata:} le operazioni onerose non transitano dal sistema di code nativo di Laravel, ma da Step Functions e SQS; l'adapter che le riceve è trattato allo stesso livello di un Controller HTTP.
    \item \textbf{Conformità verificata automaticamente:} la Dependency Rule è un controllo eseguito a ogni build, non una convenzione documentata.
\end{itemize}

Il perimetro dell'esagonale resta limitato per scelta esplicita: Identity, Audit, Observability, Support e l'infrastruttura condivisa di Workflow restano organizzati per servizio, perché nessuno di essi ha oggi, né è previsto abbia nell'arco della MVP, un'implementazione alternativa da rendere sostituibile.

% ─────────────────────────────────────────────────────────────────────────────
\section{Descrizione architettura (Backend)}

Questo capitolo scende al livello delle singole classi che compongono il nucleo dei due domini di prodotto, completando la vista d'insieme del capitolo precedente. Per ciascun modulo sono descritti l'adapter primario, le entità di dominio, tutti i casi d'uso applicativi e gli adapter secondari, seguendo lo stesso ordine in cui intervengono nella pipeline descritta dal diagramma di sequenza; la categorizzazione dei casi d'uso per responsabilità resta quella della Sezione 4.4.

\begin{sloppypar}
% ── 5.1 ──────────────────────────────────────────────────────────────────────
\subsection{Modulo Documents (AI Co-Pilot)}

Il modulo Documents è la componente del sistema dedicata alla gestione e all'elaborazione intelligente della documentazione HR caricata dai consulenti del lavoro. Il suo obiettivo è ridurre il lavoro manuale di classificazione e smistamento dei documenti: un file caricato viene automaticamente suddiviso, riconosciuto tramite OCR e associato al destinatario corretto, lasciando all'operatore solo la revisione dei casi a bassa confidenza. Il modulo si integra con Amazon Textract per il riconoscimento ottico dei caratteri e con Amazon Bedrock per la classificazione e l'estrazione dei dati, orchestrando l'intera pipeline in modo asincrono tramite AWS Step Functions.

% TODO/valutare: BugBusters, per il modulo AI CoPilot (analogo a Documents), omette il diagramma delle classi
% completo e mostra solo i diagrammi parziali, dichiarando esplicitamente il motivo: "utili a mostrare i
% componenti principali senza appesantire la lettura con l'intero grafo delle dipendenze" (Sez. 5.2, loro doc).
% Qui il diagramma completo resta perché è leggibile in una sola immagine (a differenza del loro caso), ma se in
% futuro dovesse risultare ridondante rispetto ai 5 diagrammi parziali sottostanti, può essere rimosso richiamando
% la stessa giustificazione.
\begin{figure}[H]
    \centering
    \rotatebox{90}{\includegraphics[width=\textheight,height=\textwidth,keepaspectratio]{ImmaginiSpecificaTecnica/Esagonale/diagrammaClassi_Documents.png}}
    \caption{Diagramma delle classi --- Modulo Documents}
    \label{fig:documents-class-diagram}
\end{figure}

\subsubsection{Diagrammi delle classi parziali del modulo Documents}

I diagrammi seguenti mostrano porzioni del diagramma delle classsi complessivo, raggruppate per flusso applicativo, utili a leggere le dipendenze reali senza dover tenere sott'occhio l'intero grafo.

Il primo diagramma mostra \texttt{DocumentController}, l'adapter primario che riceve tutte le richieste HTTP del modulo e le smista verso i cinque casi d'uso corrispondenti.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents-partial-controller.png}
    \caption{Diagramma classi parziale --- DocumentController}
    \label{fig:documents-partial-controller}
\end{figure}

Il secondo diagramma rappresenta il flusso di upload e avvio della pipeline: \texttt{UploadDocumentService} salva il file e crea il documento originale, mentre \texttt{StartDocumentWorkflowService} avvia l'esecuzione Step Functions tramite \texttt{SfnWorkflowEngineAdapter} (componente condiviso del modulo Workflow, non specifico di Documents).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents-partial-upload.png}
    \caption{Diagramma classi parziale --- Flusso di upload e avvio}
    \label{fig:documents-partial-upload}
\end{figure}

Il terzo diagramma evidenzia la catena di elaborazione asincrona orchestrata da \texttt{DocumentWorkflowTaskHandler}: riconoscimento OCR, split e classificazione del documento, estrazione dei campi per ogni destinatario ed aggiornamento finale dello stato.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents-partial-processing.png}
    \caption{Diagramma classi parziale --- Flusso di elaborazione OCR/AI}
    \label{fig:documents-partial-processing}
\end{figure}

Il quarto diagramma mostra il percorso di revisione umana e invio: i tre adapter primari dedicati (\texttt{DocumentReviewController}, \texttt{DocumentPreviewController}, \texttt{SendMessageController}) delegano rispettivamente a \texttt{ReviewDocumentService}, \texttt{PreviewDocumentService} e \texttt{SendMessageService}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents-partial-review.png}
    \caption{Diagramma classi parziale --- Flusso di revisione e invio}
    \label{fig:documents-partial-review}
\end{figure}

Il quinto diagramma completa il quadro con il flusso di consultazione ed eliminazione: \texttt{ListDocumentsService} e \texttt{PollDocumentProgressService} leggono lo stato tramite \texttt{EloquentDocumentRepository}, mentre \texttt{DeleteDocumentService} ne orchestra anche la cancellazione del file fisico e la pubblicazione dell'evento di dominio. È qui che compare per la prima volta l'entità \texttt{SubDocument}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/documents-partial-query.png}
    \caption{Diagramma classi parziale --- Flusso di consultazione ed eliminazione}
    \label{fig:documents-partial-query}
\end{figure}

\subsubsection{Diagrammi di sequenza: caricamento ed elaborazione di un documento}

La sequenza è divisa in due diagrammi lungo il confine reale della pipeline: la parte sincrona, servita direttamente dall'endpoint HTTP, e la parte asincrona, orchestrata da Step Functions ed eseguita dal Worker.

\begin{figure}[H]
    \noindent\makebox[\textwidth][c]{\includegraphics[width=20cm]{ImmaginiSpecificaTecnica/Esagonale/documents-sequence-upload.png}}
    \caption{Diagramma di sequenza --- Upload e avvio della pipeline}
    \label{fig:documents-sequence-upload}
\end{figure}

Il primo diagramma descrive la sequenza delle operazioni che avvengono quando un consulente del lavoro carica un documento tramite l'endpoint dedicato. Il \texttt{DocumentController} riceve la richiesta, delega il salvataggio del file a \texttt{UploadDocumentService} e, subito dopo, l'avvio della pipeline a \texttt{StartDocumentWorkflowUseCase}; la risposta HTTP torna al client con l'URL di streaming, senza attendere l'elaborazione.

\begin{figure}[H]
    \noindent\makebox[\textwidth][c]{\includegraphics[width=20cm]{ImmaginiSpecificaTecnica/Esagonale/documents-sequence-processing.png}}
    \caption{Diagramma di sequenza --- Elaborazione asincrona e streaming SSE}
    \label{fig:documents-sequence-processing}
\end{figure}

Da quel momento la sequenza prosegue in modo asincrono, come mostra il secondo diagramma. Il Worker riceve dalla state machine Step Functions un task alla volta e lo smista al caso d'uso corrispondente, un passo per fase della pipeline: riconoscimento ottico, classificazione, estrazione dei campi per ogni destinatario individuato, infine persistenza dei risultati e pubblicazione dell'evento di completamento.

In termini di classi, quello smistamento passa per \texttt{DocumentWorkflowTaskHandler}, che delega rispettivamente a \texttt{RunOcrService} (\texttt{textract.ocr}), \texttt{ProcessDocumentService} (\texttt{bedrock.extract}, che a sua volta delega l'estrazione dei campi per ogni destinatario a \texttt{ExtractSubDocumentFieldsService}, Sezione 5.1) e \texttt{FinalizeDocumentWorkflowUseCase} (\texttt{persist.results}, \texttt{dispatch.domain\_event}). La transizione di stato \textit{Processing} $\rightarrow$ \textit{Completed} avviene già durante \texttt{bedrock.extract}, quando \texttt{ProcessDocumentService} completa lo split: \texttt{persist.results} si limita a leggerlo tramite \texttt{DocumentRepository}, senza scriverlo; \texttt{dispatch.domain\_event} marca invece separatamente il workflow come concluso (\texttt{workflowCompleted}) e pubblica l'evento finale.

In parallelo, dal momento dell'upload, la SPA resta connessa all'endpoint SSE esposto da \texttt{DocumentController::stream()}, che interroga \texttt{PollDocumentProgressUseCase} a ogni iterazione e inoltra al client i sotto-documenti man mano che vengono estratti, fino all'evento finale di completamento o di errore --- anch'essa illustrata nel secondo diagramma.

\subsubsection{Tutte le classi del modulo Documents}

Dopo aver mostrato i diagrammi delle classi e di sequenza, si riportano di seguito le descrizioni di tutte le classi coinvolte nel modulo Documents, con i dettagli di attributi e metodi per ciascuna.

\paragraph{DocumentController}

Adapter primario HTTP del dominio: riceve le richieste, le traduce in chiamate ai casi d'uso tramite le rispettive porte primarie e restituisce la risposta al client. Non contiene alcuna regola di business: quella vive nel dominio e nell'applicazione.

\texttt{store()} incatena due casi d'uso distinti, caricamento e avvio del workflow. \texttt{stream()} espone invece l'avanzamento della pipeline via Server-Sent Events: il controller resta responsabile solo del protocollo SSE (loop, heartbeat, timeout), leggendo lo stato tramite \texttt{PollDocumentProgressUseCase} invece di interrogare direttamente il database a ogni iterazione.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DocumentController.png}
    \caption{Classe --- DocumentController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{index(request: ListDocumentsRequest, list: ListDocumentsUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{store(request: UploadDocumentRequest, upload: UploadDocumentUseCase, startWorkflow: StartDocumentWorkflowUseCase): JsonResponse}
    \item \texttt{stream(request: Request, originalDocument: OriginalDocument, state: MvpStateService, poll: PollDocumentProgressUseCase): StreamedResponse}
    \item \texttt{destroy(request: Request, subDocument: SubDocument, delete: DeleteDocumentUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{DocumentPreviewController, SendMessageController}

Due Controller separati da \texttt{DocumentController}, sullo stesso principio di traduzione pura verso una porta primaria: \texttt{DocumentPreviewController} espone l'anteprima del file del sotto-documento e, con \texttt{originalPreview()}, anche quella del documento originale completo non splittato, come alternativa indipendente quando la prima non è disponibile; \texttt{SendMessageController} copre l'intero ciclo del messaggio di invio (anteprima PDF composta, esportazione, correzione delle sovrascritture manuali). \texttt{DocumentPreviewController} traduce un fallimento applicativo in una risposta HTTP appropriata per entrambi i suoi metodi, invece di lasciarlo propagare: converte \texttt{DocumentPreviewUnavailableException} in un 404 e un \texttt{RuntimeException} generico (storage non raggiungibile) in un 503. \texttt{SendMessageController} fa l'opposto di proposito: non cattura più \texttt{SendMessageNotConfirmedException}/\texttt{SendMessageAttachmentUnavailableException}, lasciandole risalire al gestore centralizzato in \texttt{bootstrap/app.php} come ogni altro errore di dominio, così la risposta porta sempre \texttt{requestId}/\texttt{correlationId}, che una risposta costruita a mano qui non includerebbe.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DocumentPreviewController.png}
    \caption{Classe --- DocumentPreviewController}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/SendMessageController.png}
    \caption{Classe --- SendMessageController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{preview(request: Request, subDocument: SubDocument, preview: PreviewDocumentUseCase): Response}
    \item \texttt{originalPreview(request: Request, subDocument: SubDocument, preview: PreviewDocumentUseCase): Response}
    \item \texttt{sendPreview(request: Request, subDocument: SubDocument, sendMessage: SendMessageUseCase): Response}
    \item \texttt{sendExport(request: Request, subDocument: SubDocument, sendMessage: SendMessageUseCase): Response}
    \item \texttt{updateSendMessage(request: UpdateSendMessageRequest, subDocument: SubDocument, sendMessage: SendMessageUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{DocumentWorkflowTaskHandler}

Adapter primario guidato da Step Functions/SQS invece che da HTTP: sullo stesso piano concettuale del Controller, traduce ogni task di callback in arrivo dalla pipeline in una chiamata al caso d'uso corrispondente, tramite la sua porta primaria. Non contiene regole di business: l'idempotenza verso la riconsegna di un task (\textit{documento già completato}) vive nei singoli casi d'uso, non in questo adapter.

Risolve e aggiorna l'aggregato documentale attraverso lo stesso \texttt{DocumentRepository} usato dai casi d'uso, mai attraverso Eloquent diretto. Comunica con il runner condiviso passando solo un identificativo e un tenant, così da restare agnostico rispetto al dominio.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DocumentWorkflowTaskHandler.png}
    \caption{Classe --- DocumentWorkflowTaskHandler}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{runOcr: RunOcrUseCase}, \texttt{processDocument: ProcessDocumentUseCase}, \texttt{finalize: FinalizeDocumentWorkflowUseCase}, \texttt{documents: DocumentRepository}, \texttt{clock: ClockInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{taskTypes(): array}
    \item \texttt{subjectType(): string}
    \item \texttt{pipeline(): string} --- restituisce l'identificativo della pipeline di appartenenza (\texttt{"documents"}).
    \item \texttt{auditEventPrefix(): string} --- prefisso (\texttt{"mvp-document-workflow-task-"}) usato dal runner condiviso per nominare gli eventi di audit di questo dominio.
    \item \texttt{resolveSubject(message: array): WorkflowSubject}
    \item \texttt{execute(taskType: string, subject: WorkflowSubject, message: array): array}
    \item \texttt{onFailure(subject: WorkflowSubject, taskType: string, e: Throwable): void}
\end{itemize}

\paragraph{OriginalDocument}

Entità di dominio che rappresenta il documento caricato dall'utente e il suo avanzamento nella pipeline di elaborazione. Non è una semplice proiezione dei dati salvati: governa da sé le proprie transizioni di stato, invece di lasciare a ogni caso d'uso il compito di scriverle correttamente a mano.

\texttt{startProcessing()} e \texttt{fail()} consolidano in un unico posto la logica di transizione allo stato di fallimento, distinguendo sempre il messaggio pensato per l'operatore dal dettaglio tecnico usato per il troubleshooting. Non governa invece i campi restituiti dal riconoscimento ottico (testo, pagine, confidenza): non sono una transizione di stato, solo dati letti dal gateway OCR e scritti direttamente dal caso d'uso che li riceve.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/OriginalDocument.png}
    \caption{Classe --- OriginalDocument}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{id}, \texttt{tenantId}, \texttt{filePath}, \texttt{originalFilename}, i metadati manuali di caricamento, \texttt{processingStatus}, il risultato OCR, i riferimenti all'esecuzione Step Functions.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{fromRecord(record: OriginalDocumentRecord): self}
    \item \texttt{processingStatus(): ProcessingStatus}, \texttt{errorMessage(): ?string}, \texttt{s3Bucket(): ?string}, \texttt{s3Key(): ?string}, \texttt{workflowExecutionArn(): ?string}, \texttt{workflowCompleted(): bool}: accessori di sola lettura sui rispettivi attributi privati.
    \item \texttt{hasManualUploadMetadata(): bool} --- vero se l'utente ha fornito almeno uno dei metadati manuali di caricamento (tipo documento, ragione sociale, mese/anno di riferimento).
    \item \texttt{startProcessing(executionArn: string, s3Bucket: string, s3Key: string, startedAt: DateTimeImmutable): void}
    \item \texttt{fail(errorMessage: string, technicalReason: ?string, failedAt: DateTimeImmutable): void}
    \item \texttt{complete(): void}
    \item \texttt{markWorkflowCompleted(completedAt: DateTimeImmutable): void}
    \item \texttt{pendingChanges(): OriginalDocumentChanges}
\end{itemize}

\paragraph{SubDocument}

Entità di dominio che rappresenta un sotto-documento generato dallo split di un documento originale, e governa la propria revisione: può essere validato automaticamente dal sistema, messo in quarantena con un motivo esplicito quando l'estrazione ha una confidenza troppo bassa, oppure validato manualmente da un operatore.

Governa anche il proprio stato di invio (\texttt{sendStatus()}, \texttt{markSent()}): il download del PDF esportato tramite \texttt{SendMessageService::export()} lo marca \textit{Sent} in una transizione a senso unico (Sezione 5.1, \texttt{SendMessageService}). Non governa invece le eventuali sovrascritture apportate al messaggio destinato al destinatario (destinatario, oggetto, corpo): quei valori vivono a parte in \texttt{SendMessageContext}, letto insieme ai dati estratti al momento della composizione, non come stato proprio dell'entità.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/SubDocument.png}
    \caption{Classe --- SubDocument}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{id}, \texttt{originalDocumentId}, \texttt{filePath}, il range di pagine, \texttt{originalFilename}, \texttt{reviewStatus}, \texttt{sendStatus}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{fromRecord(record: SubDocumentRecord, reviewStatus: ReviewStatus): self}
    \item \texttt{reviewStatus(): ReviewStatus}, \texttt{sendStatus(): SendStatus}: accessori di sola lettura sui rispettivi attributi privati.
    \item \texttt{markAutoValidated(): void}
    \item \texttt{markNeedsReview(): void}
    \item \texttt{markQuarantined(reason: string): void}
    \item \texttt{markManuallyValidated(): void}
    \item \texttt{markSent(): void}
    \item \texttt{pendingChanges(): SubDocumentChanges}
\end{itemize}

\paragraph{UploadDocumentService}

Implementa \texttt{UploadDocumentUseCase} ed è il punto di ingresso della pipeline documentale: riceve il file già validato dall'adapter HTTP, lo salva su storage e crea il record del documento originale con stato \textit{Pending}. Deliberatamente non avvia la pipeline di elaborazione: quella è una decisione distinta, delegata a \texttt{StartDocumentWorkflowUseCase} e invocata in sequenza dallo stesso Controller.

L'audit passa dal dispatch dell'evento di dominio \texttt{DocumentUploadAccepted} (id documento, attore, nome file, metadati manuali), registrato da un listener dedicato: stesso principio in uso per il resto degli eventi di dominio.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/UploadDocumentService.png}
    \caption{Classe --- UploadDocumentService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{storage: DocumentStoragePort}, \texttt{events: DocumentEventDispatcherPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{upload(command: UploadDocumentCommand): int}
\end{itemize}

\paragraph{StartDocumentWorkflowService}

Implementa \texttt{StartDocumentWorkflowUseCase}: avvia l'esecuzione Step Functions della pipeline per un documento già persistito, subito dopo \texttt{UploadDocumentService} nello stesso ciclo di richiesta. È idempotente rispetto a un documento già in elaborazione: il controllo "è già in corso?" e la scrittura che lo rende vero (oltre alla chiamata a Step Functions) girano dentro un'unica transazione, con lettura tramite \texttt{DocumentRepository::findOriginalDocumentForUpdate()} (lock pessimistico sulla riga) — senza, due richieste quasi simultanee potrebbero superare entrambe il controllo e avviare due esecuzioni per lo stesso documento, una delle quali resterebbe orfana. Gli eventi di dominio (avvio riuscito o fallito) sono dispatchati solo dopo che la transazione ha commesso, altrimenti un rollback sul ramo di fallimento annullerebbe anche il salvataggio dello stato "fallito" appena scritto. Esegue anche un controllo esplicito a runtime: quando Textract reale è abilitato, verifica che i documenti risiedano effettivamente su S3 reale e non sul disco LocalStack, fallendo subito con un messaggio azionabile invece di lasciare che Textract fallisca più a valle con un errore criptico. In caso di errore nell'avvio, marca il documento come fallito tramite \texttt{OriginalDocument::fail()} prima di rilanciare l'eccezione.

Non legge configurazione tramite \texttt{config()} al proprio interno: ARN della state machine, URL della coda, flag Textract, disco e bucket di storage sono risolti una sola volta in \texttt{AppServiceProvider} e iniettati come scalari nel costruttore, lo stesso trattamento riservato a soglia di confidenza e prefisso storage altrove nel dominio. Poiché anche \texttt{WorkflowContext} è una classe pura senza dipendenze da Illuminate, \texttt{start()} è interamente istanziabile ed eseguibile in un test Pest puro, senza avviare Laravel.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/StartDocumentWorkflowService.png}
    \caption{Classe --- StartDocumentWorkflowService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{workflowEngine: WorkflowEnginePort}, \texttt{events: DocumentEventDispatcherPort}, \texttt{context: WorkflowContext}, \texttt{clock: ClockInterface}, \texttt{ids: UniqueIdGeneratorPort}, \texttt{transactions: TransactionManagerPort}.
    \item \texttt{stateMachineArn: string}, \texttt{taskQueueUrl: string}, \texttt{textractEnabled: bool}, \texttt{storageDisk: string}, \texttt{documentBucketFallback: string}, \texttt{documentKeyPrefix: string}: scalari risolti da configurazione in \texttt{AppServiceProvider}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{start(documentId: int, correlationId: ?string, requestId: ?string): void}
\end{itemize}

\paragraph{RunOcrService}

Implementa \texttt{RunOcrUseCase}: invoca il gateway OCR per il documento e ne salva il risultato grezzo (testo, pagine, confidenza media, id del job Textract) tramite \texttt{OriginalDocumentChanges}. In caso di errore marca il documento come fallito prima di rilanciare l'eccezione.

È idempotente verso la riconsegna del task workflow (un documento già completato restituisce immediatamente senza richiamare Textract) e deriva la chiave di idempotenza anche dall'oggetto S3, non solo dall'id del documento: dopo un reset seguito da un nuovo upload lo stesso id può puntare a un file diverso, e una chiave fissa farebbe scattare un conflitto contro il job precedente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/RunOcrService.png}
    \caption{Classe --- RunOcrService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{ocr: OcrGatewayPort}, \texttt{events: DocumentEventDispatcherPort}, \texttt{clock: ClockInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{run(documentId: int, bucket: ?string, key: ?string): array}
\end{itemize}

\paragraph{ProcessDocumentService}

Coordina la fase più corposa della pipeline documentale: la classificazione del documento tramite Bedrock e, per ogni destinatario individuato, la delega dell'estrazione dei campi a \texttt{ExtractSubDocumentFieldsService}.

Le sue dipendenze sono fra le più numerose del dominio, perché riflettono quante integrazioni coordina in un solo passo: un repository, uno storage, un gateway AI, un dispatcher di eventi, un heartbeat verso Step Functions (per segnalare che l'elaborazione è ancora viva durante chiamate AI che possono durare minuti), oltre a un generatore di identificativi e un lettore OCR condiviso.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/ProcessDocumentService.png}
    \caption{Classe --- ProcessDocumentService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{storage: DocumentStoragePort}, \texttt{ai: DocumentAiGatewayPort}, \texttt{events: DocumentEventDispatcherPort}, \texttt{heartbeat: WorkflowHeartbeatPort}, \texttt{logger: LoggerInterface}, \texttt{ids: UniqueIdGeneratorPort}, \texttt{ocrRange: OcrRangeReader}, \texttt{fieldsExtractor: ExtractSubDocumentFieldsUseCase}, \texttt{clock: ClockInterface}, \texttt{tempDirectory: string}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{process(documentId: int): array}
\end{itemize}

\paragraph{ExtractSubDocumentFieldsService}

Implementa \texttt{ExtractSubDocumentFieldsUseCase}: estrae nove campi (nome, cognome, azienda, data documento, tipo documento, descrizione, email destinatario, codice fiscale, matricola) di un singolo sotto-documento tramite Bedrock, un destinatario alla volta. Separata da \texttt{ProcessDocumentService} (che orchestra lo split PDF e invoca questa porta per ciascun destinatario appena creato), per isolare questa logica dalla manipolazione PDF: è così testabile indipendentemente da quest'ultima.

I metadati dichiarati manualmente in fase di upload prevalgono sempre su quelli estratti dall'AI, ed escludono il campo corrispondente dal calcolo della confidenza (un'azienda o una data dichiarate dall'utente non hanno bisogno di essere lette dall'AI per essere affidabili). Codice fiscale, email e matricola sono identificativi, non semplici estrazioni: un valore che non supera il proprio controllo formale (email tramite \texttt{filter\_var}, codice fiscale tramite \texttt{CodiceFiscale::isValid()}) torna a \texttt{null} invece di essere salvato — un dato falso che l'operatore non avrebbe modo di riconoscere come tale. La matricola non ha una forma dichiarata e passa così com'è.

Il punteggio di confidenza del sotto-documento è la confidenza più bassa fra i suoi campi chiave (nome, cognome, azienda, data — non la media: un documento è affidabile quanto il suo dato meno affidabile), ciascuna letta dalla riga OCR specifica da cui il campo proviene tramite \texttt{FieldConfidence} (Sezione 5.1, \texttt{TextractOcrAdapter}, ADR 0013), con la confidenza media di pagina come ripiego quando un campo non è rintracciabile fra le righe. Un campo chiave assente azzera il punteggio. Sopra soglia (\texttt{confidenceThreshold}, default 80) il sotto-documento è validato automaticamente — salvo che porti un codice fiscale sotto la propria soglia dedicata (\texttt{fiscalCodeConfidenceThreshold}, default 95, più alta perché un carattere letto male assegnerebbe il documento alla persona sbagliata) — altrimenti è marcato per revisione manuale. Un output AI non conforme allo schema atteso mette il sotto-documento in quarantena invece di propagare l'errore.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/ExtractSubDocumentFieldsService.png}
    \caption{Classe --- ExtractSubDocumentFieldsService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{ai: DocumentAiGatewayPort}, \texttt{events: DocumentEventDispatcherPort}, \texttt{logger: LoggerInterface}, \texttt{ocrRange: OcrRangeReader}, \texttt{tx: TransactionManagerPort}, \texttt{confidenceThreshold: int} (default 80), \texttt{fiscalCodeConfidenceThreshold: int} (default 95).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{extractAndSaveFields(subDocumentId: int): void}
    \item \texttt{extractAndSaveFieldsWithContext(subDocumentId: int, original: OriginalDocument): void}
\end{itemize}

\paragraph{FinalizeDocumentWorkflowService}

Implementa \texttt{FinalizeDocumentWorkflowUseCase}: chiude la pipeline documentale, marcando il workflow come completato sull'entità \texttt{OriginalDocument} quando lo stato di elaborazione è \textit{Completed}, e pubblicando l'evento di completamento che la SPA osserva tramite lo stream SSE.

\texttt{currentStatus()} espone lo stato corrente per i punti della pipeline che devono decidere se procedere; \texttt{dispatchCompletionEvent()} distingue nel proprio risultato un completamento effettivo da un semplice avanzamento, così che il chiamante possa scegliere l'evento SSE corretto da inoltrare al client.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/FinalizeDocumentWorkflowService.png}
    \caption{Classe --- FinalizeDocumentWorkflowService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{events: DocumentEventDispatcherPort}, \texttt{clock: ClockInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{currentStatus(documentId: int): string}
    \item \texttt{dispatchCompletionEvent(documentId: int): array}
\end{itemize}

\paragraph{PollDocumentProgressService}

Implementa \texttt{PollDocumentProgressUseCase}: legge un'istantanea dello stato di avanzamento di un documento (stato di elaborazione, eventuale messaggio d'errore, identificativi dei sotto-documenti per cui l'estrazione è già disponibile), consumata da \texttt{DocumentController::stream()} a ogni iterazione del loop SSE invece di interrogare direttamente il repository dal controller.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/PollDocumentProgressService.png}
    \caption{Classe --- PollDocumentProgressService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{poll(documentId: int): DocumentProgressSnapshot}
\end{itemize}

\paragraph{DocumentReviewController}

Adapter primario HTTP dedicato alla revisione human-in-the-loop: traduce le due operazioni di correzione manuale verso \texttt{ReviewDocumentUseCase}, senza regole di business proprie. \texttt{updateExtractedData()} accetta anche un flag opzionale che marca contestualmente il sotto-documento come validato manualmente, oltre a correggerne i campi.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DocumentReviewController.png}
    \caption{Classe --- DocumentReviewController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{updateExtractedData(request: UpdateExtractedDataRequest, subDocument: SubDocument, review: ReviewDocumentUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{markReviewed(request: Request, subDocument: SubDocument, review: ReviewDocumentUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{ReviewDocumentService}

Implementa \texttt{ReviewDocumentUseCase}: gestisce la revisione manuale di un sotto-documento da parte dell'operatore, con due metodi che coprono due percorsi distinti. Entrambi verificano per primi che il tenant dell'attore coincida con quello del documento originale del sotto-documento (\texttt{DocumentNotAuthorizedException} altrimenti, difesa in profondità dietro il controllo già fatto a livello HTTP — a differenza degli altri casi d'uso di Documents, questa classe non aveva ancora quel controllo).

\texttt{updateExtractedData()} applica correzioni ai campi estratti; scrive comunque i dati estratti se il sotto-documento non ne ha ancora, anche in assenza di modifiche. Lo stato di revisione che ne segue non è un semplice riflesso del parametro \texttt{markAsValidated} ricevuto dal chiamante: se \textit{true} valida manualmente il sotto-documento, ma se \textit{false} lo rimanda in revisione solo quando non era già validato manualmente — una correzione non declassa una scheda che una persona aveva già confermato, perché quei dati sono appena passati un'altra volta sotto i suoi occhi, e declassarli chiederebbe di confermare due volte la stessa scheda. \texttt{markReviewed()} valida invece un sotto-documento senza modificarne i campi, e rifiuta l'operazione se non esistono ancora dati estratti da validare.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/ReviewDocumentService.png}
    \caption{Classe --- ReviewDocumentService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{events: DocumentEventDispatcherPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{updateExtractedData(subDocumentId: int, fieldUpdates: array, markAsValidated: bool, actor: Actor): string}
    \item \texttt{markReviewed(subDocumentId: int, actor: Actor): void}
\end{itemize}

\paragraph{PreviewDocumentService}

Implementa \texttt{PreviewDocumentUseCase}: restituisce il contenuto grezzo del file di un sotto-documento per l'anteprima nella SPA, verificando prima che esista ancora sullo storage. Distinta da \texttt{SendMessageService::preview()}, che genera invece un PDF composto a partire dai dati estratti, non il file originale.

\texttt{previewOriginal()} risale invece dal sotto-documento al suo \texttt{OriginalDocument} e ne restituisce il file completo, non splittato: un'alternativa indipendente dallo stato dell'anteprima del sotto-documento, utile quando quest'ultima non è disponibile ma il documento originale caricato è ancora leggibile dallo storage.

Verifica anche l'autorizzazione: se il tenant dell'attore non coincide con quello del documento (originale o del sotto-documento, a seconda del metodo), solleva \texttt{DocumentNotAuthorizedException} prima di leggere il file.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/PreviewDocumentService.png}
    \caption{Classe --- PreviewDocumentService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{storage: DocumentStoragePort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{preview(subDocumentId: int, actor: Actor): PreviewableDocument}
    \item \texttt{previewOriginal(subDocumentId: int, actor: Actor): PreviewableDocument}
\end{itemize}

\paragraph{SendMessageService}

Implementa \texttt{SendMessageUseCase}: compone il messaggio di invio precompilato a partire dai dati già estratti (nessuna generazione AI coinvolta) e ne orchestra anteprima, esportazione e correzione manuale. Destinatario, corpo e oggetto hanno un valore proposto di default, sovrascrivibile dall'operatore tramite le sovrascritture salvate sul sotto-documento; l'oggetto proposto nomina il tipo di documento e il suo periodo di riferimento (mese/anno, se dichiarati), non l'invio in sé, che a quel punto non è ancora avvenuto. Sia l'anteprima sia l'esportazione accodano al PDF composto anche il documento originale del destinatario (letto dallo storage tramite \texttt{DocumentStoragePort}, \texttt{SendMessageAttachmentUnavailableException} se assente): chi riceve il messaggio riceve anche il documento a cui si riferisce, non solo il testo.

Il download è vincolato a una revisione umana già avvenuta: \texttt{export()} rifiuta con \texttt{SendMessageNotConfirmedException} se il sotto-documento non è ancora \textit{ManuallyValidated} — il pannello tiene spento il comando finché non lo è, ma la API resterebbe altrimenti chiamabile anche senza passare dal pannello. Il recapito avviene fuori dalla piattaforma: poiché il sistema non può osservare l'invio reale, è proprio il download del PDF esportato a marcare lo stato come \textit{Sent}, in una transizione a senso unico: un secondo download non la ripete. Tutti e tre i metodi pubblici verificano l'autorizzazione tramite un metodo privato condiviso (\texttt{assertActorOwnsSubDocument()}), che solleva \texttt{DocumentNotAuthorizedException} in caso di tenant non corrispondente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/SendMessageService.png}
    \caption{Classe --- SendMessageService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{documents: DocumentRepository}, \texttt{renderer: SendMessageRendererPort}, \texttt{storage: DocumentStoragePort}, \texttt{events: DocumentEventDispatcherPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{preview(subDocumentId: int, actor: Actor): RenderedSendMessage}
    \item \texttt{export(subDocumentId: int, actor: Actor): RenderedSendMessage}
    \item \texttt{updateOverrides(subDocumentId: int, overrides: array, actor: Actor): void}
\end{itemize}

\paragraph{ListDocumentsService, DeleteDocumentService}

Due casi d'uso minimi, simmetrici ai rispettivi sul dominio Communications. \texttt{ListDocumentsService} implementa \texttt{ListDocumentsUseCase} e inoltra la paginazione al repository tramite \texttt{DocumentListFilters} (value object tipizzato), senza logica propria.

\texttt{DeleteDocumentService} implementa \texttt{DeleteDocumentUseCase}: verifica che l'attore appartenga allo stesso tenant del documento originale (\texttt{DocumentNotAuthorizedException} altrimenti), elimina un sotto-documento e, se non resta alcun altro sotto-documento collegato allo stesso documento originale, elimina anche quest'ultimo insieme ai suoi task di workflow pendenti. In entrambi i casi il record resta rimosso dal database anche se la pulizia dello storage fallisce: un guasto di rete verso S3 non deve far fallire un'eliminazione già avvenuta dal punto di vista dell'utente, a costo di lasciare temporaneamente un file orfano.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/ListDocumentsService.png}
    \caption{Classe --- ListDocumentsService}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DeleteDocumentService.png}
    \caption{Classe --- DeleteDocumentService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{ListDocumentsService}: \texttt{documents: DocumentRepository}.
    \item \texttt{DeleteDocumentService}: \texttt{documents: DocumentRepository}, \texttt{storage: DocumentStoragePort}, \texttt{events: DocumentEventDispatcherPort}, \texttt{logger: LoggerInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{list(tenantId: string, filters: DocumentListFilters, page: int, perPage: int): SubDocumentPage}
    \item \texttt{delete(subDocumentId: int, actor: Actor): void}
\end{itemize}

\paragraph{EloquentDocumentRepository}

Adapter secondario che implementa \texttt{DocumentRepository} sopra Eloquent: è il punto in cui l'aggregato documentale (documento originale e sotto-documenti) viene effettivamente letto e scritto sul database. Consuma i value object tipizzati del dominio (\texttt{NewOriginalDocument}, \texttt{OriginalDocumentChanges}, \texttt{SubDocumentChanges}...) invece di array associativi grezzi, così che un refuso nel nome di una colonna venga intercettato dall'analisi statica invece che a runtime.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/EloquentDocumentRepository.png}
    \caption{Classe --- EloquentDocumentRepository}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{createOriginalDocument(document: NewOriginalDocument): int}
    \item \texttt{findOriginalDocument(id: int): OriginalDocument}
    \item \texttt{findOriginalDocumentForUpdate(id: int): OriginalDocument}: come \texttt{findOriginalDocument()}, ma con \texttt{lockForUpdate()}, usato dai casi d'uso che racchiudono un controllo "è già in corso?" e la scrittura che lo rende vero in un'unica transazione (\texttt{StartDocumentWorkflowService}).
    \item \texttt{updateOriginalDocument(id: int, changes: OriginalDocumentChanges): void}
    \item \texttt{saveOriginalDocument(document: OriginalDocument): void}
    \item \texttt{deleteOriginalDocumentWithWorkflowTasks(id: int): void}
    \item \texttt{paginateSubDocuments(tenantId: string, filters: DocumentListFilters, page: int, perPage: int): SubDocumentPage}
    \item \texttt{findSubDocument(id: int): SubDocument}
    \item \texttt{saveSubDocument(subDocument: SubDocument): void}
    \item \texttt{createSubDocument(subDocument: NewSubDocument): int}
    \item \texttt{updateSubDocument(id: int, changes: SubDocumentChanges): void}
    \item \texttt{deleteSubDocument(id: int): void}
    \item \texttt{originalDocumentIdForSubDocument(subDocumentId: int): int}
    \item \texttt{originalDocumentHasRemainingSubDocuments(originalDocumentId: int): bool}
    \item \texttt{subDocumentIdsWithExtractedData(originalDocumentId: int): array}
    \item \texttt{deleteExistingSubDocuments(originalDocumentId: int): array}: legge ed elimina in un'unica transazione, restituendo i percorsi storage da ripulire.
    \item \texttt{saveExtractedData(subDocumentId: int, changes: ExtractedDataChanges): void}
    \item \texttt{deleteExtractedData(subDocumentId: int): void}
    \item \texttt{findSendMessageContext(subDocumentId: int): SendMessageContext}
    \item \texttt{subDocumentHasExtractedData(subDocumentId: int): bool}
    \item \texttt{countSubDocuments(originalDocumentId: int): int}
\end{itemize}

\paragraph{Adapter AI e OCR: TextractOcrAdapter, BedrockDocumentAiAdapter}

I due adapter che collegano il dominio ai servizi AI esterni. \texttt{TextractOcrAdapter} implementa \texttt{OcrGatewayPort} sopra Amazon Textract e restituisce il risultato grezzo del riconoscimento ottico, senza toccare la persistenza: è il caso d'uso chiamante a decidere cosa salvarne. Se Textract è disattivato (\texttt{TEXTRACT\_ENABLED=false}, il default), non contatta AWS: restituisce un risultato vuoto (\texttt{enabled: false}), così la pipeline prosegue comunque fino alla classificazione AI senza un OCR reale alle spalle.

\texttt{BedrockDocumentAiAdapter} implementa \texttt{DocumentAiGatewayPort} sopra il servizio condiviso \texttt{BedrockService} (Sezione 2.7), per lo split del documento e l'estrazione dei campi.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/TextractOcrAdapter.png}
    \caption{Classe --- TextractOcrAdapter}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/BedrockDocumentAiAdapter.png}
    \caption{Classe --- BedrockDocumentAiAdapter}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{TextractOcrAdapter}: \texttt{client: TextractClient}, \texttt{heartbeat: WorkflowTaskHeartbeat}.
    \item \texttt{BedrockDocumentAiAdapter}: \texttt{bedrock: BedrockService}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{detectText(bucket: string, key: string, idempotencyKey: string): array}
    \item \texttt{splitDocument(ocrText: string, pageCount: int, boundaryNonce: string): array}
    \item \texttt{extractFields(ocrText: string): array}
    \item \texttt{pageBoundaryMarker(page: int, nonce: string): string} --- delega al marcatore di confine pagina di \texttt{BedrockService}, usato per far riconoscere al modello dove finisce ogni pagina nel testo OCR concatenato.
    \item \texttt{formatUserError(e: Throwable, defaultMessage: string): string} --- delega alla formattazione errori di \texttt{BedrockService}, per un messaggio utente leggibile a partire da un'eccezione tecnica.
\end{itemize}

\paragraph{FlysystemDocumentStorageAdapter, DompdfSendMessageRenderer}

Due adapter secondari di supporto. \texttt{FlysystemDocumentStorageAdapter} implementa \texttt{DocumentStoragePort} sopra il disco Laravel/Flysystem configurato per i documenti, generando per ogni file caricato un nome univoco (UUID) nella directory di destinazione, per evitare collisioni fra upload concorrenti.

\texttt{DompdfSendMessageRenderer} implementa \texttt{SendMessageRendererPort} sopra Dompdf e la vista Blade dedicata, applicando lo stesso timbro di piè di pagina (\texttt{PdfFooterStamper}) condiviso con l'esportazione delle comunicazioni. Accoda al messaggio anche il documento del destinatario, quando fornito: lo unisce tramite Fpdi (la stessa libreria che ha già tagliato il documento originale nei sotto-documenti, quindi il file è già importabile per costruzione), copiando le pagine del documento così come sono, senza sovrastamparci numeri o marcatori — è un documento di terzi. Il piè di pagina numera l'intero file risultante (messaggio più documento accodato), non la sola lettera.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/FlysystemDocumentStorageAdapter.png}
    \caption{Classe --- FlysystemDocumentStorageAdapter}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/DompdfSendMessageRenderer.png}
    \caption{Classe --- DompdfSendMessageRenderer}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{DompdfSendMessageRenderer}: \texttt{footerStamper: PdfFooterStamper}.
    \item \texttt{FlysystemDocumentStorageAdapter}: nessuno (il disco è risolto da configurazione a ogni chiamata).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{storeFromLocalPath(absoluteSourcePath: string, directory: string): string}
    \item \texttt{read(path: string): string}, \texttt{write(path: string, contents: string): void}, \texttt{delete(path: string): void}, \texttt{exists(path: string): bool}
    \item \texttt{renderPdf(composition: SendMessageComposition, attachmentPdf: ?string): string}
\end{itemize}

\paragraph{LaravelDocumentEventDispatcher}

Adapter secondario minimo: implementa \texttt{DocumentEventDispatcherPort} appoggiandosi al dispatcher di eventi nativo di Laravel. I 13 listener del dominio (su 14 eventi dispatchati: \texttt{DocumentWorkflowCompleted} non ha un listener registrato) sono registrati nel service provider, non in questa classe, che si limita a inoltrare l'evento.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingole/LaravelDocumentEventDispatcher.png}
    \caption{Classe --- LaravelDocumentEventDispatcher}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{dispatcher: Dispatcher}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{dispatch(event: object): void}
\end{itemize}

% ── 5.2 ──────────────────────────────────────────────────────────────────────
\subsection{Modulo Communications (AI Assistant Generativo)}

Il modulo Communications è la componente del sistema dedicata alla generazione assistita di comunicazioni interne, dal titolo al testo fino all'immagine di copertina. Il suo obiettivo è offrire ai redattori aziendali uno strumento che produca contenuti coerenti con il tono e lo stile dell'organizzazione a partire da un semplice prompt, riducendo il tempo dedicato alla scrittura senza sottrarre all'utente il controllo editoriale: ogni bozza generata resta modificabile, valutabile e rigenerabile finché non viene scartata, anche dopo l'approvazione. Il modulo si integra con Amazon Bedrock sia per la generazione testuale sia per quella delle immagini di copertina, orchestrando la pipeline in modo asincrono tramite AWS Step Functions, sullo stesso schema del modulo Documents.

\begin{figure}[H]
    \centering
    \rotatebox{90}{\includegraphics[width=\textheight,height=\textwidth,keepaspectratio]{ImmaginiSpecificaTecnica/Esagonale/diagrammaClassi_Communications.png}}
    \caption{Diagramma delle classi --- Modulo Communications}
    \label{fig:communications-class-diagram}
\end{figure}

\subsubsection{Diagrammi delle classi parziali del modulo Communications}

I diagrammi seguenti mostrano porzioni del diagramma delle classi complessivo, raggruppate per flusso applicativo, sullo stesso principio già applicato al modulo Documents.

Il primo diagramma mostra \texttt{CommunicationController}, l'adapter primario che riceve la maggior parte delle richieste HTTP del modulo e le smista verso i cinque casi d'uso corrispondenti.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-controller.png}
    \caption{Diagramma classi parziale --- CommunicationController}
    \label{fig:communications-partial-controller}
\end{figure}

Il secondo diagramma rappresenta il flusso di generazione e avvio della pipeline: \texttt{GenerateCommunicationService} crea la comunicazione, mentre \texttt{StartCommunicationWorkflowService} avvia l'esecuzione Step Functions tramite \texttt{SfnWorkflowEngineAdapter} (componente condiviso del modulo Workflow, non specifico di Communications) ed elimina l'eventuale copertina precedente in caso di rigenerazione.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-generation.png}
    \caption{Diagramma classi parziale --- Flusso di generazione e avvio}
    \label{fig:communications-partial-generation}
\end{figure}

Il terzo diagramma evidenzia la catena di generazione asincrona orchestrata da \texttt{CommunicationWorkflowTaskHandler}: generazione del testo, generazione della copertina ed aggiornamento finale dello stato, entrambe le generazioni tramite Bedrock.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-pipeline.png}
    \caption{Diagramma classi parziale --- Flusso di generazione asincrona}
    \label{fig:communications-partial-pipeline}
\end{figure}

Il quarto diagramma mostra il percorso di copertina, esportazione e valutazione: i tre adapter primari dedicati (\texttt{CommunicationCoverController}, \texttt{CommunicationExportController}, \texttt{CommunicationRatingController}) delegano rispettivamente a \texttt{UpdateCommunicationCoverService}/\texttt{DownloadCommunicationCoverService}, \texttt{ExportCommunicationService} e \texttt{RateCommunicationService}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-cover-export.png}
    \caption{Diagramma classi parziale --- Flusso di copertina, esportazione e valutazione}
    \label{fig:communications-partial-cover-export}
\end{figure}

Il quinto diagramma completa il quadro con il flusso di consultazione, streaming ed eliminazione: \texttt{ListCommunicationsService} e \texttt{PollCommunicationProgressService} leggono lo stato tramite \texttt{EloquentCommunicationRepository}, mentre \texttt{DeleteCommunicationService} ne orchestra anche la cancellazione della copertina e la pubblicazione dell'evento di dominio.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-query.png}
    \caption{Diagramma classi parziale --- Flusso di consultazione ed eliminazione}
    \label{fig:communications-partial-query}
\end{figure}

Il sesto diagramma isola la gestione delle configurazioni di prompt salvate, un aggregato distinto da \texttt{Communication} con un proprio repository dedicato.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{ImmaginiSpecificaTecnica/Esagonale/communications-partial-promptconfig.png}
    \caption{Diagramma classi parziale --- Configurazioni di prompt}
    \label{fig:communications-partial-promptconfig}
\end{figure}

\subsubsection{Diagramma di sequenza: generazione di una comunicazione}

\begin{figure}[H]
    \noindent\makebox[\textwidth][c]{\includegraphics[width=20cm]{ImmaginiSpecificaTecnica/Esagonale/communications-sequence.png}}
    \caption{Diagramma di sequenza --- Generazione di una comunicazione}
    \label{fig:communications-sequence}
\end{figure}

Il diagramma descrive la sequenza delle operazioni che avvengono quando un redattore richiede la generazione di una comunicazione tramite l'endpoint dedicato. Il \texttt{CommunicationController} riceve prompt, tono e stile, delega la creazione della bozza a \texttt{GenerateCommunicationService} e, subito dopo, l'avvio della pipeline a \texttt{StartCommunicationWorkflowUseCase}; la risposta HTTP torna al client con l'identificativo della comunicazione e l'URL di streaming, senza attendere la generazione.

Da quel momento la sequenza prosegue in modo asincrono. Il Worker riceve dalla state machine Step Functions un task alla volta: prima la generazione del testo, poi quella della copertina a partire dal prompt visivo prodotto dal passo precedente (un passo che può fallire senza invalidare la comunicazione, degradando semplicemente lo stato della copertina), infine la pubblicazione dell'evento di completamento.

In termini di classi, quei tre passi passano rispettivamente per \texttt{CommunicationWorkflowTaskHandler}, che delega a \texttt{GenerateCommunicationTextService}, \texttt{GenerateCommunicationCoverService} e \texttt{FinalizeCommunicationUseCase}. Ogni passo aggiorna lo stato dell'entità \texttt{Communication} tramite \texttt{EloquentCommunicationRepository}.

In parallelo, la SPA resta connessa all'endpoint SSE esposto da \texttt{CommunicationStreamController}, che interroga \texttt{PollCommunicationProgressUseCase} a ogni iterazione e inoltra al client l'avanzamento di testo e copertina, fino all'evento finale di completamento o di errore.

\subsubsection{Tutte le classi del modulo Communications}

Dopo aver mostrato i diagrammi delle classi e di sequenza, si riportano di seguito le descrizioni di tutte le classi coinvolte nel modulo Communications, con i dettagli di attributi e metodi per ciascuna.

\paragraph{CommunicationController}

Adapter primario HTTP del dominio: raccoglie le operazioni sul ciclo di vita della bozza (creazione, modifica, rigenerazione, salvataggio, scarto, eliminazione, preferiti) che nel dominio Documents sono invece distribuite su più Controller specializzati, perché qui condividono lo stesso aggregato e la stessa autorizzazione di base.

Le operazioni più specifiche (copertina, esportazione, valutazione, streaming SSE) restano su Controller dedicati (\texttt{CommunicationCoverController}, \texttt{CommunicationExportController}, \texttt{CommunicationRatingController}, \texttt{CommunicationStreamController}), sullo stesso principio di traduzione pura verso una porta primaria, senza regole di business proprie.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationController.png}
    \caption{Classe --- CommunicationController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{index(request: ListCommunicationsRequest, list: ListCommunicationsUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{store(request: GenerateCommunicationRequest, generate: GenerateCommunicationUseCase, startWorkflow: StartCommunicationWorkflowUseCase): JsonResponse}
    \item \texttt{update(request: UpdateCommunicationRequest, communication: Communication, draft: CommunicationDraftUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{favorite(request: Request, communication: Communication, draft: CommunicationDraftUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{unfavorite(request: Request, communication: Communication, draft: CommunicationDraftUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{regenerate(request: Request, communication: Communication, startWorkflow: StartCommunicationWorkflowUseCase): JsonResponse}
    \item \texttt{save(request: Request, communication: Communication, draft: CommunicationDraftUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{discard(request: Request, communication: Communication, draft: CommunicationDraftUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{destroy(request: Request, communication: Communication, delete: DeleteCommunicationUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{CommunicationCoverController}

Adapter primario HTTP dedicato all'immagine di copertina della comunicazione: sostituzione manuale, rimozione e download. L'audit della mutazione passa dagli eventi \texttt{CommunicationCoverReplaced}/\texttt{CommunicationCoverRemoved} dispatchati da \texttt{UpdateCommunicationCoverService}, non scritto qui direttamente. Il download passa dalla porta primaria \texttt{DownloadCommunicationCoverUseCase}, stesso schema di \texttt{DocumentPreviewController} sul dominio Documents.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationCoverController.png}
    \caption{Classe --- CommunicationCoverController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{updateCoverImage(request: UpdateCommunicationCoverRequest, communication: Communication, covers: UpdateCommunicationCoverUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{removeCoverImage(request: Request, communication: Communication, covers: UpdateCommunicationCoverUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{coverImage(request: Request, communication: Communication, covers: DownloadCommunicationCoverUseCase): Response}
\end{itemize}

\paragraph{CommunicationExportController}

Adapter primario HTTP per il documento finale impaginato: anteprima inline ed esportazione come allegato. Entrambi i metodi condividono la stessa logica privata di risposta PDF, che usa l'impronta (\texttt{fingerprint()}) come \texttt{ETag} per evitare di rigenerare il documento se il client possiede già l'ultima versione.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationExportController.png}
    \caption{Classe --- CommunicationExportController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{preview(request: Request, communication: Communication, export: ExportCommunicationUseCase): Response}
    \item \texttt{export(request: Request, communication: Communication, export: ExportCommunicationUseCase): Response}
\end{itemize}

\paragraph{CommunicationRatingController}

Adapter primario HTTP per la valutazione a stelle (1-5) con commento opzionale, una sola per generazione: traduce la richiesta verso \texttt{RateCommunicationUseCase} senza regole di business proprie.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationRatingController.png}
    \caption{Classe --- CommunicationRatingController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{rate(request: RateCommunicationRequest, communication: Communication, rate: RateCommunicationUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{CommunicationStreamController}

Adapter primario HTTP dedicato allo stream SSE dell'avanzamento della generazione: legge lo stato tramite \texttt{PollCommunicationProgressUseCase} (porta primaria), restando responsabile solo del protocollo SSE e dello shaping via \texttt{MvpStateService}, ricaricando dal modello Eloquent solo quando deve emettere gli eventi \textit{cover}/\textit{done} che richiedono l'intero aggregato.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationStreamController.png}
    \caption{Classe --- CommunicationStreamController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{stream(request: Request, communication: Communication, state: MvpStateService, poll: PollCommunicationProgressUseCase): StreamedResponse}
\end{itemize}

\paragraph{CommunicationWorkflowTaskHandler}

Adapter primario guidato da Step Functions/SQS, equivalente concettuale del \texttt{DocumentWorkflowTaskHandler} sul dominio gemello: traduce i task di callback della pipeline di generazione in chiamate ai casi d'uso di dominio tramite le loro porte primarie, senza contenere regole di business proprie.

Per costruire un messaggio di ripiego usa direttamente il messaggio dell'eccezione, stesso comportamento del suo equivalente sul dominio Documents.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationWorkflowTaskHandler.png}
    \caption{Classe --- CommunicationWorkflowTaskHandler}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{generateText: GenerateCommunicationTextUseCase}, \texttt{generateCover: GenerateCommunicationCoverUseCase}, \texttt{finalize: FinalizeCommunicationUseCase}, \texttt{communications: CommunicationRepository}, \texttt{clock: ClockInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{taskTypes(): array}
    \item \texttt{subjectType(): string}
    \item \texttt{pipeline(): string} --- restituisce l'identificativo della pipeline di appartenenza (\texttt{"communications"}).
    \item \texttt{auditEventPrefix(): string} --- prefisso (\texttt{"mvp-communication-workflow-task-"}) usato dal runner condiviso per nominare gli eventi di audit di questo dominio.
    \item \texttt{resolveSubject(message: array): WorkflowSubject}
    \item \texttt{execute(taskType: string, subject: WorkflowSubject, message: array): array}
    \item \texttt{onFailure(subject: WorkflowSubject, taskType: string, e: Throwable): void}
\end{itemize}

\paragraph{Communication}

Entità di dominio che governa l'intero ciclo di vita di una bozza di comunicazione, ed è la più estesa dei due domini perché accumula responsabilità distinte (preferiti, approvazione e scarto della bozza, valutazione a stelle, copertina manuale o generata dall'AI, l'intera pipeline di generazione fino alla rigenerazione) invece delle una o due tipiche di \texttt{SubDocument} e \texttt{OriginalDocument}.

Verifica anche un'invariante propria: una copertina non può mai essere applicata prima che il testo sia stato generato, controllata ogni volta che l'entità riceve l'esito della generazione AI. Come le altre due entità, ogni metodo verifica la propria guardia (una comunicazione già scartata non può essere approvata, una valutazione già assegnata non può essere ripetuta) e accumula il proprio delta in un oggetto di modifiche interno, letto una sola volta dall'adapter di persistenza.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/Communication.png}
    \caption{Classe --- Communication}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{id}, \texttt{tenantId}, i dati della richiesta, l'output della generazione, gli stati di generazione/copertina/bozza, \texttt{isFavorite}, \texttt{rating}, i riferimenti all'esecuzione Step Functions ed eventuali errori.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{fromRecord(record: CommunicationRecord): self}
    \item \texttt{favorite(): void}, \texttt{unfavorite(): void}
    \item \texttt{updateDraft(title: string, body: string): void}, \texttt{approve(): void}, \texttt{discard(): void}
    \item \texttt{rate(rating: int, comment: ?string, actorId: string, ratedAt: DateTimeImmutable): void}
    \item \texttt{replaceCover(path: string, mime: string, size: int): void}, \texttt{removeCover(): void}
    \item \texttt{applyGeneratedText(generated: GeneratedCommunicationText): void}, \texttt{applyGeneratedCover(image: GeneratedCommunicationImage, path: string): void}, \texttt{degradeCover(warning: string): void}
    \item \texttt{startGeneration(executionArn: string, startedAt: DateTimeImmutable): void}, \texttt{failGeneration(errorMessage: string, technicalReason: ?string, failedAt: DateTimeImmutable): void}, \texttt{completeGeneration(completedAt: DateTimeImmutable): void}
    \item \texttt{regenerate(): void}
    \item \texttt{hasGeneratedText(): bool} --- l'invariante testo-prima-di-copertina descritta sopra: usato da \texttt{GenerateCommunicationCoverService} come guardia prima di generare la copertina.
    \item \texttt{isReadyForExport(): bool}, \texttt{isEditable(): bool}
    \item \texttt{pendingChanges(): CommunicationChanges}
\end{itemize}

\paragraph{GenerateCommunicationService}

Implementa \texttt{GenerateCommunicationUseCase} ed è il punto di ingresso della pipeline di generazione: crea la comunicazione con stato \textit{Draft} e generazione \textit{Pending} a partire da prompt, tono e stile, e restituisce l'identificativo appena creato. Come il suo equivalente sul dominio Documents, non avvia da sé la pipeline: quella responsabilità è delegata a \texttt{StartCommunicationWorkflowUseCase}, invocato subito dopo dallo stesso Controller.

L'audit passa dal dispatch dell'evento \texttt{CommunicationGenerationRequested}, non da un \texttt{AuditLogger} diretto, stesso principio già applicato a \texttt{UploadDocumentService} sul dominio Documents.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/GenerateCommunicationService.png}
    \caption{Classe --- GenerateCommunicationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{events: CommunicationEventDispatcherPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{generate(command: GenerateCommunicationCommand): int}
\end{itemize}

\paragraph{StartCommunicationWorkflowService}

Implementa \texttt{StartCommunicationWorkflowUseCase}: avvia l'esecuzione Step Functions della pipeline di generazione, equivalente concettuale di \texttt{StartDocumentWorkflowService} sul dominio Documents, con la stessa idempotenza verso un'esecuzione già in corso: il controllo "è già in corso?" e la scrittura che lo rende vero girano dentro un'unica transazione, con lettura tramite \texttt{CommunicationRepository::findCommunicationForUpdate()} (lock pessimistico sulla riga) — senza, due richieste quasi simultanee potrebbero superare entrambe il controllo e avviare due esecuzioni per la stessa comunicazione. Gli eventi di dominio (avvio riuscito o fallito) sono dispatchati solo dopo che la transazione ha commesso, stesso schema di \texttt{CommunicationDraftService::favorite()}/\texttt{unfavorite()}. Espone anche \texttt{regenerate()}: verifica il tenant dell'attore (\texttt{CommunicationNotAuthorizedException} altrimenti), riporta la comunicazione allo stato di rigenerazione, elimina l'eventuale copertina precedente dallo storage e riavvia la pipeline richiamando internamente \texttt{start()}, così che prima generazione e rigenerazione condividano sempre la stessa logica di avvio. Non ha bisogno di un lock proprio: nessun controllo di stato concorrente da proteggere qui, il lock su \texttt{start()} (chiamato subito dopo) resta la difesa contro un doppio avvio.

Stesso trattamento della configurazione del suo equivalente Documents: ARN della state machine e URL della coda sono risolti una volta in \texttt{AppServiceProvider} e iniettati come scalari, non letti tramite \texttt{config()} internamente: stessa istanziabilità in un test Pest puro, senza avviare Laravel.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/StartCommunicationWorkflowService.png}
    \caption{Classe --- StartCommunicationWorkflowService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{coverStorage: CommunicationCoverStoragePort}, \texttt{workflowEngine: WorkflowEnginePort}, \texttt{events: CommunicationEventDispatcherPort}, \texttt{context: WorkflowContext}, \texttt{clock: ClockInterface}, \texttt{ids: UniqueIdGeneratorPort}, \texttt{transactions: TransactionManagerPort}.
    \item \texttt{stateMachineArn: string}, \texttt{taskQueueUrl: string}: scalari risolti da configurazione in \texttt{AppServiceProvider}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{start(communicationId: int, correlationId: ?string, requestId: ?string): void}
    \item \texttt{regenerate(communicationId: int, actor: Actor, correlationId: ?string, requestId: ?string): void}
\end{itemize}

\paragraph{GenerateCommunicationTextService, GenerateCommunicationCoverService}

Le due classi che orchestrano la generazione vera e propria, invocate dal Worker durante la pipeline asincrona: la prima genera titolo e corpo del testo tramite Bedrock, la seconda l'immagine di copertina a partire dal prompt visivo prodotto dalla prima. Sono rimaste distinte invece di essere unificate in un unico servizio, perché rappresentano due passi della state machine Step Functions con criticità diverse: se il testo non viene generato la bozza è fallita, mentre una copertina non disponibile viene degradata e segnalata, lasciando la comunicazione comunque valida.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/GenerateCommunicationTextService.png}
    \caption{Classe --- GenerateCommunicationTextService}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/GenerateCommunicationCoverService.png}
    \caption{Classe --- GenerateCommunicationCoverService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{GenerateCommunicationTextService}: \texttt{communications: CommunicationRepository}, \texttt{ai: CommunicationAiGatewayPort}, \texttt{events: CommunicationEventDispatcherPort}.
    \item \texttt{GenerateCommunicationCoverService}: gli stessi tre più \texttt{storage: CommunicationCoverStoragePort}, \texttt{logger: LoggerInterface}, \texttt{ids: UniqueIdGeneratorPort}, \texttt{coverPathPrefix: string} (prefisso del percorso di storage, default \texttt{"communications/covers"}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{generate(communicationId: int): array}
\end{itemize}

\paragraph{FinalizeCommunicationService}

Implementa \texttt{FinalizeCommunicationUseCase}, equivalente concettuale di \texttt{FinalizeDocumentWorkflowService}: chiude la pipeline di generazione, marcando la comunicazione come completata e pubblicando l'evento di completamento osservato dallo stream SSE. È idempotente verso la riconsegna del task: una comunicazione già completata non ripubblica l'evento.

Se la copertina è rimasta bloccata in \textit{Pending}/\textit{Processing} (il ramo di degrado della state machine può saltare il task in caso di timeout o worker caduto), questo caso d'uso la degrada esplicitamente a \textit{Failed} prima di chiudere la pipeline, così la SPA non resta in attesa indefinita.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/FinalizeCommunicationService.png}
    \caption{Classe --- FinalizeCommunicationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{events: CommunicationEventDispatcherPort}, \texttt{clock: ClockInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{finalize(communicationId: int): array}
\end{itemize}

\paragraph{PollCommunicationProgressService}

Implementa \texttt{PollCommunicationProgressUseCase}, equivalente concettuale di \texttt{PollDocumentProgressService}: legge un'istantanea dell'avanzamento (stato di generazione, stato copertina, testo generato finora, eventuali errori di copertina o di generazione), consumata da \texttt{CommunicationStreamController} a ogni iterazione dello stream SSE.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/PollCommunicationProgressService.png}
    \caption{Classe --- PollCommunicationProgressService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{poll(communicationId: int): CommunicationProgressSnapshot}
\end{itemize}

\paragraph{CommunicationDraftService}

Implementa \texttt{CommunicationDraftUseCase}: raccoglie le operazioni sul ciclo di vita editoriale della bozza (preferiti, modifica, approvazione, scarto) dietro cui il \texttt{CommunicationController} delega senza contenere logica propria. Ogni metodo segue lo stesso schema: recupera l'entità con lock pessimistico, verifica che il tenant dell'attore coincida con quello della comunicazione (\texttt{CommunicationNotAuthorizedException} altrimenti — difesa in profondità: il controllo HTTP già protegge la rotta, ma il caso d'uso non si affida solo a chi lo chiama), invoca la transizione corrispondente (che verifica da sé le proprie guardie), persiste e pubblica l'evento di dominio associato.

Tutti e cinque i metodi racchiudono lettura-per-aggiornamento e scrittura in un'unica transazione tramite \texttt{TransactionManagerPort}: senza il lock, un \texttt{save()} e un \texttt{discard()} quasi simultanei sulla stessa bozza leggerebbero entrambi lo stato prima che l'altro scriva, supererebbero entrambi il proprio controllo in \texttt{Communication}, e l'ultimo a scrivere vincerebbe — una bozza appena scartata potrebbe tornare "approvata" per puro timing, bypassando l'invariante di dominio.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/CommunicationDraftService.png}
    \caption{Classe --- CommunicationDraftService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{events: CommunicationEventDispatcherPort}, \texttt{transactions: TransactionManagerPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{favorite(communicationId: int, actor: Actor): void}, \texttt{unfavorite(communicationId: int, actor: Actor): void}
    \item \texttt{update(communicationId: int, title: string, body: string, actor: Actor): void}
    \item \texttt{save(communicationId: int, actor: Actor): void}, \texttt{discard(communicationId: int, actor: Actor): void}
\end{itemize}

\paragraph{RateCommunicationService}

Implementa \texttt{RateCommunicationUseCase}: registra la valutazione a stelle di una comunicazione insieme a un commento opzionale, normalizzando un commento composto solo da spazi bianchi a \texttt{null} prima di passarlo all'entità. Lettura-per-aggiornamento, verifica del tenant dell'attore (\texttt{CommunicationNotAuthorizedException} altrimenti, difesa in profondità dietro il controllo già fatto a livello HTTP), mutazione e salvataggio avvengono dentro una transazione (\texttt{TransactionManagerPort}), stessa cautela di \texttt{CommunicationDraftService::favorite()}/\texttt{unfavorite()} contro valutazioni concorrenti sulla stessa comunicazione.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/RateCommunicationService.png}
    \caption{Classe --- RateCommunicationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{events: CommunicationEventDispatcherPort}, \texttt{clock: ClockInterface}, \texttt{transactions: TransactionManagerPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{rate(communicationId: int, rating: int, comment: ?string, actor: Actor): void}
\end{itemize}

\paragraph{UpdateCommunicationCoverService}

Implementa \texttt{UpdateCommunicationCoverUseCase}: gestisce la sostituzione manuale della copertina da parte dell'utente, in alternativa a quella generata dall'AI. Entrambi i metodi verificano per primi che il tenant dell'attore coincida con quello della comunicazione (\texttt{CommunicationNotAuthorizedException} altrimenti, difesa in profondità dietro il controllo già fatto a livello HTTP). \texttt{update()} rifiuta poi l'operazione solo se la bozza è già scartata (\texttt{Communication::isEditable()}): una bozza approvata resta modificabile anche dopo il salvataggio nello storico, come previsto dal perimetro MVP: testo, rigenerazione e copertina restano tutti ammessi finché la bozza non è scartata.

Genera un percorso univoco per ogni nuova copertina caricata e rimuove quella precedente dallo storage solo dopo che la nuova è stata salvata con successo, per non lasciare la comunicazione temporaneamente senza copertina in caso di errore a metà operazione.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/UpdateCommunicationCoverService.png}
    \caption{Classe --- UpdateCommunicationCoverService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{communications: CommunicationRepository}, \texttt{storage: CommunicationCoverStoragePort}.
    \item \texttt{events: CommunicationEventDispatcherPort}, \texttt{ids: UniqueIdGeneratorPort}, \texttt{coverPathPrefix: string} (default \texttt{communications/covers}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{update(communicationId: int, bytes: string, mime: string, size: int, actor: Actor): void}
    \item \texttt{remove(communicationId: int, actor: Actor): void}
\end{itemize}

\paragraph{DownloadCommunicationCoverService, ExportCommunicationService}

Due casi d'uso di lettura per l'output finale di una comunicazione. \texttt{DownloadCommunicationCoverService} implementa \texttt{DownloadCommunicationCoverUseCase} e restituisce i byte grezzi della sola immagine di copertina.

\texttt{ExportCommunicationService} implementa \texttt{ExportCommunicationUseCase} e produce il PDF completo della comunicazione tramite \texttt{CommunicationPdfRendererPort}, rifiutando l'operazione se la bozza non è ancora pronta per l'esportazione (\texttt{isReadyForExport()}); espone anche un'impronta (\texttt{fingerprint()}) usata dall'adapter di rendering per la cache su disco, senza che il caso d'uso sappia che quella cache esiste.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/DownloadCommunicationCoverService.png}
    \caption{Classe --- DownloadCommunicationCoverService}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/ExportCommunicationService.png}
    \caption{Classe --- ExportCommunicationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{DownloadCommunicationCoverService}: \texttt{communications: CommunicationRepository}, \texttt{storage: CommunicationCoverStoragePort}.
    \item \texttt{ExportCommunicationService}: \texttt{communications: CommunicationRepository}, \texttt{renderer: CommunicationPdfRendererPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{download(communicationId: int): DownloadableCoverImage}
    \item \texttt{fingerprint(communicationId: int): string}, \texttt{render(communicationId: int): RenderedCommunicationPdf}
\end{itemize}

\paragraph{ListCommunicationsService, DeleteCommunicationService}

Due casi d'uso minimi, equivalenti concettuali di \texttt{ListDocumentsService} e \texttt{DeleteDocumentService}. \texttt{ListCommunicationsService} implementa \texttt{ListCommunicationsUseCase} e inoltra la paginazione al repository.

\texttt{DeleteCommunicationService} implementa \texttt{DeleteCommunicationUseCase}: verifica che il tenant dell'attore coincida con quello della comunicazione (\texttt{CommunicationNotAuthorizedException} altrimenti, difesa in profondità dietro il controllo già fatto a livello HTTP), poi elimina la comunicazione e, se presente, la sua copertina dallo storage: con lo stesso trattamento delle eliminazioni sul dominio Documents, un guasto nella pulizia dello storage viene solo loggato, non propagato, perché il record è già rimosso dal punto di vista dell'utente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/ListCommunicationsService.png}
    \caption{Classe --- ListCommunicationsService}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/DeleteCommunicationService.png}
    \caption{Classe --- DeleteCommunicationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{ListCommunicationsService}: \texttt{communications: CommunicationRepository}.
    \item \texttt{DeleteCommunicationService}: \texttt{communications: CommunicationRepository}, \texttt{coverStorage: CommunicationCoverStoragePort}, \texttt{events: CommunicationEventDispatcherPort}, \texttt{logger: LoggerInterface}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{list(tenantId: string, filters: CommunicationListFilters, page: int, perPage: int): CommunicationPage}
    \item \texttt{delete(communicationId: int, actor: Actor): void}
\end{itemize}

\paragraph{PromptConfigurationController}

Adapter primario HTTP per i preset di prompt riutilizzabili: traduce creazione ed eliminazione verso \texttt{PromptConfigurationUseCase}, senza regole di business proprie: l'unico controllo qui è di trasporto, la verifica di ownership del tenant prima di autorizzare l'eliminazione.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/PromptConfigurationController.png}
    \caption{Classe --- PromptConfigurationController}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{store(request: SavePromptConfigurationRequest, configurations: PromptConfigurationUseCase, state: MvpStateService): JsonResponse}
    \item \texttt{destroy(request: Request, promptConfiguration: PromptConfiguration, configurations: PromptConfigurationUseCase, state: MvpStateService): JsonResponse}
\end{itemize}

\paragraph{PromptConfigurationService}

Implementa \texttt{PromptConfigurationUseCase}: gestisce le configurazioni di prompt salvate dall'utente (combinazioni di prompt, tono e stile riutilizzabili). Se il nome richiesto è vuoto o già in uso per il tenant, \texttt{save()} non restituisce un errore all'utente ma assegna un'etichetta progressiva (\textit{"Senza nome (1)"}, \textit{"(2)"}, ...) tramite un resolver privato, scegliendo sempre di lasciar salvare la configurazione piuttosto che bloccare l'utente su un conflitto di nome.

Il nome è protetto anche da un vincolo di unicità a livello di database: \texttt{save()} tenta il salvataggio fino a 8 volte, e se l'adapter di persistenza segnala una collisione (\texttt{PromptConfigurationNameTakenException}, tradotta da una violazione del vincolo unico) rigenera un nome e riprova. È una difesa contro la corsa fra due richieste concorrenti che il resolver, da solo, non può escludere: solo dopo 8 tentativi falliti l'eccezione risale al chiamante.

\texttt{delete()} verifica invece l'autorizzazione: se il tenant della configurazione non coincide con quello dell'attore, solleva \texttt{PromptConfigurationNotAuthorizedException}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/PromptConfigurationService.png}
    \caption{Classe --- PromptConfigurationService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{configurations: PromptConfigurationRepository}, \texttt{events: CommunicationEventDispatcherPort}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{save(command: SavePromptConfigurationCommand): int}
    \item \texttt{delete(configurationId: int, actor: Actor): void}
\end{itemize}

\paragraph{EloquentCommunicationRepository}

Adapter secondario che implementa \texttt{CommunicationRepository} sopra Eloquent, equivalente concettuale dell'\texttt{EloquentDocumentRepository}: consuma i value object tipizzati del dominio (\texttt{NewCommunication}, \texttt{CommunicationChanges}) invece di array grezzi. È l'unico punto di scrittura dell'aggregato \texttt{Communication}, condiviso da tutti gli otto casi d'uso che lo modificano.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/EloquentCommunicationRepository.png}
    \caption{Classe --- EloquentCommunicationRepository}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{createCommunication(communication: NewCommunication): int}
    \item \texttt{findCommunication(id: int): Communication}
    \item \texttt{findCommunicationForUpdate(id: int): Communication}: con \texttt{lockForUpdate()}, usato dai casi d'uso che racchiudono lettura e scrittura in una transazione.
    \item \texttt{updateCommunication(id: int, changes: CommunicationChanges): void}
    \item \texttt{saveCommunication(communication: Communication): void}
    \item \texttt{deleteCommunication(id: int): void}
    \item \texttt{paginateApprovedCommunications(tenantId: string, filters: CommunicationListFilters, page: int, perPage: int): CommunicationPage}
\end{itemize}

\paragraph{EloquentPromptConfigurationRepository}

Adapter secondario che implementa \texttt{PromptConfigurationRepository} sopra Eloquent, per la persistenza delle configurazioni di prompt salvate dall'utente: un aggregato distinto da \texttt{Communication}, con un proprio repository invece di essere annegato in quello principale. \texttt{create()} riceve il value object tipizzato \texttt{NewPromptConfiguration} invece di un array grezzo, e traduce una violazione del vincolo unico a livello database in \texttt{PromptConfigurationNameTakenException}: è questa traduzione che \texttt{PromptConfigurationService::save()} intercetta nel proprio ciclo di retry.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/EloquentPromptConfigurationRepository.png}
    \caption{Classe --- EloquentPromptConfigurationRepository}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{nameExists(tenantId: string, name: string): bool}
    \item \texttt{create(configuration: NewPromptConfiguration): int}
    \item \texttt{tenantIdOf(id: int): string}
    \item \texttt{delete(id: int): void}
\end{itemize}

\paragraph{BedrockCommunicationAiAdapter}

Adapter secondario che implementa \texttt{CommunicationAiGatewayPort} sopra il servizio condiviso \texttt{BedrockService} (Sezione 2.7), equivalente concettuale del \texttt{BedrockDocumentAiAdapter} sul dominio Documents: la generazione testuale e quella dell'immagine di copertina restano due chiamate distinte, coerentemente con il fatto che sono due passi separati della pipeline.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/BedrockCommunicationAiAdapter.png}
    \caption{Classe --- BedrockCommunicationAiAdapter}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{bedrock: BedrockService}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{generateText(prompt: string, tone: string, style: string): GeneratedCommunicationText}
    \item \texttt{generateImage(prompt: string, tone: string, style: string, modelImagePrompt: ?string): GeneratedCommunicationImage}
\end{itemize}

\paragraph{FlysystemCommunicationCoverAdapter, DompdfCommunicationPdfRenderer}

Due adapter secondari di supporto, equivalenti concettuali di \texttt{FlysystemDocumentStorageAdapter} e \texttt{DompdfSendMessageRenderer} sul dominio Documents. \texttt{FlysystemCommunicationCoverAdapter} implementa \texttt{CommunicationCoverStoragePort} sopra Flysystem, registrando ogni fallimento di lettura/scrittura nel log applicativo.

\texttt{DompdfCommunicationPdfRenderer} implementa \texttt{CommunicationPdfRendererPort} sopra Dompdf: prima di renderizzare calcola un'impronta SHA-1 del contenuto (titolo, corpo, stato e percorso della copertina) e, se un PDF con la stessa impronta è già stato materializzato su disco, lo restituisce direttamente invece di rigenerarlo; applica inoltre una filigrana diagonale (\textit{"Creato da AI Assistant"}) e lo stesso timbro di piè di pagina condiviso con \texttt{DompdfSendMessageRenderer}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/FlysystemCommunicationCoverAdapter.png}
    \caption{Classe --- FlysystemCommunicationCoverAdapter}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/DompdfCommunicationPdfRenderer.png}
    \caption{Classe --- DompdfCommunicationPdfRenderer}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{FlysystemCommunicationCoverAdapter}: nessuno, nessun costruttore proprio (usa il disco storage tramite la facade \texttt{Storage}).
    \item \texttt{DompdfCommunicationPdfRenderer}: \texttt{footerStamper: PdfFooterStamper}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{store(path: string, bytes: string): void}, \texttt{read(path: string): string}, \texttt{delete(path: string): void}, \texttt{exists(path: string): bool}
    \item \texttt{fingerprint(context: CommunicationPdfContext): string}
    \item \texttt{render(context: CommunicationPdfContext): string}, \texttt{filename(context: CommunicationPdfContext): string}
\end{itemize}

\paragraph{LaravelCommunicationEventDispatcher}

Adapter secondario minimo, equivalente concettuale del \texttt{LaravelDocumentEventDispatcher}: implementa \texttt{CommunicationEventDispatcherPort} appoggiandosi al dispatcher di eventi nativo di Laravel. I 20 listener del dominio (su 21 eventi dispatchati: \texttt{CommunicationWorkflowCompleted} non ha un listener registrato, simmetricamente a \texttt{DocumentWorkflowCompleted}) sono registrati nel service provider.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCommunications/LaravelCommunicationEventDispatcher.png}
    \caption{Classe --- LaravelCommunicationEventDispatcher}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{dispatcher: Dispatcher}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{dispatch(event: object): void}
\end{itemize}

% ── 5.3 ──────────────────────────────────────────────────────────────────────
\subsection{Classi condivise}

Infrastruttura trasversale ai due domini, non riorganizzata in porte e adapter propri (si veda la Sezione 4.7 per la motivazione del perimetro).

\subsubsection{Tutte le classi condivise}

Si riportano di seguito le descrizioni di tutte le classi condivise fra i due domini, con i dettagli di attributi e metodi per ciascuna.

\paragraph{WorkflowTaskRegistry}

Instrada un tipo di task (\texttt{communication.generate\_text}, \texttt{document.split}, \ldots) verso l'handler del dominio competente: ogni \texttt{WorkflowTaskHandler} di dominio (Sezioni 5.1 e 5.2) viene registrato qui nel binding del singleton in \texttt{AppServiceProvider::register()}, così che \texttt{WorkflowTaskRunner} non debba conoscere staticamente quali domini esistono.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/WorkflowTaskRegistry.png}
    \caption{Classe --- WorkflowTaskRegistry}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{handlers: array} --- handler registrati, indicizzati per tipo di task gestito.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{register(handler: WorkflowTaskHandler): void} --- registra un handler per i tipi di task che dichiara di gestire.
    \item \texttt{for(taskType: string): WorkflowTaskHandler} --- restituisce l'handler registrato per un tipo di task, lanciando un'eccezione se nessuno è registrato.
    \item \texttt{taskTypes(): array} --- restituisce l'elenco di tutti i tipi di task registrati.
\end{itemize}

\paragraph{WorkflowTaskRunner}

Punto di ingresso generico invocato da \texttt{ConsumeWorkflowTasks} per ogni messaggio SQS: deduplica il task tramite il suo token hash, lo prende in carico atomicamente a database, delega l'esecuzione all'handler risolto tramite \texttt{WorkflowTaskRegistry} e registra l'audit in modo indipendente dal dominio del task.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/WorkflowTaskRunner.png}
    \caption{Classe --- WorkflowTaskRunner}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{registry: WorkflowTaskRegistry}.
    \item \texttt{audit: AuditLogger}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{handle(message: array): array} --- esegue un singolo task a partire dal messaggio SQS decodificato, restituendo l'esito, incluso se e come va inviato il callback verso Step Functions.
\end{itemize}

\paragraph{ConsumeWorkflowTasks}

Il comando che il Worker esegue in loop, consumando i messaggi SQS e invocando \texttt{WorkflowTaskRunner} per ciascuno. Nel callback di esito a Step Functions distingue gli errori permanenti (\texttt{TaskTimedOut}, \texttt{TaskDoesNotExist}, \texttt{InvalidToken}: il token non è più valido, nessun retry potrebbe riuscire) da quelli transitori (throttling, servizio non disponibile): sui primi rimuove comunque il messaggio dalla coda, sui secondi lo lascia per un nuovo tentativo, evitando di popolare la DLQ con falsi fallimenti per un task già concluso a database.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/ConsumeWorkflowTasks.png}
    \caption{Classe --- ConsumeWorkflowTasks}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{PERMANENT\_CALLBACK\_ERRORS: array} (costante) --- codici di errore Step Functions per cui un retry con lo stesso token non può mai riuscire.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{handle(sqs, stepFunctions, runner, heartbeat, context): int} --- ricevuti per dependency injection dal metodo (non dal costruttore, come da convenzione dei comandi Artisan): riceve i messaggi da SQS, delega l'esecuzione a \texttt{WorkflowTaskRunner}, invia il callback di esito a Step Functions e cancella il messaggio dalla coda solo a callback confermato.
\end{itemize}

\paragraph{WorkflowTaskHeartbeat}

Unico adapter condiviso della porta \texttt{WorkflowHeartbeatPort}, invocata dai casi d'uso di lunga durata (split PDF, OCR) per segnalare a Step Functions che l'elaborazione è ancora viva, senza che quei casi d'uso conoscano \texttt{SfnClient}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/WorkflowTaskHeartbeat.png}
    \caption{Classe --- WorkflowTaskHeartbeat}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{stepFunctions: SfnClient}.
    \item \texttt{taskToken: string} --- token del task attivo, impostato da \texttt{activate()}.
    \item \texttt{taskType: string}.
    \item \texttt{lastBeatAt: float} --- timestamp Unix (\texttt{microtime(true)}) dell'ultimo segnale inviato.
    \item \texttt{degraded: bool}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{activate(taskToken: string, taskType: string): void} --- attiva l'heartbeat per il task corrente.
    \item \texttt{deactivate(): void} --- disattiva l'heartbeat al termine del task.
    \item \texttt{beat(force: bool): void} --- invia un segnale di attività a Step Functions se necessario (o sempre, se \texttt{force} è vero).
\end{itemize}

\paragraph{SfnWorkflowEngineAdapter}

Unico adapter che implementa la porta condivisa verso Step Functions per entrambi i domini.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/SfnWorkflowEngineAdapter.png}
    \caption{Classe --- SfnWorkflowEngineAdapter}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{client: SfnClient}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{startExecution(stateMachineArn: string, executionName: string, input: array): string} --- avvia un'esecuzione Step Functions, restituendo l'ARN dell'esecuzione.
\end{itemize}

\paragraph{WorkflowContext}

Contenitore puro degli identificativi di correlazione (request id, correlation id, tenant) propagati lungo l'esecuzione di un caso d'uso, senza alcuna dipendenza da Illuminate: il tagging dei log (\texttt{Log::withContext()}) vive nel middleware HTTP \texttt{CorrelateRequests} e, per il worker SQS, in \texttt{ConsumeWorkflowTasks}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/WorkflowContext.png}
    \caption{Classe --- WorkflowContext}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{requestId: ?string}.
    \item \texttt{correlationId: ?string}.
    \item \texttt{tenantId: ?string}.
\end{itemize}

Tutti e tre nullable: inizializzati a \texttt{null} e azzerati da \texttt{clear()}, valorizzati solo fra un \texttt{bind()} e la fine dell'esecuzione corrente.

Metodi
\begin{itemize}\raggedright
    \item \texttt{bind(?requestId, ?correlationId, ?tenantId): void} --- imposta gli identificativi di correlazione per l'esecuzione corrente; parametri nullable, una stringa vuota è normalizzata a \texttt{null}.
    \item \texttt{clear(): void} --- azzera il contesto a fine esecuzione.
    \item \texttt{requestId(): ?string}, \texttt{correlationId(): ?string}, \texttt{tenantId(): ?string} --- accessori di sola lettura.
\end{itemize}

\paragraph{Actor}

Chi ha compiuto un'azione (id, tenant, ruoli), tradotto una sola volta al bordo HTTP da \texttt{MvpUser}; porte, eventi e comandi di dominio vedono solo questa classe, mai il contratto \texttt{Authenticatable} di Laravel.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/Actor.png}
    \caption{Classe --- Actor}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{id: string}, \texttt{email: string}, \texttt{name: string}, \texttt{tenantId: string}, \texttt{roles: array} --- tutti \texttt{readonly}, impostati una sola volta alla costruzione.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{hasAnyRole(requiredRoles: array): bool} --- vero se l'attore possiede almeno uno dei ruoli richiesti.
\end{itemize}

\paragraph{MvpUserProvider}

\texttt{UserProvider} esplicito per il guard \texttt{mvp}, registrato in \texttt{AppServiceProvider::boot()}. Poiché \texttt{MvpUser} non ha un archivio persistente (viene ricostruito a ogni richiesta da \texttt{ResolveMvpIdentity}, mai risolto da un provider), ogni metodo di questa classe fallisce deliberatamente con un messaggio esplicito invece di lasciare che una risoluzione inattesa fallisca con un errore criptico.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/MvpUserProvider.png}
    \caption{Classe --- MvpUserProvider}
\end{figure}

Metodi
\begin{itemize}\raggedright
    \item \texttt{retrieveById(identifier)}, \texttt{retrieveByToken(identifier, token)}, \texttt{updateRememberToken(user, token)}, \texttt{retrieveByCredentials(credentials)}, \texttt{validateCredentials(user, credentials)}, \texttt{rehashPasswordIfRequired(user, credentials, force)} --- i sei metodi del contratto \texttt{UserProvider}, tutti implementati per lanciare una \texttt{LogicException} esplicativa.
\end{itemize}

\paragraph{SystemClock}

Unico adapter condiviso dell'interfaccia standard PSR-20 \texttt{ClockInterface}, usata al posto di un helper temporale custom.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/SystemClock.png}
    \caption{Classe --- SystemClock}
\end{figure}

Metodi
\begin{itemize}\raggedright
    \item \texttt{now(): DateTimeImmutable} --- restituisce l'istante corrente.
\end{itemize}

\paragraph{RandomUuidGenerator}

Unico adapter condiviso della porta \texttt{UniqueIdGeneratorPort}, per la generazione di identificativi univoci.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/RandomUuidGenerator.png}
    \caption{Classe --- RandomUuidGenerator}
\end{figure}

Metodi
\begin{itemize}\raggedright
    \item \texttt{generate(): string} --- genera un nuovo UUID.
\end{itemize}

\paragraph{LaravelTransactionManager}

Unico adapter condiviso della porta \texttt{TransactionManagerPort}, per le sequenze di scritture DB pure che richiedono un confine transazionale.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/LaravelTransactionManager.png}
    \caption{Classe --- LaravelTransactionManager}
\end{figure}

Metodi
\begin{itemize}\raggedright
    \item \texttt{run(operation: callable): mixed} --- esegue l'operazione all'interno di una transazione DB.
\end{itemize}

\paragraph{AuditLogger}

Registra gli eventi di audit dispatchati dai listener di dominio.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleCondivise/AuditLogger.png}
    \caption{Classe --- AuditLogger}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{context: WorkflowContext}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{record(eventType, actor, resourceType, resourceId, metadata, request, tenantId): AuditEvent} --- registra un evento di audit; fuori da una richiesta HTTP (worker SQS) gli id di correlazione arrivano da \texttt{WorkflowContext} anziché dalla request.
\end{itemize}
\end{sloppypar}

% ─────────────────────────────────────────────────────────────────────────────
\section{Architettura Frontend}

\begin{sloppypar}

% ── 6.1 ──────────────────────────────────────────────────────────────────────
\subsection{Principi di progettazione}

La SPA Angular è organizzata per feature (\texttt{features/assistant}, \texttt{features/copilot}, \texttt{features/overview}), affiancate da tre cartelle trasversali: \texttt{core} (stato applicativo, interceptor HTTP, osservabilità), \texttt{shared} (componenti UI riutilizzabili) e \texttt{layout} (guscio dell'applicazione, intestazioni, navigazione). Ogni feature raggruppa a sua volta una pagina (View), il proprio ViewModel, i propri componenti (\texttt{components/}) e i propri servizi/modelli (\texttt{data/}), rispecchiando la stessa separazione per dominio applicativo già adottata nel backend (Sezione 4.3).

Tutti i componenti sono standalone (nessun \texttt{NgModule}) e usano \texttt{ChangeDetectionStrategy.OnPush} con le API a segnali di Angular 21 (\texttt{signal}, \texttt{computed}, \texttt{input()}) al posto delle proprietà decorate con \texttt{@Input()}: un componente si ridisegna solo quando un segnale che consuma cambia valore, non a ogni ciclo di change detection globale.

Il routing e i template usano la sintassi di controllo nativa (\texttt{@if}, \texttt{@for}) introdotta nelle versioni recenti del framework, senza le direttive strutturali \texttt{*ngIf}/\texttt{*ngFor}. Le tre pagine principali (\texttt{AssistantPage}, \texttt{CopilotPage}, \texttt{OverviewPage}) separano inoltre la logica di presentazione dalla vista tramite un ViewModel dedicato (Sezione 6.2), un principio applicato solo lì: i componenti di feature e quelli condivisi restano invece componenti Angular ordinari, senza un ViewModel proprio.

% ── 6.2 ──────────────────────────────────────────────────────────────────────
\subsection{Il pattern MVVM}

La figura seguente illustra il pattern e la comunicazione fra i suoi tre componenti; un esempio concreto tratto dal progetto segue in Sezione 6.2.1.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{ImmaginiSpecificaTecnica/Esagonale/frontend-mvvm-concept.png}
    \caption{Il pattern MVVM}
    \label{fig:frontend-mvvm-concept}
\end{figure}

Le tre pagine principali applicano MVVM in senso classico: un \emph{ViewModel} (più precisamente un \emph{Presentation Model} nel senso di Fowler) è una classe TypeScript che non tocca il DOM né l'infrastruttura di rendering o di dependency injection di Angular (nessun \texttt{@Component}, \texttt{inject()}, decoratore, injection context, chiamata diretta a \texttt{document}/\texttt{window}); riceve le proprie dipendenze dal costruttore, ed è quindi istanziabile con un semplice \texttt{new} e testabile senza \texttt{TestBed}, con dipendenze fittizie create a mano. L'unica eccezione apparente è l'uso di \texttt{signal}/\texttt{computed} di \texttt{@angular/core}: restano nel ViewModel non come parte del motore di rendering, ma come primitiva reattiva di piattaforma (l'equivalente di \texttt{INotifyPropertyChanged} in WPF), quindi non compromettono l'istanziabilità con \texttt{new} né la testabilità senza Angular avviato; è questa distinzione, non un conteggio letterale degli import da \texttt{@angular/core}, a rendere la classe un Presentation Model genuino.

Il \emph{Model} resta quanto descritto in Sezione 6.3: i tipi generati dal contratto OpenAPI e i modelli di dominio specifici di ciascuna feature. La \emph{View} è la pagina stessa (\texttt{AssistantPage}, \texttt{CopilotPage}, \texttt{OverviewPage}): l'unico punto accoppiato ad Angular e al DOM, che si procura le dipendenze con \texttt{inject()} nel proprio costruttore e le passa al ViewModel per costruirlo, poi lega il template ai segnali e ai metodi pubblici che il ViewModel espone (\texttt{vm.stato()}, \texttt{(evento)="vm.metodo()"}). Anche l'unica operazione DOM richiesta da alcuni ViewModel (lo scorrimento verso una sezione dopo un'azione) resta un'operazione della View, ma non tramite una funzione-callback ricevuta dal costruttore: \texttt{AssistantPageViewModel} e \texttt{OverviewPageViewModel} scrivono un segnale osservabile, \texttt{pendingScrollTarget: WritableSignal<string | null>}, e un \texttt{effect()} nella View lo legge, chiama \texttt{scrollToElement} (definita in \texttt{shared/util/scroll.ts}) quando non è nullo, poi lo azzera. Un segnale osservato è un fatto dichiarativo puro, che la View può scegliere di leggere o ignorare senza che il ViewModel sappia se lo ha fatto. Se non azzerasse il segnale dopo averlo consumato, la stessa destinazione richiesta due volte di seguito non ritriggererebbe l'\texttt{effect()}, che reagisce solo a un cambio di valore.

Lo stato condiviso fra le pagine non vive nel ViewModel: una sola istanza di \texttt{MvpStateStore}, fornita a livello di applicazione (\texttt{providedIn: "root"}), conserva lo stato restituito dal backend e sopravvive ai cambi di rotta. Ogni ViewModel riceve lo Store come una qualunque altra dipendenza nel proprio costruttore e lo consuma in sola lettura per derivare i propri segnali \texttt{computed}; le mutazioni (generazione, upload, revisione, eliminazione) non aggiornano lo stato localmente, ma rimpiazzano l'intero stato con quello autorevole restituito dal backend nella risposta, evitando divergenze fra ciò che la SPA mostra e ciò che il backend ha effettivamente persistito.

Un'eccezione è documentata esplicitamente nel codice: \texttt{effect()} e \texttt{takeUntilDestroyed()} restano nella View perché richiedono un injection context che il ViewModel non ha per costruzione. Gli \texttt{effect()} però si limitano a leggere i segnali sorgente (lo storico o i documenti condivisi, i filtri attivi, il documento selezionato) e a chiamare un metodo del ViewModel (\texttt{vm.reload()}, \texttt{vm.loadPreviewStatus()}...): la chiamata vera e propria (stessa forma di ogni altra azione del ViewModel, come \texttt{generate()} o \texttt{discard()}) vive nel ViewModel, non nella View. I metodi che inoltrano il risultato (\texttt{setFilteredCommunications()}, \texttt{handleHistoryError()}) sono privati: dettagli interni di \texttt{reload()}, non superficie pubblica del ViewModel. Per lo stesso motivo il template delle tre pagine legge sempre e solo \texttt{vm.*}, mai \texttt{store.*} direttamente: anche il banner d'errore principale chiama un metodo del ViewModel — \texttt{vm.reloadState()} su \texttt{AssistantPage} e \texttt{CopilotPage}, \texttt{vm.reload()} su \texttt{OverviewPage} (l'unica delle tre il cui ViewModel non distingue fra ricarica dello stato condiviso e ricarica di uno storico proprio, non avendone uno). Nessuna delle tre pagine permette a una View di raggiungere lo Store direttamente dal markup: il campo \texttt{store}, \texttt{inject()}ato su tutte e tre, serve solo a costruire il ViewModel nel costruttore e, su \texttt{AssistantPage}/\texttt{CopilotPage}, come segnale sorgente letto dall'\texttt{effect()} di ricerca — mai referenziato nel template.

I segnali \texttt{computed} pass-through sul ViewModel seguono lo stesso schema su tutte e tre le pagine: tutte e tre espongono \texttt{vm.error()} e \texttt{vm.loading()}; \texttt{AssistantPage} e \texttt{CopilotPage} espongono inoltre \texttt{vm.metrics()} (l'elenco di metriche del proprio modulo, filtrato delle voci che il pannello non mostra come scheda a sé) e \texttt{vm.metricsPresentation()} (la mappa che indica a \texttt{MetricsPanelComponent}, Sezione 6.6, come disegnare ciascuna scheda). \texttt{OverviewPage} non monta un pannello di metriche generico: il suo ViewModel espone segnali propri più mirati (\texttt{vm.priorities()}, \texttt{vm.assistantQuality()}, \texttt{vm.copilotQuality()}, \texttt{vm.quarantined()}), descritti nella sua sottosezione più sotto.

Angular offre già disaccoppiamento e testabilità tramite i propri servizi e la dependency injection, quindi un ViewModel separato potrebbe sembrare ridondante rispetto a testare direttamente il componente con \texttt{TestBed}. Il motivo per cui si è scelto comunque questo confine è pratico, non dogmatico: un test su \texttt{TestBed} richiede l'avvio del modulo, la compilazione del template e la risoluzione dell'albero di dependency injection a ogni esecuzione, mentre un test sul ViewModel istanzia la classe con \texttt{new} passando dipendenze fittizie create a mano: più veloce da eseguire e più semplice da scrivere, perché isola la logica applicativa dal ciclo di vita di Angular. Per questo il confine è tracciato solo sulle tre pagine principali, dove quella logica è più densa, e non esteso a ogni componente della SPA.

L'assenza di \texttt{inject()} e dell'infrastruttura di rendering nel ViewModel non è un'omissione stilistica: è la condizione che rende possibile l'obiettivo dichiarato sopra. \texttt{inject()} funziona solo dentro un injection context, quindi una classe che lo usasse sarebbe istanziabile solo da Angular: per testarla servirebbe comunque \texttt{TestBed.runInInjectionContext()} o un \texttt{TestBed.inject()}, lo stesso costo che l'estrazione del ViewModel vuole evitare. Lo stesso vale per il rendering: un ViewModel che manipolasse il DOM richiederebbe un ambiente browser (o \texttt{jsdom}) anche solo per verificarne la logica. Estendere questo pattern a ogni componente della SPA, non solo alle tre pagine principali, non è però la conclusione corretta: il beneficio (test framework-indipendenti su logica applicativa densa) va confrontato con il costo (un file e un'indirection in più). I componenti condivisi (Sezione 6.6) non hanno quasi logica propria (ricevono un \texttt{input()} e lo mostrano), e un ViewModel dedicato aggiungerebbe solo un file vuoto da mantenere, senza nulla di significativo da isolare che il codice non mostri già direttamente.

\subsubsection{Esempio di pattern MVVM nel progetto}

Il diagramma seguente illustra il pattern con un esempio concreto tratto dalla pagina di generazione (\texttt{AssistantPage}): per chiarezza è semplificato a una sola delle componenti riutilizzabili della pagina.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{ImmaginiSpecificaTecnica/Esagonale/frontend-mvvm-example.png}
    \caption{Esempio di pattern MVVM --- AssistantPage}
    \label{fig:frontend-mvvm-example}
\end{figure}

La classe \texttt{AssistantPage} (View) costruisce \texttt{AssistantPageViewModel} (ViewModel) con \texttt{new()} nel proprio costruttore e nel template legge solo i suoi segnali e metodi pubblici (\texttt{vm.*}). \texttt{AssistantPageViewModel}, a sua volta, non tocca mai Angular né il DOM: risponde alle azioni richieste dalla View chiamando le operazioni esposte da \texttt{AssistantService} (Model), il servizio che comunica con l'API generata e con lo stream SSE.

Per completezza si mostra anche la relazione con il componente riutilizzabile \texttt{CommunicationGeneratorPanelComponent}: \texttt{AssistantPage} lo importa nel proprio template e gli passa lo stato tramite property binding (\texttt{[isGenerating]}, \texttt{[status]}, \texttt{[phase]}); il componente notifica le azioni dell'utente alla View tramite event binding (\texttt{(generate)}, \texttt{(saveConfiguration)}), che le inoltra al ViewModel (\texttt{vm.generate(\$event)}) — lo stesso schema vale per qualunque altro componente riutilizzabile della SPA.

\subsubsection{Two-way data binding}

Il progetto non usa il binding bidirezionale nativo di Angular (\texttt{[(ngModel)]}, equivalente meccanicamente alla stessa coppia \texttt{[input]}/\texttt{(outputChange)} già usata altrove, o l'API \texttt{model()}): il motivo è strutturale, non stilistico. \texttt{model()} richiede che sia il componente stesso a possedere il segnale bidirezionale, ma il ViewModel resta volutamente una classe pura fuori dall'injector di Angular (Sezione 6.2), quindi non potrebbe comunque parteciparvi. La relazione fra View e ViewModel non è inoltre il caso d'uso naturale del two-way binding, pensato per rispecchiare un valore grezzo: il componente riceve stato derivato e \texttt{computed} dal ViewModel, ed emette azioni (\texttt{generate}, \texttt{saveConfiguration}) che devono passare per logica applicativa, non per una scrittura diretta, prima di cambiare qualcosa.

Questo spiega anche la scelta di MVVM invece di MVP: un Presenter MVP dovrebbe aggiornare la View direttamente tramite un riferimento esplicito a essa, il che comprometterebbe l'istanziabilità con \texttt{new()} e la testabilità senza \texttt{TestBed} del ViewModel. Con MVVM è invece Angular stesso a occuparsi della sincronizzazione: \texttt{ChangeDetectionStrategy.OnPush} ridisegna la View a ogni cambiamento di un segnale letto nel template, senza che il ViewModel debba mai sapere che la View esiste.

\subsubsection{AssistantPageViewModel}

Il ViewModel più corposo dei tre, perché copre l'intero ciclo di vita di una bozza (generazione, rigenerazione, valutazione, copertina, storico, configurazioni di prompt salvate) oltre alla presentazione delle proprie metriche. Il test unitario associato (\texttt{assistant-page.view-model.spec.ts}) lo costruisce con \texttt{new} passando un \texttt{AssistantService} e un \texttt{MvpStateStore} simulati a mano (senza avviare Angular), a riprova diretta che qui MVVM non è solo nominale.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontend/AssistantPageViewModel.png}
    \caption{Classe --- AssistantPageViewModel}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{assistant: AssistantService}, \texttt{store: MvpStateStore}: dipendenze ricevute dal costruttore, non da \texttt{inject()}.
    \item \texttt{phase}, \texttt{isGenerating}, \texttt{latestDraft}, \texttt{previewDraft}, \texttt{status}: segnali sullo stato della generazione in corso e della bozza visualizzata.
    \item \texttt{pendingScrollTarget: WritableSignal<string | null>}: destinazione di uno scorrimento richiesto dal ViewModel, non ancora consumato dalla View (si veda l'inizio della Sezione 6.2); scritto da \texttt{selectDraft()}, \texttt{discard()}, \texttt{useConfiguration()} e da \texttt{handleProgress()}, azzerato dalla View dopo averlo eseguito.
    \item \texttt{error: Signal<string | null>}, \texttt{loading: Signal<boolean>}: combinano \texttt{store.error()}/\texttt{store.loading()} con \texttt{store.filteredAssistantError()}/\texttt{store.filteredAssistantLoading()}, così il banner e lo spinner principali riflettono anche un fallimento o un caricamento della sola sezione Metriche filtrata, non solo dello stato condiviso.
    \item \texttt{metricsFilters: WritableSignal<AssistantMetricsFilters>}, \texttt{hasActiveMetricsFilters: Signal<boolean>}: filtri della sezione Metriche (tono, stile, intervallo di date — RF38-OB..RF41-OB), separati da \texttt{activeFilters} sotto: filtrare le metriche non deve filtrare anche lo storico, e viceversa.
    \item \texttt{metrics}: pass-through su \texttt{store.filteredAssistantState()?.metrics}, esclusa la voce \texttt{assistant.drafts} (le bozze da valutare sono una priorità e vivono nella Overview; qui sono già la parte "in bozza" di \texttt{draftComposition}, sotto). Legge dalla fetta filtrata dello Store, non da \texttt{store.assistantMetrics()}: quest'ultima resterebbe sempre sul totale non filtrato.
    \item \texttt{metricsPresentation: Signal<Record<string, MetricPresentation>>}: per ciascuna delle quattro metriche mostrate (\texttt{assistant.total}, \texttt{assistant.prompt\_configurations}, \texttt{assistant.rated}, \texttt{assistant.rating\_average} — un andamento, un conteggio, una quota e una media a stelle, tutte a due celle), la forma della scheda e il suo ingombro nella griglia, letta da \texttt{MetricsPanelComponent} (Sezione 6.6).
    \item \texttt{recentFeedback: Signal<Communication[]>}: pass-through su \texttt{store.filteredAssistantState()?.recentFeedback} (UC-27.3), gli ultimi contenuti valutati con voto e commento testuale, ricalcolati secondo \texttt{metricsFilters} come il resto della sezione — non solo la media, che resta in \texttt{metricsPresentation} sopra.
    \item \texttt{metricsReportExportUrl: string}: URL statico del download del report riepilogativo delle metriche (\texttt{/api/v1/assistant/metrics/export}, RF34-OB), letto direttamente dal template come \texttt{href} del link di esportazione.
    \item \texttt{draftComposition: Signal<CompositionSlice[]>}: ripartizione delle comunicazioni fra "In bozza" e "Decise" (il totale meno le bozze), derivata dalla stessa fetta filtrata di \texttt{metrics} sopra.
    \item \texttt{activeFilters}, \texttt{filteredCommunications}, \texttt{historyError}: segnali sullo storico, letti dal template; scritti solo internamente da \texttt{reload()}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{generate(payload: CommunicationDraftForm): void}, \texttt{regenerate(): void}
    \item \texttt{rateDraft(payload: RateDraftPayload): void}, \texttt{selectDraft(communicationId: number): void}
    \item \texttt{uploadCover(file: File): void}, \texttt{removeCover(): void}
    \item \texttt{saveDraft(event: \{communicationId: number, payload: UpdateCommunicationRequest\}): void}
    \item \texttt{discard(): void}, \texttt{saveToHistory(): void}, \texttt{deleteHistoryItem(communicationId: number): void}
    \item \texttt{toggleFavorite(communication: Communication): void}
    \item \texttt{saveConfiguration(payload: SavePromptConfigurationRequest): void}, \texttt{useConfiguration(configuration: PromptConfiguration): void}, \texttt{deleteConfiguration(configurationId: number): void}
    \item \texttt{setActiveFilters(filters: CommunicationFilters): void}
    \item \texttt{setMetricsFilters(filters: AssistantMetricsFilters): void}
    \item \texttt{refreshFilteredMetrics(): void}: rilegge la sezione Metriche con il filtro attivo corrente, tramite \texttt{store.reloadFilteredAssistantMetrics()} (RF38-OB).
    \item \texttt{reload(): void}: rilegge lo storico con i filtri correnti; stessa forma delle altre azioni, chiamata dall'\texttt{effect()} della View a ogni cambio di filtro o di stato condiviso. Annulla la propria sottoscrizione precedente prima di avviarne una nuova: senza questa cancellazione, una risposta di ricerca arrivata in ritardo potrebbe sovrascrivere un risultato più recente.
    \item \texttt{reloadState(): void}: ricarica lo stato condiviso tramite \texttt{store.reload()} e la sezione Metriche filtrata tramite \texttt{refreshFilteredMetrics()}, invocato dal pulsante "Riprova" del banner d'errore principale. Distinto da \texttt{reload()}, che rilegge solo lo storico comunicazioni: confonderli lascerebbe lo stato globale in errore pur avendo ricaricato l'elenco.
    \item \texttt{destroy(): void}: chiamato dalla View alla distruzione del componente (tramite \texttt{DestroyRef}): annulla solo la sottoscrizione di lettura in corso, non le azioni di scrittura (\texttt{generate()}, \texttt{discard()}...), che devono completare lato server anche se l'utente ha già navigato altrove.
\end{itemize}

\subsubsection{CopilotPageViewModel}

Stessa forma di \texttt{AssistantPageViewModel} applicata al Co-Pilot, con una superficie più piccola: copre upload, eliminazione, revisione dei dati estratti e correzione del messaggio di invio. Non ha bisogno di una dipendenza di scorrimento (nessuna delle sue azioni richiede di spostare la pagina verso una sezione), ma possiede una fetch propria oltre allo storico documenti: la raggiungibilità dell'anteprima del documento selezionato.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontend/CopilotPageViewModel.png}
    \caption{Classe --- CopilotPageViewModel}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{workflow: DocumentWorkflowService}, \texttt{store: MvpStateStore}: dipendenze ricevute dal costruttore, non da \texttt{inject()}.
    \item \texttt{selectedDocumentId}, \texttt{selectedDocument}, \texttt{selectedDocumentIdForList}: segnali sul sotto-documento attualmente in revisione.
    \item \texttt{previewStatus: WritableSignal<DocumentPreviewStatus>}: raggiungibilità dell'anteprima del documento selezionato (\textit{idle}, \textit{loading}, \textit{available}, \textit{unavailable}, \textit{unreachable}), verificata prima che il dettaglio monti l'iframe del PDF; segue \texttt{selectedDocument}, richiesta di nuovo a ogni cambio tramite \texttt{loadPreviewStatus()}.
    \item \texttt{isUploading}, \texttt{uploadStatus}, \texttt{uploadPhase}, \texttt{isDeleting}, \texttt{isSavingReview}, \texttt{reviewError}, \texttt{reviewFieldErrors}, \texttt{isSavingSendMessage}, \texttt{sendMessageError}: segnali di stato per le rispettive azioni asincrone.
    \item \texttt{activeFilters}, \texttt{hasActiveFilters}, \texttt{filteredDocuments}, \texttt{documentsError}: segnali sullo storico documenti, scritti solo internamente da \texttt{reload()}.
    \item \texttt{currentPage: WritableSignal<number>} (default 1), \texttt{totalDocuments: WritableSignal<number>} (default 0), \texttt{pageSize}: paginazione dello storico, dieci righe a pagina.
    \item \texttt{totalPages: Signal<number>}, \texttt{hasPreviousPage: Signal<boolean>}, \texttt{hasNextPage: Signal<boolean>}: segnali derivati da \texttt{currentPage}/\texttt{totalDocuments}, letti da \texttt{DocumentListComponent} per i controlli di paginazione.
    \item \texttt{error}, \texttt{loading}: pass-through su \texttt{store.error()}/\texttt{store.loading()}, così il template non legge mai lo Store direttamente.
    \item \texttt{metrics}: pass-through su \texttt{store.copilotMetrics()}, escluse le voci che il pannello non mostra come scheda a sé (\texttt{copilot.ocr\_confidence}, \texttt{copilot.validated}, \texttt{copilot.quarantined}, \texttt{copilot.sub\_documents}): \texttt{validated} e \texttt{quarantined} sono esiti di validazione, \texttt{quarantined} vive anche come priorità nella Overview; \texttt{sub\_documents} è il totale su cui si misurano le quote; \texttt{ocr\_confidence} non risponde a nessun caso d'uso UC-56, ma resta nel contratto perché la Overview la legge (\texttt{vm.copilotQuality()}, Sezione 6.2).
    \item \texttt{metricsPresentation: Signal<Record<string, MetricPresentation>>}: stesso ruolo di \texttt{AssistantPageViewModel.metricsPresentation}, per le cinque metriche mostrate (\texttt{copilot.documents}, \texttt{copilot.processing\_seconds} — due andamenti stretti — e tre quote larghe: \texttt{copilot.auto\_classified} per UC-56.2, \texttt{copilot.needs\_review} per UC-56.3, \texttt{copilot.recipient\_auto\_matched} per UC-56.4). L'ordine delle chiavi non è arbitrario: la griglia CSS non ricompone le righe per colmare i vuoti, quindi le due schede strette devono precedere le tre larghe perché le righe si chiudano senza celle vuote (due celle, poi sei, chiudono due righe da quattro).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{setActiveFilters(filters: DocumentFilters): void}: azzera anche \texttt{currentPage} a 1 — cambiare filtro rimescola i risultati, e restare su una pagina che con il nuovo filtro non esiste più mostrerebbe una tabella vuota senza spiegazione.
    \item \texttt{goToPage(page: number): void}: va alla pagina indicata, vincolata fra 1 e \texttt{totalPages()}; se cambia, aggiorna \texttt{currentPage} e richiama \texttt{reload()}.
    \item \texttt{reload(): void}: stessa cancellazione della sottoscrizione precedente vista in \texttt{AssistantPageViewModel.reload()}; legge anche \texttt{currentPage}/\texttt{pageSize} nella richiesta al backend. Il corpo gira dentro \texttt{untracked()}: \texttt{CopilotPage} lo chiama da un \texttt{effect()} che legge solo \texttt{store.documents()}/\texttt{activeFilters()} apposta, per non far ripartire la ricerca due volte a ogni cambio pagina (\texttt{goToPage()} richiama già \texttt{reload()} da sé) — senza \texttt{untracked()}, la lettura di \texttt{currentPage()} qui dentro diventerebbe comunque una dipendenza nascosta di quell'effect, e ogni "pagina successiva" scatenerebbe due richieste invece di una.
    \item \texttt{loadPreviewStatus(previewUrl: string | null): void}: richiede di nuovo la raggiungibilità dell'anteprima, stessa forma di \texttt{reload()} ma sul documento selezionato invece che sullo storico; annulla la propria sottoscrizione precedente, così una risposta tardiva su un documento ormai deselezionato non sovrascrive lo stato di quello corrente. Con \texttt{previewUrl} nullo azzera lo stato a \textit{idle} senza chiamare il backend.
    \item \texttt{reloadState(): void}: ricarica lo stato condiviso tramite \texttt{store.reload()}, invocato dal pulsante "Riprova" del banner d'errore principale; distinto da \texttt{reload()} come nel ViewModel dell'Assistant.
    \item \texttt{destroy(): void}: annulla sia la sottoscrizione di ricerca sia quella di anteprima.
    \item \texttt{selectDocument(documentId: string | null): void}
    \item \texttt{upload(request: DocumentUploadRequest): void}, \texttt{deleteDocument(documentId: string): void}
    \item \texttt{markReviewed(documentId: string): void}
    \item \texttt{saveReview(event: \{documentId: string, payload: UpdateExtractedDataRequest\}): void}
    \item \texttt{saveSendMessage(event: \{documentId: string, payload: UpdateSendMessageRequest\}): void}
\end{itemize}

\subsubsection{OverviewPageViewModel}

Il più semplice dei tre nella forma del costruttore (nessuna azione di scrittura verso il backend): sintetizza priorità operative e indicatori di qualità per entrambi i moduli, così il template legge solo \texttt{vm.*} invece di raggiungere direttamente lo Store. Tutti i valori derivano da metriche sull'intero tenant tramite \texttt{store.metricEntry()}, non da un ricalcolo sulle liste locali: \texttt{history} e \texttt{documents} nello Store sono finestre limitate (rispettivamente 10 e 40 elementi), e un valore derivato da quelle finestre sarebbe silenziosamente sbagliato appena i dati reali le superano.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontend/OverviewPageViewModel.png}
    \caption{Classe --- OverviewPageViewModel}
\end{figure}

Due tipi di supporto, definiti in questo stesso file, formattano i dati prima che raggiungano il template: \texttt{QualityMetric} (\texttt{label}, \texttt{value}, \texttt{unit}, \texttt{numeric}, \texttt{threshold}) per un indicatore di qualità già pronto per una scheda su scala; \texttt{PriorityMetric} (\texttt{key}, \texttt{label}, \texttt{value}, \texttt{tone}, \texttt{history}, \texttt{context}) per una priorità operativa con il proprio andamento a sette giorni.

Attributi
\begin{itemize}\raggedright
    \item \texttt{store: MvpStateStore}, \texttt{router: Router}: dipendenze ricevute dal costruttore (nessuna funzione di scorrimento: si veda \texttt{pendingScrollTarget} sotto).
    \item \texttt{communications}: pass-through su \texttt{store.history()}.
    \item \texttt{error: Signal<string | null>}, \texttt{loading: Signal<boolean>}: pass-through su \texttt{store.error()}/\texttt{store.loading()}, come già su \texttt{AssistantPageViewModel} e \texttt{CopilotPageViewModel}.
    \item \texttt{priorities: Signal<PriorityMetric[]>}: le tre metriche su cui si agisce (\textit{da verificare}, \textit{in quarantena}, \textit{bozze da valutare}), non un riassunto di tutte: i conteggi puramente descrittivi restano nelle pagine dei rispettivi moduli.
    \item \texttt{assistantQuality: Signal<QualityMetric | null>}, \texttt{copilotQuality: Signal<QualityMetric | null>}: un solo indicatore di qualità per modulo (voto medio delle bozze per l'Assistant, confidenza media OCR per il Co-Pilot), con rimando alla pagina che ne porta il dettaglio.
    \item \texttt{quarantined: Signal<number>}: numero di sotto-documenti in quarantena, letto da \texttt{store.metricEntry("copilot.quarantined")}, usato per decidere se mostrare la segnalazione d'attenzione in cima alla sezione priorità.
    \item \texttt{pendingScrollTarget: WritableSignal<string | null>}: stesso ruolo e stesso protocollo di consumo/azzeramento di \texttt{AssistantPageViewModel.pendingScrollTarget}, scritto da \texttt{navigate()}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{navigate(view: MvpView, targetId: string): void}: naviga verso un'altra pagina e, a navigazione completata, chiede lo scorrimento verso la sezione target scrivendo \texttt{pendingScrollTarget}.
    \item \texttt{reload(): void}: ricarica lo stato condiviso tramite \texttt{store.reload()}, invocato dal pulsante "Riprova" del banner d'errore principale. A differenza degli altri due ViewModel non esiste un \texttt{reloadState()} distinto: la Overview non ha uno storico proprio da rileggere separatamente dallo stato condiviso.
\end{itemize}

\subsubsection{MvpStateStore (stato condiviso fra i ViewModel)}

Sorgente unica dello stato applicativo (Assistant e Co-Pilot), condivisa da tutte le viste tramite i rispettivi ViewModel. Espone segnali di sola lettura derivati da un unico segnale privato, così che né i ViewModel né i componenti possano scrivere lo stato direttamente, ma solo tramite i metodi dedicati.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontend/MvpStateStore.png}
    \caption{Classe --- MvpStateStore}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{api: AlittlebyteMVPAPIService}: client HTTP generato dal contratto OpenAPI, iniettato tramite \texttt{inject()} (unico punto della classe accoppiato ad Angular, essendo un vero \texttt{@Injectable} e non un ViewModel puro).
    \item \texttt{state: Signal<MvpState | null>}, \texttt{loading: Signal<boolean>}, \texttt{error: Signal<string | null>}: segnali di sola lettura.
    \item \texttt{documents}, \texttt{history}, \texttt{recentFeedback}, \texttt{promptConfigurations}, \texttt{assistantMetrics}, \texttt{copilotMetrics}: segnali derivati (\texttt{computed}) sulle diverse viste dello stato; \texttt{recentFeedback} è il più recente (UC-27.3), gli ultimi contenuti valutati con voto e commento testuale.
    \item \texttt{filteredAssistantState: Signal<MvpState["assistant"] | null>}, \texttt{filteredAssistantLoading: Signal<boolean>}, \texttt{filteredAssistantError: Signal<string | null>}: fetta di stato separata da \texttt{state} per la sezione Metriche filtrata dell'AI Assistant (RF38-OB..RF41-OB, tono/stile/intervallo di date): applicare un filtro qui non cambia i numeri non filtrati che Overview e Co-Pilot leggono da \texttt{state}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{loadOnce(): void}: carica lo stato al primo montaggio della shell applicativa; deriva la guardia dallo stato reale dei segnali (\texttt{loading} o \texttt{state} già valorizzato), così un primo tentativo fallito può essere ritentato da una chiamata successiva invece di restare bloccato per sempre.
    \item \texttt{reload(): void}: ricarica lo stato dal backend, con un tentativo di retry per errori temporanei. Ignora una chiamata mentre una richiesta è già in volo, la stessa guardia già applicata da \texttt{loadOnce()}: senza di essa un doppio click su "Riprova" avvierebbe due \texttt{GET /state} in parallelo, l'ultima risposta vincerebbe comunque ma la richiesta in più sarebbe sprecata.
    \item \texttt{reloadFilteredAssistantMetrics(filters: AssistantMetricsFilters): void}: ricarica solo la fetta Assistant della sezione Metriche, filtrata, su uno stato separato da \texttt{state}. Annulla la richiesta filtrata precedente ancora in volo prima di avviarne una nuova, stesso schema di \texttt{AssistantPageViewModel::reload()} per lo storico: un cambio di filtro mentre la risposta al precedente non è ancora arrivata non deve andare perso, altrimenti il pannello resterebbe fermo su un filtro che i controlli non mostrano più. Chiamato da un \texttt{effect()} della View a ogni cambio filtro: l'intero corpo gira dentro \texttt{untracked()}, perché una lettura di segnale al suo interno diventerebbe altrimenti una dipendenza di quell'effect, innescando un ciclo di richieste che non si ferma mai.
    \item \texttt{setState(state: MvpState): void}: rimpiazza lo stato con quello autorevole restituito da una mutazione.
    \item \texttt{upsertDocument(document: SubDocument): void}: inserisce o aggiorna in testa un sotto-documento ricevuto dallo stream SSE, senza ricaricare l'intero stato.
    \item \texttt{metric(key: string): number}: legge il valore di una metrica per chiave stabile, evitando di ricalcolarla su elenchi parzialmente caricati; \texttt{0} se la chiave non esiste o lo stato non è ancora caricato.
    \item \texttt{metricEntry(key: string): Metric | null}: come \texttt{metric()} ma restituisce la metrica completa (compresa \texttt{history}, che il solo valore non porta) o \texttt{null} finché lo stato non è disponibile, invece di collassare l'assenza su \texttt{0} — una scheda che mostrasse \texttt{0} durante il caricamento affermerebbe un dato che non ha ancora. \texttt{metric()} lo chiama internamente invece di duplicarne la logica di ricerca.
\end{itemize}

% ── 6.3 ──────────────────────────────────────────────────────────────────────
\subsection{Definizione dei modelli di dati}

Il livello Model si articola su due strati. Il primo è generato automaticamente da Orval a partire dal contratto OpenAPI (Sezione 2.4): interfacce TypeScript per ogni schema (\texttt{Communication}, \texttt{SubDocument}, \texttt{MvpState}...), non modificabili a mano. Il secondo è specifico di ciascuna feature e arricchisce i tipi generati con concetti che esistono solo lato UI (fasi di una pipeline, etichette, vincoli dei form) senza duplicarne i campi.

\subsubsection{assistant.model.ts}

Arricchisce i tipi generati per il modulo Assistant. \texttt{CommunicationGenerationPhase} traduce le combinazioni di \texttt{generationStatus}/\texttt{coverStatus} restituite dal backend in un'unica fase discreta, usata dal template per etichette e stato dei controlli senza replicare quella logica in più punti; \texttt{GeneratedDraft} è la proiezione della bozza usata dal pannello di anteprima, con solo i campi che quella vista consuma.

\paragraph{CommunicationDraftForm}

Alias del tipo di richiesta generato da OpenAPI, usato dal form di generazione senza introdurre una forma propria.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/CommunicationDraftForm.png}
    \caption{Tipo --- CommunicationDraftForm}
\end{figure}

\paragraph{CommunicationGenerationPhase}

Unione di stringhe letterali che traduce le combinazioni di \texttt{generationStatus}/\texttt{coverStatus} restituite dal backend in un'unica fase discreta, usata dal template per etichette e stato dei controlli senza replicare quella logica in più punti.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/CommunicationGenerationPhase.png}
    \caption{Tipo --- CommunicationGenerationPhase}
\end{figure}

\paragraph{CommunicationGenerationProgress}

Proiezione dell'evento di avanzamento SSE ricevuto durante la generazione: testo e copertina restano opzionali perché arrivano in momenti diversi della pipeline.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/CommunicationGenerationProgress.png}
    \caption{Tipo --- CommunicationGenerationProgress}
\end{figure}

\paragraph{GeneratedDraft}

Proiezione della bozza usata dal pannello di anteprima, con solo i campi che quella vista consuma, non l'intera \texttt{Communication} generata da OpenAPI.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/GeneratedDraft.png}
    \caption{Tipo --- GeneratedDraft}
\end{figure}

\paragraph{RateDraftPayload}

Payload minimo per la valutazione di una bozza: stelle e commento facoltativo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.25\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/RateDraftPayload.png}
    \caption{Tipo --- RateDraftPayload}
\end{figure}

\paragraph{communicationTones e communicationStyles}

Le opzioni di tono e stile selezionabili nel form di generazione, condivise fra form e validazione così da non poter divergere.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.28\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/communicationTones.png}
    \caption{Tipo --- communicationTones}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.28\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/communicationStyles.png}
    \caption{Tipo --- communicationStyles}
\end{figure}

\subsubsection{Tipi del modulo Co-Pilot}

L'equivalente Co-Pilot di \texttt{assistant.model.ts}, distribuito fra \texttt{document-workflow.service.ts} (i tipi) e \texttt{document-filters.ts} (il form dei filtri e la sua conversione). \texttt{DocumentUploadPhase} traduce l'evento SSE \texttt{progress} in una fase discreta per la barra di avanzamento, analogamente a \texttt{CommunicationGenerationPhase} sul modulo Assistant.

\texttt{toDocumentFilters()} converte i valori grezzi del form nei criteri inviati all'API scartando quelli vuoti, evitando percorsi che possano sollevare eccezioni: un valore inatteso nel form non deve poter interrompere silenziosamente lo stream dei filtri.

\paragraph{DocumentUploadPhase}

Fase dell'elaborazione usata dalla barra di progressione a step; \textit{still\_running} riflette l'omonima fase SSE del backend (Sezione 6.4, \texttt{SseClient}) quando lo stream supera il timeout senza che la pipeline sia effettivamente fallita.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentUploadPhase.png}
    \caption{Tipo --- DocumentUploadPhase}
\end{figure}

\paragraph{DocumentFilters}

Criteri di filtro per l'elenco documenti mostrato nel Co-Pilot: ricerca testuale, stato di invio, soglia di confidenza col relativo criterio di confronto, mese e anno.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentFilters.png}
    \caption{Tipo --- DocumentFilters}
\end{figure}

\paragraph{ConfidenceCriterion}

Criterio di confronto per il filtro di confidenza: sopra o sotto la soglia impostata.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/ConfidenceCriterion.png}
    \caption{Tipo --- ConfidenceCriterion}
\end{figure}

\paragraph{DocumentUploadMetadata}

Metadati manuali inviati insieme al file in upload: tipo di documento, ragione sociale, mese e anno di riferimento — gli stessi che il backend espone tramite \texttt{OriginalDocument::hasManualUploadMetadata()} (Sezione 5.1).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentUploadMetadata.png}
    \caption{Tipo --- DocumentUploadMetadata}
\end{figure}

\paragraph{DocumentUploadProgress}

Avanzamento dell'upload e dell'elaborazione di un documento, con l'identificativo ricevuto non appena disponibile.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentUploadProgress.png}
    \caption{Tipo --- DocumentUploadProgress}
\end{figure}

\paragraph{DocumentPage}

Una pagina dello storico documenti, con il totale che le sta dietro: senza \texttt{total} la vista non può sapere quante pagine esistono. \texttt{CopilotPageViewModel} (Sezione 6.2) naviga esplicitamente fra le pagine tramite \texttt{goToPage()}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentPage.png}
    \caption{Tipo --- DocumentPage}
\end{figure}

\paragraph{DocumentPreviewStatus}

Stato dell'anteprima PDF di un sotto-documento, dal caricamento all'eventuale indisponibilità.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentPreviewStatus.png}
    \caption{Tipo --- DocumentPreviewStatus}
\end{figure}

\paragraph{DocumentFilterFormValue}

Tipo derivato dal \texttt{FormGroup} dei filtri tramite \texttt{ReturnType}, così i due restano sincronizzati senza doverli dichiarare due volte.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendModel/DocumentFilterFormValue.png}
    \caption{Tipo --- DocumentFilterFormValue}
\end{figure}

\paragraph{createDocumentFilterForm e toDocumentFilters}

Le due funzioni che costruiscono il \texttt{FormGroup} dei filtri e ne convertono il valore nei criteri inviati all'API.

Metodi
\begin{itemize}\raggedright
    \item \texttt{createDocumentFilterForm(): FormGroup}
    \item \texttt{toDocumentFilters(value: DocumentFilterFormValue): DocumentFilters}
\end{itemize}

% ── 6.4 ──────────────────────────────────────────────────────────────────────
\subsection{Servizi di comunicazione con il backend}

Ogni feature ha un servizio dedicato (\texttt{AssistantService}, \texttt{DocumentWorkflowService}) che incapsula le chiamate al client generato e aggiorna lo Store con la risposta autorevole. Nessun componente né ViewModel chiama direttamente il client HTTP: passa sempre dal servizio della propria feature, ricevuto come dipendenza dal costruttore del ViewModel (Sezione 6.2).

\subsubsection{AssistantService}

Il servizio più corposo del frontend, perché incapsula anche il consumo dello stream SSE lato client, simmetrico a quello che il backend espone (Sezione 3.2). \texttt{trackGeneration()} avvolge la chiamata HTTP iniziale e la connessione SSE risultante in un unico \texttt{Observable}: chi lo sottoscrive riceve una sequenza di stati di avanzamento (\textit{queued} → testo → copertina → \textit{completed}/\textit{failed}) senza dover gestire direttamente gli eventi SSE.

La meccanica di basso livello (apertura dello stream, parsing dei frame, distinzione fra evento nominato \texttt{error} e caduta della connessione) vive in \texttt{SseClient} (più avanti in questa sezione); questo servizio resta responsabile solo dell'orchestrazione specifica di feature (interpretazione degli eventi \textit{progress}/\textit{text}/\textit{cover}/\textit{done}, aggiornamento dello Store).

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendServizi/AssistantService.png}
    \caption{Classe --- AssistantService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{api: AlittlebyteMVPAPIService}, \texttt{store: MvpStateStore}, \texttt{sse: SseClient}.
\end{itemize}

Metodi
\begin{itemize}\raggedright\small
    \item \texttt{generate(payload: CommunicationDraftForm):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationGenerationProgress>}
    \item \texttt{regenerate(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationGenerationProgress>}
    \item \texttt{update(communicationId: number, payload: UpdateCommunicationRequest):}\\ \hspace*{1.5em}\texttt{Observable<UpdateCommunicationResponse>}: usato da \texttt{AssistantPageViewModel.saveDraft()} per correggere titolo e corpo della bozza.
    \item \texttt{searchCommunications(filters: CommunicationFilters):}\\ \hspace*{1.5em}\texttt{Observable<Communication[]>}
    \item \texttt{rate(communicationId: number, payload: RateCommunicationRequest):}\\ \hspace*{1.5em}\texttt{Observable<RateCommunicationResponse>}
    \item \texttt{favorite(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<UpdateCommunicationFavoriteResponse>}
    \item \texttt{unfavorite(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<UpdateCommunicationFavoriteResponse>}
    \item \texttt{updateCoverImage(communicationId: number, image: File):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationMutationResponse>}
    \item \texttt{removeCoverImage(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationMutationResponse>}
    \item \texttt{saveToHistory(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationMutationResponse>}
    \item \texttt{discard(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<CommunicationMutationResponse>}
    \item \texttt{deleteFromHistory(communicationId: number):}\\ \hspace*{1.5em}\texttt{Observable<DeleteDocumentResponse>}
    \item \texttt{saveConfiguration(payload: SavePromptConfigurationRequest):}\\ \hspace*{1.5em}\texttt{Observable<SavePromptConfigurationResponse>}
    \item \texttt{deleteConfiguration(configurationId: number):}\\ \hspace*{1.5em}\texttt{Observable<DeleteDocumentResponse>}
\end{itemize}

\subsubsection{DocumentWorkflowService}

Equivalente Co-Pilot di \texttt{AssistantService}, con lo stesso incapsulamento dello stream SSE lato client. \texttt{upload()} avvolge la chiamata HTTP di caricamento e la connessione SSE risultante in un unico \texttt{Observable}, esattamente come \texttt{trackGeneration()}: anche qui la meccanica di basso livello è delegata a \texttt{SseClient} (sottosezione successiva).

\texttt{previewStatus()} è invece un caso specifico di questo modulo, assente sul dominio Communications e indipendente da SSE: verifica il \textit{content-type} della risposta prima di montare l'iframe di anteprima, perché l'endpoint può restituire il PDF (200), un 404 se il file non è ancora disponibile o un 503 JSON se lo storage non è raggiungibile: tre esiti che il componente chiamante deve distinguere per mostrare lo stato corretto.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendServizi/DocumentWorkflowService.png}
    \caption{Classe --- DocumentWorkflowService}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{api: AlittlebyteMVPAPIService}, \texttt{store: MvpStateStore}, \texttt{sse: SseClient}.
\end{itemize}

Metodi
\begin{itemize}\raggedright\small
    \item \texttt{upload(file: File, metadata: DocumentUploadMetadata):}\\ \hspace*{1.5em}\texttt{Observable<DocumentUploadProgress>}
    \item \texttt{deleteSubDocument(documentId: string):}\\ \hspace*{1.5em}\texttt{Observable<DeleteDocumentResponse>}
    \item \texttt{saveExtractedData(documentId: string, payload: UpdateExtractedDataRequest):}\\ \hspace*{1.5em}\texttt{Observable<UpdateSubDocumentReviewResponse>}
    \item \texttt{markReviewed(documentId: string):}\\ \hspace*{1.5em}\texttt{Observable<UpdateSubDocumentReviewResponse>}
    \item \texttt{saveSendMessage(documentId: string, payload: UpdateSendMessageRequest):}\\ \hspace*{1.5em}\texttt{Observable<UpdateSubDocumentReviewResponse>}
    \item \texttt{searchDocuments(filters: DocumentFilters, page: number, perPage: number):}\\ \hspace*{1.5em}\texttt{Observable<DocumentPage>}: restituisce la pagina intera (\texttt{items} più \texttt{total}), non i soli elementi — senza il totale la vista non può sapere quante pagine esistono.
    \item \texttt{previewStatus(previewUrl: string):}\\ \hspace*{1.5em}\texttt{Observable<DocumentPreviewStatus>}
\end{itemize}

\subsubsection{SseClient}

Client SSE condiviso da entrambi i servizi di feature, iniettabile a livello applicazione (\texttt{providedIn: "root"}). A differenza di \texttt{EventSource}, è basato su \texttt{fetch}: può quindi inviare gli header di correlazione (\texttt{X-Request-ID}, \texttt{X-Correlation-ID}) sulla richiesta iniziale di apertura dello stream, cosa che \texttt{EventSource} non permette.

Distingue esplicitamente l'evento SSE nominato \texttt{error} (dati applicativi, es. un messaggio di errore) dalla caduta della connessione o da una risposta non riuscita: due condizioni che \texttt{EventSource} collassa in un unico \texttt{onerror}, costringendo il chiamante a indovinare quale sia successo.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendServizi/SseClient.png}
    \caption{Classe --- SseClient}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item Nessuno: nessuno stato d'istanza, ogni chiamata a \texttt{connect()} è indipendente.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{connect(url: string, handlers: SseHandlers): () => void}: apre lo stream e restituisce una funzione di chiusura (basata su \texttt{AbortController}), da invocare alla disiscrizione.
\end{itemize}

% ── 6.5 ──────────────────────────────────────────────────────────────────────
\subsection{Serializzazione e deserializzazione dei dati}

A differenza di un backend Rails con serializer scritti a mano per ogni risorsa, qui la serializzazione verso e dal backend è interamente generata: Orval crea, insieme alle interfacce dei modelli, anche le funzioni client che serializzano il corpo delle richieste e deserializzano le risposte JSON secondo i tipi del contratto OpenAPI. Non esiste quindi un adapter di serializzazione scritto a mano da documentare: la conformità fra client e contratto è verificata automaticamente in CI (Sezione 2.10, \texttt{openapi:check}), che rigenera il client e fallisce se il risultato differisce da quello committato.

% ── 6.6 ──────────────────────────────────────────────────────────────────────
\subsection{Micro componenti UI}

I componenti condivisi (\texttt{shared/components}) sono privi di logica di dominio: ricevono dati tramite segnali di input e riemettono eventi tramite \texttt{output()}, senza conoscere da quale feature vengono usati. Sono ventuno in tutto: \texttt{ButtonComponent}, \texttt{AlertComponent}, \texttt{ErrorStateComponent}, \texttt{EmptyStateComponent}, \texttt{LoadingStateComponent}, \texttt{StatusBadgeComponent}, \texttt{StatusDotComponent}, \texttt{AttentionNoteComponent}, \texttt{MetricCardComponent}, \texttt{MetricsPanelComponent}, \texttt{MetricShareComponent}, \texttt{MetricGaugeComponent}, \texttt{MetricStatusComponent}, \texttt{MetricBreakdownComponent}, \texttt{MetricStarsComponent}, \texttt{MetricCompositionComponent}, \texttt{StageProgressComponent}, \texttt{StarRating}, \texttt{SelectFieldComponent}, \texttt{TextAreaFieldComponent}, \texttt{FileDropzoneComponent}. La cartella \texttt{data-table} contiene solo un foglio di stile condiviso, non un componente: le tabelle restano markup HTML nativo nelle singole pagine, non un elemento riutilizzabile a sé.

\texttt{MetricsPanelComponent} è un dispatcher con più componenti di presentazione dedicati, ciascuno la risposta visiva a un tipo diverso di domanda che una metrica può porre (un andamento, una quota, una posizione su scala, una ripartizione...): cinque forme dedicate oltre a quella di default (Sezione successiva). \texttt{StarRating} vive in \texttt{shared/components} con due punti di utilizzo: la scheda \texttt{assistant.rating\_average} nel pannello metriche dell'Assistant, e la valutazione della bozza in anteprima.

\subsubsection{ButtonComponent}

Il più semplice dei componenti condivisi: non introduce un nuovo elemento HTML, ma si applica come attributo su un \texttt{<button>} nativo (\texttt{<button mvpButton variant="secondary">}). In questo modo il componente lascia al browser tipo, stato disabilitato, attributi ARIA ed eventi nativi, occupandosi solo della variante di stile applicata all'host. \texttt{busy} segnala che il comando è in corso senza toccare l'etichetta, che resta ferma invece di cambiare a metà clic; \texttt{aria-busy} porta la stessa informazione alle tecnologie assistive.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/ButtonComponent.png}
    \caption{Classe --- ButtonComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{variant: InputSignal<"primary" | "secondary" | "icon">}: variante di stile, di default \textit{primary}.
    \item \texttt{type: InputSignal<string>}: tipo dell'attributo HTML \texttt{type}, di default \textit{button}.
    \item \texttt{busy: InputSignal<boolean>}: default \textit{false}; mostra uno spinner sull'host e imposta \texttt{aria-busy}, senza sostituire il contenuto proiettato.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno: il comportamento è interamente dichiarativo, espresso nei binding \texttt{host} del decoratore \texttt{@Component}.
\end{itemize}

\subsubsection{AlertComponent, ErrorStateComponent}

\texttt{AlertComponent} è il riquadro di base per un messaggio con titolo, usato ovunque serva segnalare qualcosa con enfasi (\texttt{role="alert"}, \texttt{aria-live="polite"}).

\texttt{ErrorStateComponent} non duplica quel markup: lo compone, fissando un titolo di default (\textit{"Servizio non disponibile"}) e passando il messaggio d'errore come contenuto proiettato: è il componente che compare in cima a ogni pagina quando lo stato di errore è valorizzato. Espone anche un pulsante di ripetizione opzionale (\textit{"Riprova"}), renderizzato solo quando \texttt{canRetry()} è vero: alla pressione emette l'evento \texttt{retry}, lasciando al chiamante decidere cosa ritentare.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/AlertErrorState.png}
    \caption{Classi --- AlertComponent, ErrorStateComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{AlertComponent}: \texttt{title: InputSignal<string>} (obbligatorio).
    \item \texttt{ErrorStateComponent}: \texttt{message: InputSignal<string>} (obbligatorio), \texttt{title: InputSignal<string>} (default \textit{"Servizio non disponibile"}), \texttt{canRetry: InputSignal<boolean>} (default \textit{false}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{ErrorStateComponent}: \texttt{retry: OutputEmitterRef<void>}: emesso al click sul pulsante "Riprova", visibile solo se \texttt{canRetry()} è vero.
    \item \texttt{AlertComponent}: nessuno.
\end{itemize}

\subsubsection{EmptyStateComponent, LoadingStateComponent, StatusBadgeComponent, StatusDotComponent}

Quattro componenti minimi di visualizzazione, senza alcuna logica: si limitano a proiettare contenuto o un'etichetta dentro un elemento con lo stile e la semantica ARIA corretti (\texttt{role="status"} per il caricamento, colori diversi per tono sull'etichetta di stato). \texttt{StatusDotComponent} è un indicatore binario (fatto/non fatto) da affiancare a \texttt{StatusBadgeComponent} senza confondersi con esso, perché rispondono a due domande diverse — l'etichetta dice in quale dei quattro stati di revisione si trova un sotto-documento, il pallino se è stato scaricato o no. Pieno contro vuoto non è il solo colore a portare l'informazione (SC 1.4.1): il pallino è comunque \texttt{aria-hidden}, perché ripete un'informazione che il testo proiettato accanto dice già.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/EmptyLoadingStatus.png}
    \caption{Classi --- EmptyStateComponent, LoadingStateComponent, StatusBadgeComponent, StatusDotComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{EmptyStateComponent}: nessuno, solo \texttt{<ng-content>}.
    \item \texttt{LoadingStateComponent}: \texttt{label: InputSignal<string>} (default \textit{"Caricamento in corso."}).
    \item \texttt{StatusBadgeComponent}: \texttt{tone: InputSignal<StatusTone>} (\textit{info}, \textit{success}, \textit{warning}, \textit{danger}, default \textit{neutral}).
    \item \texttt{StatusDotComponent}: \texttt{done: InputSignal<boolean>} (default \textit{false}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

\subsubsection{AttentionNoteComponent}

Riga che segnala qualcosa su cui agire, con l'azione che la risolve: una scheda metrica dice \textit{quanto} ("23 da verificare"), questo componente dice \textit{cosa fare}, con un pulsante opzionale che porta alla coda interessata. Va montato solo quando c'è davvero qualcosa da fare (\texttt{OverviewPage} lo mostra solo se \texttt{vm.quarantined() > 0}, Sezione 6.7): mostrato sempre diventerebbe arredamento, e si smetterebbe di leggerlo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/AttentionNoteComponent.png}
    \caption{Classe --- AttentionNoteComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{title: InputSignal<string>} (obbligatorio), \texttt{detail: InputSignal<string | null>} (default \textit{null}).
    \item \texttt{tone: InputSignal<"watch" | "alert">} (default \textit{watch}): cambia glifo e colore insieme, mai il solo colore.
    \item \texttt{actionLabel: InputSignal<string | null>} (default \textit{null}): quando valorizzato monta il pulsante d'azione.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{acted: OutputEmitterRef<void>}: emesso al click sul pulsante d'azione.
\end{itemize}

\subsubsection{MetricCardComponent, MetricsPanelComponent}

\texttt{MetricCardComponent} mostra la forma di scheda di default (un conteggio e il proprio andamento a sette giorni). \texttt{MetricsPanelComponent} è un dispatcher: per ciascuna metrica ricevuta sceglie, in base alla mappa \texttt{presentation} passata dalla pagina (il tipo \texttt{MetricPresentation}, Sezione 6.2), quale fra sei componenti di presentazione montare (Sezione successiva) — \texttt{MetricCardComponent} resta la scelta di default quando la mappa non specifica un \texttt{kind} diverso. Gestisce inoltre da sé gli stati della richiesta a monte: caricamento (schede segnaposto con \texttt{isLoading}), errore (messaggio dedicato, distinto da "nessuna metrica": in errore i dati esistono, è la lettura ad essere fallita) o elenco vuoto (\texttt{EmptyStateComponent}). Montato da \texttt{AssistantPage} e \texttt{CopilotPage}; \texttt{OverviewPage} non lo usa (Sezione 6.7).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/MetricCardPanel.png}
    \caption{Classi --- MetricCardComponent, MetricsPanelComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{MetricCardComponent}: \texttt{label: InputSignal<string>} (obbligatorio), \texttt{value: InputSignal<string | number | null>} (obbligatorio, \texttt{null} distinto da zero: uno zero durante il caricamento affermerebbe un dato che non c'è ancora), \texttt{unit}, \texttt{tone}, \texttt{context}, \texttt{history: InputSignal<readonly number[] | undefined>} (flusso degli ultimi sette giorni, disegnato a barre — non a linea, perché sono ingressi distinti, non un valore continuo), \texttt{isLoading}.
    \item \texttt{MetricsPanelComponent}: \texttt{isLoading: InputSignal<boolean>} (default \textit{false}), \texttt{hasError: InputSignal<boolean>} (default \textit{false}), \texttt{metrics: InputSignal<Metric[]>} (default \texttt{[]}), \texttt{ariaLabel: InputSignal<string>} (default \textit{"Metriche"}), \texttt{presentation: InputSignal<Record<string, MetricPresentation>>} (default \texttt{\{\}}): forma, rilievo e ingombro nella griglia per ciascuna chiave di metrica, decisi dalla pagina che conosce il contesto, non dal pannello.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

\subsubsection{Le forme di scheda del pannello metriche}

Le cinque forme di presentazione oltre a quella di default, ciascuna la risposta a un tipo diverso di domanda che una metrica può porre: \texttt{MetricShareComponent} (quanta parte di un totale, con l'anello che colora anche il residuo perché è quello il numero su cui agire), \texttt{MetricGaugeComponent} (dove cade un valore dentro una scala nota, con la tacca della soglia oltre la quale il sistema decide da solo), \texttt{MetricStatusComponent} (c'è qualcosa da guardare? — il verdetto prima del numero, perché un conteggio di guasti vale quasi sempre zero e uno zero grande quanto un totale chiederebbe di fermarsi a capire), \texttt{MetricBreakdownComponent} (come si divide un insieme fra gli stati in cui si trova, un anello a più colori invece della quota a due) e \texttt{MetricStarsComponent} (una media su una scala a stelle, con l'ultima riempita per la frazione reale e non arrotondata). Tutte condividono lo stesso stato di caricamento (\texttt{isLoading}, schede segnaposto) e lo stesso involucro visivo (\texttt{shared/styles/metric-shell.css}), montate esclusivamente da \texttt{MetricsPanelComponent} in base al \texttt{kind} dichiarato nella mappa \texttt{presentation} della pagina. \texttt{copilot.processing\_seconds} è una singola media in secondi (kind \textit{trend}, come un conteggio qualunque): il modulo Communications non ha una metrica equivalente, la durata della generazione non è oggi esposta come voce del contratto \texttt{/api/v1/state}. Nessuna delle due pagine usa \texttt{MetricGaugeComponent} o \texttt{MetricStatusComponent} tramite il pannello: restano forme valide del dispatcher, e \texttt{MetricGaugeComponent} è montato direttamente da \texttt{OverviewPage} (Sezione 6.7) per la qualità del Co-Pilot.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/MetricPresentationKinds.png}
    \caption{Classi --- le cinque forme di scheda dispacciate da MetricsPanelComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{MetricShareComponent}: \texttt{label}, \texttt{value: InputSignal<number | null>}, \texttt{total: InputSignal<number | null>} (obbligatori), \texttt{restTone: InputSignal<"alert" | "neutral">} (default \textit{alert}: distingue un residuo che chiede un intervento da uno che è solo il complemento di una misura), \texttt{restNoun}, \texttt{isLoading}.
    \item \texttt{MetricGaugeComponent}: \texttt{label}, \texttt{value: InputSignal<string>} (obbligatori, già formattato), \texttt{numeric: InputSignal<number | null>} (posiziona il riempimento), \texttt{max} (default 100), \texttt{threshold: InputSignal<number | null>}, \texttt{unit}, \texttt{tone}, \texttt{context}, \texttt{isLoading}.
    \item \texttt{MetricStatusComponent}: \texttt{label}, \texttt{value: InputSignal<number | null>} (obbligatori), \texttt{okLabel} (default \textit{"Nessun caso"}), \texttt{issueLabel} (default \textit{"da esaminare"}), \texttt{issueTone: InputSignal<"danger" | "warning">} (default \textit{danger}: un guasto blocca, un degrado no), \texttt{context}, \texttt{isLoading}.
    \item \texttt{MetricBreakdownComponent}: \texttt{label: InputSignal<string>} (obbligatorio), \texttt{parts: InputSignal<MetricPart[] | undefined>}, \texttt{emptyLabel}, \texttt{isLoading}.
    \item \texttt{MetricStarsComponent}: \texttt{label}, \texttt{value: InputSignal<string>} (obbligatori, già formattato), \texttt{numeric: InputSignal<number | null>} (riempie le stelle), \texttt{max} (default 5), \texttt{context}, \texttt{isLoading}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno: ciascuna deriva internamente solo i segnali \texttt{computed} necessari al proprio disegno (l'arco dell'anello, il riempimento della scala, i segmenti della barra...), senza superficie pubblica oltre agli \texttt{input()}.
\end{itemize}

\subsubsection{MetricCompositionComponent}

Barra segmentata di una partizione completa, usata dove i conteggi affiancati nasconderebbero le proporzioni: la ripartizione delle bozze fra "in bozza" e "decise" in \texttt{AssistantPage} (\texttt{vm.draftComposition()}, Sezione 6.2) è la sola istanza attuale, montata fuori da \texttt{MetricsPanelComponent} perché non è una metrica di per sé, ma una vista sulla stessa metrica \texttt{assistant.total}/\texttt{assistant.drafts} già mostrata come scheda. Le fette usano una scala monocromatica derivata dal colore primario, non colori di stato: verde e rosso qui suggerirebbero un giudizio che una composizione non esprime.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/MetricCompositionComponent.png}
    \caption{Classe --- MetricCompositionComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{parts: InputSignal<readonly CompositionSlice[]>} (obbligatorio): coppie \texttt{\{label, value\}}.
    \item \texttt{emptyLabel: InputSignal<string>} (default \textit{"Nessun elemento da ripartire."}), \texttt{subject: InputSignal<string>} (default \textit{"elementi"}): nome della grandezza ripartita, usato nella descrizione accessibile.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

\subsubsection{StageProgressComponent}

Avanzamento per tappe, non a percentuale: le tappe (\texttt{ProgressStage}, \texttt{\{id, label\}}) sono gli stati reali che arrivano dallo stream SSE, quindi l'avanzamento è mostrato senza inventare una quantità — un documento non è mai "al 60\%" di niente; un contatore del tempo trascorso scorre accanto, perché senza di esso un'attesa lunga è indistinguibile da un blocco. Condiviso fra le pipeline asincrone di entrambi i moduli tramite due adattatori di feature (\texttt{GenerationProgressComponent} per l'Assistant, quattro tappe; \texttt{UploadProgressComponent} per il Co-Pilot, cinque), che mappano le rispettive fasi di dominio sull'elenco di \texttt{ProgressStage} ricevuto.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/StageProgressComponent.png}
    \caption{Classe --- StageProgressComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{stages: InputSignal<readonly ProgressStage[]>} (obbligatorio), \texttt{currentId: InputSignal<string | null>} (default \textit{null}, tappa raggiunta), \texttt{failed}, \texttt{slow} (annotazione sulla tappa corrente, non una tappa a sé: la pipeline resta nello stesso stato, solo da più tempo del previsto), \texttt{label} (default \textit{"Avanzamento elaborazione"}).
    \item \texttt{elapsedSeconds: Signal<number | null>}: cronometro interno, azzerato quando l'avanzamento torna indietro o riparte dopo essersi fermato — altrimenti una corsa nuova erediterebbe il tempo di quella precedente.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno: la sola logica è l'\texttt{effect()} nel costruttore che aggiorna il cronometro, interno al componente.
\end{itemize}

\subsubsection{StarRating}

Valutazione a stelle come gruppo di radio ARIA (il pattern che l'APG indica per una scala a valori discreti): le frecce scorrono i punteggi, il gruppo ha un nome, e ogni valore ne annuncia uno proprio ("3 stelle su 5"). Il disegno è un SVG disegnato due volte (contorno e riempimento ritagliato), così non dipende dal glifo di sistema; il contorno resta visibile in entrambi gli stati, cosicché chi non distingue i colori legga comunque quante stelle sono piene (SC 1.4.1).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/StarRatingComponent.png}
    \caption{Classe --- StarRating}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{rating: InputSignal<number>} (default 0), \texttt{disabled: InputSignal<boolean>} (default \textit{false}), \texttt{label: InputSignal<string>} (default \textit{"Valutazione da 1 a 5 stelle"}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{rated: OutputEmitterRef<number>}: emesso alla selezione di un punteggio; il punteggio mostrato resta guidato dal chiamante, il componente non lo applica da sé.
\end{itemize}

\subsubsection{SelectFieldComponent, TextAreaFieldComponent}

I due componenti di campo condivisi dai form reattivi della SPA, con la stessa struttura: un'etichetta associata via \texttt{<label for>}, un identificativo generato automaticamente dal testo dell'etichetta se non fornito esplicitamente, e un \texttt{FormControl} ricevuto come segnale di input invece che tramite \texttt{ControlValueAccessor}: una scelta più semplice, sufficiente perché nessuno dei due deve integrarsi con \texttt{ngModel}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/SelectTextAreaField.png}
    \caption{Classi --- SelectFieldComponent, TextAreaFieldComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{SelectFieldComponent}: \texttt{label}, \texttt{options: InputSignal<readonly string[]>}, \texttt{control: InputSignal<FormControl<string>>} (tutti obbligatori), \texttt{id: InputSignal<string>} (opzionale).
    \item \texttt{TextAreaFieldComponent}: \texttt{label}, \texttt{control} (obbligatori), \texttt{rows: InputSignal<number>} (default 4), \texttt{placeholder}, \texttt{id} (opzionali).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno.
\end{itemize}

\subsubsection{FileDropzoneComponent}

Area di trascinamento/selezione file per il caricamento documenti nel Co-Pilot, ristretta ai soli PDF (\texttt{accept="application/pdf"}). Ripulisce il valore dell'\texttt{<input>} subito dopo aver emesso il file selezionato, così lo stesso file può essere ricaricato una seconda volta senza che il browser lo consideri un valore invariato e sopprima l'evento \texttt{change}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/FileDropzoneComponent.png}
    \caption{Classe --- FileDropzoneComponent}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{label: InputSignal<string>} (obbligatorio), \texttt{disabled: InputSignal<boolean>} (default \textit{false}).
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{fileSelected: OutputEmitterRef<File>}: evento emesso alla selezione di un file valido.
\end{itemize}

% ── 6.7 ──────────────────────────────────────────────────────────────────────
\subsection{Macro componenti UI (pagine)}

Le pagine sono i componenti radice di ciascuna feature, montati dal router: \texttt{OverviewPage} (cruscotto con le metriche di entrambi i moduli), \texttt{AssistantPage} (AI Assistant Generativo) e \texttt{CopilotPage} (AI Co-Pilot). Nel pattern MVVM descritto in Sezione 6.2 sono la View: si procurano con \texttt{inject()} le dipendenze Angular (Store, servizio di feature) e costruiscono il proprio ViewModel passandogliele nel costruttore, poi compongono i componenti di feature e quelli condivisi legando il template ai segnali e ai metodi esposti dal ViewModel. Sulla View restano solo gli aspetti che richiedono un injection context Angular che il ViewModel non ha (i \texttt{FormGroup} reattivi dei filtri e l'\texttt{effect()} che li collega al ViewModel), oltre a poche azioni locali prive di logica di dominio (\texttt{resetFilters()}, su \texttt{AssistantPage} anche \texttt{resetMetricsFilters()} per il filtro separato della sezione Metriche, e su \texttt{CopilotPage} anche lo scorrimento verso il dettaglio in \texttt{selectDocument()}); nessuna chiamata HTTP e nessuna regola di business vive più nella pagina.

\subsubsection{AssistantPage}

Compone il pannello di generazione (\texttt{CommunicationGeneratorPanelComponent}) e quello di anteprima (\texttt{GeneratedCommunicationPreviewComponent}), entrambi componenti di feature, insieme all'elenco delle configurazioni di prompt salvate (\texttt{PromptConfigurationListComponent}) e allo storico comunicazioni (\texttt{CommunicationHistoryListComponent}), al proprio pannello di metriche (\texttt{MetricsPanelComponent}, Sezione 6.6, più \texttt{MetricCompositionComponent} per la ripartizione delle bozze e \texttt{RecentFeedbackListComponent}, componente di feature, per gli ultimi feedback testuali, UC-27.3) e a componenti condivisi per stato di errore e stato vuoto, legando ogni input e ogni evento del template a un segnale o un metodo di \texttt{vm} (\texttt{AssistantPageViewModel}, Sezione 6.2) — senza eccezioni: anche il banner d'errore principale chiama \texttt{(retry)="vm.reloadState()"}, non lo Store direttamente (Sezione 6.2).

Nel costruttore istanzia il ViewModel con \texttt{new AssistantPageViewModel(inject(AssistantService), this.store)}: non riceve una funzione di scorrimento, perché \texttt{AssistantPageViewModel} non la chiede al costruttore (Sezione 6.2).

Collega un \texttt{FormGroup} di filtri e un \texttt{effect()} che si limita a leggere lo storico condiviso e i filtri attivi del ViewModel, poi chiama \texttt{vm.reload()}: la chiamata di ricerca vera e propria vive nel ViewModel, non qui. Un secondo \texttt{effect()} legge \texttt{vm.metricsFilters()} e chiama \texttt{vm.refreshFilteredMetrics()} per la sezione Metriche (RF38-OB..RF41-OB), filtro separato da quello dello storico e collegato a un \texttt{FormGroup} proprio (\texttt{metricsFilterForm}). Un terzo \texttt{effect()} legge \texttt{vm.pendingScrollTarget()} e, quando non è nullo, chiama \texttt{scrollToElement} (l'unica operazione DOM richiesta dal ViewModel) poi azzera il segnale — è la View a eseguire lo scorrimento richiesto. Registra infine \texttt{inject(DestroyRef).onDestroy(() => vm.destroy())}, così la sottoscrizione di lettura in corso viene annullata alla distruzione del componente senza che il ViewModel sappia nulla del ciclo di vita di Angular.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/AssistantPageRelation.png}
    \caption{Classe --- AssistantPage}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{store: MvpStateStore}: dipendenza Angular procurata con \texttt{inject()}, campo \texttt{protected}: passata al costruttore del ViewModel e letta come segnale sorgente nell'\texttt{effect()} di ricerca; il template non la referenzia mai (Sezione 6.2).
    \item \texttt{vm: AssistantPageViewModel}: istanziato nel costruttore, punto a cui si lega il resto del template.
    \item \texttt{filterForm: FormGroup}: form reattivo dei filtri dello storico, resta sulla View perché \texttt{ReactiveFormsModule} richiede un injection context.
    \item \texttt{metricsFilterForm: FormGroup}: form reattivo dei filtri della sezione Metriche (tono, stile, intervallo di date), separato da \texttt{filterForm}: non condivide stato con lo storico.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{resetFilters(): void}: azzera il form dei filtri dello storico.
    \item \texttt{resetMetricsFilters(): void}: azzera il form dei filtri della sezione Metriche.
\end{itemize}

\subsubsection{CopilotPage}

Stessa struttura di \texttt{AssistantPage}, applicata al Co-Pilot: compone il pannello di upload (\texttt{DocumentUploadPanelComponent}), l'elenco documenti (\texttt{DocumentListComponent}, a cui passa \texttt{[page]="vm.currentPage()"} e \texttt{[totalPages]="vm.totalPages()"}, inoltrando \texttt{(goToPage)} a \texttt{vm.goToPage(\$event)}), il pannello di revisione del sotto-documento selezionato (\texttt{SubDocumentListComponent}, a cui passa anche \texttt{[previewStatus]="vm.previewStatus()"}) e il proprio pannello di metriche (\texttt{MetricsPanelComponent}, con \texttt{[presentation]="vm.metricsPresentation()"}), leggendo dal template solo \texttt{vm.*}: anche qui il banner d'errore principale chiama \texttt{(retry)="vm.reloadState()"}, non lo Store direttamente (Sezione 6.2). Istanzia il ViewModel con \texttt{new CopilotPageViewModel(inject(DocumentWorkflowService), store)}, senza la dipendenza di scorrimento perché questo ViewModel non ne ha mai avuto bisogno (Sezione 6.2).

Collega allo stesso modo un \texttt{FormGroup} di filtri (\texttt{createDocumentFilterForm()}) e un \texttt{effect()} che rilegge lo storico documentale chiamando \texttt{vm.reload()} a ogni cambio di filtro o di stato condiviso. Un secondo \texttt{effect()} legge \texttt{vm.selectedDocument()} e chiama \texttt{vm.loadPreviewStatus(document?.previewUrl ?? null)}: la fetch della raggiungibilità dell'anteprima segue la selezione, non un elenco a parte. Registra inoltre \texttt{inject(DestroyRef).onDestroy(() => vm.destroy())}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/CopilotPageRelation.png}
    \caption{Classe --- CopilotPage}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{store: MvpStateStore}: dipendenza Angular procurata con \texttt{inject()}, campo \texttt{protected}: passata al costruttore del ViewModel e letta come segnale sorgente nel primo \texttt{effect()} (quello di ricerca); il secondo legge solo \texttt{vm.selectedDocument()}. Il template non la referenzia mai, stesso ruolo che ha su \texttt{AssistantPage}.
    \item \texttt{vm: CopilotPageViewModel}: istanziato nel costruttore.
    \item \texttt{filterForm: FormGroup}: form reattivo dei filtri dello storico documenti.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item \texttt{selectDocument(documentId: string | null): void}: apre il dettaglio del sotto-documento selezionato e vi scorre la pagina (verso \texttt{copilot-document-detail}), solo se \texttt{documentId} non è nullo. Lo scorrimento resta nella View e non nel ViewModel perché è lei l'unica responsabile della resa visiva (ADR 0011).
    \item \texttt{resetFilters(): void}.
\end{itemize}

\subsubsection{OverviewPage}

La pagina più semplice nella logica di collegamento (nessun \texttt{FormGroup}, perché la Overview non ha uno storico filtrabile proprio): mostra le priorità operative (\texttt{AttentionNoteComponent}, montato solo se \texttt{vm.quarantined() > 0}, più una griglia di \texttt{MetricCardComponent} per \texttt{vm.priorities()}), gli indicatori di qualità per modulo (\texttt{MetricStarsComponent} per \texttt{vm.assistantQuality()}, \texttt{MetricGaugeComponent} per \texttt{vm.copilotQuality()}, Sezione 6.6) e le attività recenti già presenti nello Store, in una tabella con \texttt{StatusBadgeComponent}/\texttt{EmptyStateComponent}. Non monta \texttt{MetricsPanelComponent}: mostra direttamente le sue schede mirate, non un pannello generico.

Istanzia il ViewModel con \texttt{new OverviewPageViewModel(this.store, inject(Router))}: non riceve una funzione di scorrimento nel costruttore, per lo stesso motivo di \texttt{AssistantPage}. I pulsanti di richiamo all'azione nella sezione hero e i due link "Vedi tutte le metriche" delegano interamente a \texttt{vm.navigate()}, che naviga e poi chiede lo scorrimento verso la sezione target sulla pagina di destinazione; un \texttt{effect()} nel costruttore legge \texttt{vm.pendingScrollTarget()} e lo esegue, stesso schema di \texttt{AssistantPage}/\texttt{CopilotPage}.

Qui la View non legge lo Store in nessun punto del template, banner d'errore compreso (\texttt{(retry)="vm.reload()"}): il ViewModel sintetizza priorità e qualità (Sezione 6.2), e il campo \texttt{store} sulla View resta solo per costruire il ViewModel, esattamente come su \texttt{AssistantPage} e \texttt{CopilotPage}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ImmaginiSpecificaTecnica/ClassiSingoleFrontendMicro/OverviewPageRelation.png}
    \caption{Classe --- OverviewPage}
\end{figure}

Attributi
\begin{itemize}\raggedright
    \item \texttt{store: MvpStateStore}: dipendenza Angular procurata con \texttt{inject()}, campo \texttt{protected}: passata al costruttore del ViewModel; il template non la referenzia mai.
    \item \texttt{vm: OverviewPageViewModel}: istanziato nel costruttore insieme a \texttt{inject(Router)}.
\end{itemize}

Metodi
\begin{itemize}\raggedright
    \item Nessuno: a differenza delle altre due pagine, non ha bisogno di cablare filtri propri; l'unico \texttt{effect()} è logica di collegamento nel costruttore, non un metodo della classe.
\end{itemize}

\end{sloppypar}

% ─────────────────────────────────────────────────────────────────────────────
\section{Design Pattern}

\begin{sloppypar}

I design pattern individuati nel sistema si dividono in due categorie: quelli applicati esplicitamente nella logica applicativa, con un adapter, una porta o un collaboratore reale dietro il nome (non citati per completare l'elenco), e i pattern nativi del framework, adottati così come sono senza reimplementarli. Per ciascun pattern applicato esplicitamente viene indicato dove si trova nel codice, quale problema risolve in quel punto specifico e cosa succederebbe senza. Due pattern valutati e scartati (Proxy, Abstract Factory) sono documentati alla fine della Sezione 7.1, per lo stesso motivo: un pattern citato solo per apparire nell'elenco non è meno fuorviante di uno assente.

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\footnotesize
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{4pt}
\begin{longtable}{|p{2.0cm}|p{2.9cm}|p{10.3cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Pattern} & \textbf{Categoria} & \textbf{Dove applicato} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Pattern} & \textbf{Categoria} & \textbf{Dove applicato} \\
    \hline
    \endhead
    Adapter & Strutturale & I repository Eloquent, gli adapter Bedrock/Textract e \texttt{SfnWorkflowEngineAdapter} di entrambi i domini (Sezione 5) \\
    \hline
    Strategy & Comportamentale & \texttt{WorkflowTaskHandler} (porta primaria), selezionato da \texttt{WorkflowTaskRegistry::for(\$taskType)} \\
    \hline
    Facade & Strutturale & I casi d'uso applicativi (\texttt{UploadDocumentService}, \texttt{GenerateCommunicationService}, ...), un solo metodo pubblico per coordinare più porte secondarie \\
    \hline
    Factory Method & Creazionale & \texttt{WorkflowTaskRegistry::for(string \$taskType): WorkflowTaskHandler} \\
    \hline
    Singleton & Creazionale & Binding dei client AWS e degli adapter nel service provider, bindati all'interfaccia di porta \\
    \hline
    Observer & Comportamentale & Porte dispatcher di eventi dei due domini → dispatcher Laravel, 13 + 20 listener \\
    \hline
    Command & Comportamentale & Ogni caso d'uso applicativo, una classe per una porta primaria \\
    \hline
    \caption{Design pattern individuati nel backend}
\end{longtable}
}

\subsection{Design Pattern Backend}

\subsubsection{Adapter}

\textbf{Intent:} convertire l'interfaccia di una classe in un'altra interfaccia attesa dal client, permettendo la collaborazione fra classi altrimenti incompatibili per via delle librerie o dei framework esterni che usano.

È il pattern costitutivo dell'intera architettura esagonale (Sezione 4.3): isola sistematicamente il dominio applicativo dalla forma specifica delle tecnologie esterne (ORM, SDK cloud) che altrimenti imporrebbero la propria interfaccia direttamente ai casi d'uso. Ogni porta secondaria ha un adapter che la implementa sopra una tecnologia concreta, mai il contrario.

Esempio nel progetto. \texttt{EloquentDocumentRepository} ed \texttt{EloquentCommunicationRepository} implementano le porte di persistenza sopra Eloquent, \texttt{BedrockDocumentAiAdapter}, \texttt{BedrockCommunicationAiAdapter} e \texttt{TextractOcrAdapter} sopra gli SDK AWS, \texttt{SfnWorkflowEngineAdapter} sopra Step Functions (Sezione 5). Un caso d'uso parla sempre con \texttt{DocumentRepository}, mai con \texttt{SubDocument::query()}. Senza questo pattern il dominio dipenderebbe direttamente da Eloquent e dagli SDK AWS, e nessun caso d'uso sarebbe testabile senza avviare Laravel e senza credenziali AWS/LocalStack: è questa la testabilità del dominio senza framework descritta in Sezione 4.7, dimostrata dalla suite dedicata \texttt{tests/DomainUnit} che istanzia i casi d'uso con implementazioni in memoria delle porte secondarie.

\begin{lstlisting}[style=phplisting, caption={BedrockDocumentAiAdapter -- adapter secondario sopra BedrockService}]
// app/Mvp/Documents/Adapters/Outbound/Ai/BedrockDocumentAiAdapter.php
class BedrockDocumentAiAdapter implements DocumentAiGatewayPort
{
    public function __construct(private readonly BedrockService $bedrock) {}

    public function splitDocument(string $ocrText, int $pageCount, string $boundaryNonce): array
    {
        return $this->bedrock->splitDocument($ocrText, $pageCount, $boundaryNonce);
    }

    public function extractFields(string $ocrText): array
    {
        return $this->bedrock->extractFields($ocrText);
    }

    public function pageBoundaryMarker(int $page, string $nonce): string
    {
        return BedrockService::pageBoundaryMarker($page, $nonce);
    }

    public function formatUserError(\Throwable $e, string $defaultMessage): string
    {
        return BedrockService::formatUserError($e, $defaultMessage);
    }
}
\end{lstlisting}

\subsubsection{Strategy}

\textbf{Intent:} definire una famiglia di algoritmi intercambiabili, incapsularli singolarmente e renderli intercambiabili indipendentemente dal client che li usa.

Risolve il problema di un componente infrastrutturale condiviso da più domini che deve restare agnostico rispetto al passo di business specifico di ciascuno, delegando solo quel passo a un'implementazione selezionata a runtime invece di conoscerla in anticipo.

Esempio nel progetto. \texttt{WorkflowTaskHandler} è la porta primaria implementata da \texttt{DocumentWorkflowTaskHandler} e \texttt{CommunicationWorkflowTaskHandler} (Sezione 5): \texttt{WorkflowTaskRunner}, il collaboratore condiviso che riceve un task da Step Functions/SQS, resta completamente agnostico rispetto al dominio (deduplicazione, presa in carico e audit sono identici per entrambi) e delega il solo passo di business all'handler selezionato da \texttt{WorkflowTaskRegistry} in base al tipo di task. Senza questo pattern, \texttt{WorkflowTaskRunner} avrebbe bisogno di un \texttt{match}/\texttt{if} sul dominio del task, violando la Dependency Rule verificata in CI (Sezione 4.7): l'infrastruttura di workflow condivisa dovrebbe conoscere i dettagli interni di Documents e Communications.

\begin{lstlisting}[style=phplisting, caption={WorkflowTaskRunner -- selezione della Strategy concreta e delega del passo di business}]
// app/Mvp/Workflow/Services/WorkflowTaskRunner.php
class WorkflowTaskRunner
{
    public function __construct(
        private readonly WorkflowTaskRegistry $registry,
        private readonly AuditLogger $audit,
    ) {}

    public function handle(array $message): array
    {
        $taskToken = (string) ($message['taskToken'] ?? $message['task_token'] ?? '');
        $taskType = (string) ($message['taskType'] ?? $message['task_type'] ?? '');

        if ($taskToken === '' || $taskType === '') {
            throw new \InvalidArgumentException('Messaggio workflow non valido: taskToken e taskType sono obbligatori.');
        }

        // Selezione della Strategy concreta: il runner non conosce
        // DocumentWorkflowTaskHandler ne' CommunicationWorkflowTaskHandler,
        // solo l'interfaccia WorkflowTaskHandler.
        $handler = $this->registry->for($taskType);
        $subject = $handler->resolveSubject($message);

        // [...] deduplicazione via task_token_hash, claim atomico dello
        // stato "running", gestione dei casi gia' completato/fallito:
        // logica agnostica dal dominio, identica per ogni taskType.

        // Delega del solo passo di business alla Strategy selezionata.
        $result = $handler->execute($taskType, $subject, $message);

        // [...] aggiornamento del task, audit, costruzione della risposta.
    }
}
\end{lstlisting}

\subsubsection{Facade}

\textbf{Intent:} fornire un'interfaccia unificata a un insieme di interfacce di un sottosistema, semplificando l'uso del sottosistema per il client.

Evita che l'adapter primario (un Controller HTTP o un handler di workflow) debba conoscere e orchestrare direttamente i singoli collaboratori necessari a soddisfare una richiesta applicativa, riducendo quella conoscenza a un solo metodo pubblico.

Esempio nel progetto. Ogni caso d'uso applicativo (Sezione 5) è una Facade sulle proprie porte secondarie: \texttt{ProcessDocumentService} coordina repository, storage, gateway AI, dispatcher di eventi, heartbeat e generatore di identificativi dietro un solo metodo pubblico (\texttt{process()}); l'adapter primario che lo invoca (\texttt{DocumentController} o \texttt{DocumentWorkflowTaskHandler}) non deve sapere quanti collaboratori servono per soddisfare la richiesta. Senza questo pattern il Controller orchestrerebbe direttamente 4-5 servizi, la situazione esplicitamente evitata dalla separazione fra adapter primario e caso d'uso applicativo su cui si fonda tutta la Sezione 5.

\begin{lstlisting}[style=phplisting, caption={ProcessDocumentService -- Facade su repository, storage, gateway AI, eventi ed heartbeat dietro process()}]
// app/Mvp/Documents/Application/UseCases/ProcessDocumentService.php
class ProcessDocumentService implements ProcessDocumentUseCase
{
    public function __construct(
        private readonly DocumentRepository $documents,
        private readonly DocumentStoragePort $storage,
        private readonly DocumentAiGatewayPort $ai,
        private readonly DocumentEventDispatcherPort $events,
        private readonly WorkflowHeartbeatPort $heartbeat,
        private readonly LoggerInterface $logger,
        private readonly UniqueIdGeneratorPort $ids,
        private readonly OcrRangeReader $ocrRange,
        private readonly ExtractSubDocumentFieldsUseCase $fieldsExtractor,
        private readonly ClockInterface $clock,
        private readonly string $tempDirectory,
    ) {}

    public function process(int $documentId): array
    {
        $document = $this->documents->findOriginalDocument($documentId);

        if ($document->processingStatus() === ProcessingStatus::Completed) {
            return ['skipped' => true];
        }

        $absoluteSource = null;

        try {
            $this->documents->updateOriginalDocument($documentId, OriginalDocumentChanges::none()
                ->withProcessingStatus(ProcessingStatus::Processing)
                ->withErrorMessage(null));
            $this->events->dispatch(new DocumentProcessingStarted($documentId, $document->tenantId));

            $absoluteSource = $this->copyStorageFileToTemporaryPath($document->filePath);
            $pdf = new Fpdi;
            $pageCount = max(1, $pdf->setSourceFile($absoluteSource));

            $this->heartbeat->beat(force: true);

            $segments = $this->normalizeSegments($this->splitDocument($document, $pageCount), $pageCount);

            $oldSplitPaths = $this->documents->deleteExistingSubDocuments($documentId);
            $this->deleteStoragePaths($oldSplitPaths);

            foreach ($segments as $segment) {
                $this->heartbeat->beat();
                $splitPath = $this->extractPages($absoluteSource, $documentId, $segment['employee_name'], $segment['start_page'], $segment['end_page']);

                try {
                    $subDocumentId = $this->documents->createSubDocument(new NewSubDocument(
                        originalDocumentId: $documentId,
                        filePath: $splitPath,
                        startPage: $segment['start_page'],
                        endPage: $segment['end_page'],
                    ));
                } catch (\Throwable $e) {
                    $this->deleteStoragePaths([$splitPath]);

                    throw $e;
                }

                $this->fieldsExtractor->extractAndSaveFieldsWithContext($subDocumentId, $document);
            }

            $document->complete();
            $this->documents->saveOriginalDocument($document);
            $this->events->dispatch(new DocumentProcessingCompleted($documentId, $document->tenantId));
        } catch (\Throwable $e) {
            $this->handleProcessingFailure($documentId, $e);
        } finally {
            if ($absoluteSource !== null) {
                @unlink($absoluteSource);
            }
        }

        return ['skipped' => false];
    }

    // [...] handleProcessingFailure(), normalizeSegments(), splitDocument(),
    // deleteStoragePaths(), extractPages(), copyStorageFileToTemporaryPath(),
    // temporaryPath(): dettagli privati di manipolazione PDF e storage, mai
    // esposti al chiamante di process().
}
\end{lstlisting}

\subsubsection{Factory Method}

\textbf{Intent:} definire un'interfaccia per creare un oggetto, lasciando che sia un metodo dedicato a decidere quale classe concreta istanziare, invece del chiamante.

Centralizza in un solo punto la selezione dell'implementazione corretta a runtime, così che il chiamante non debba conoscere in anticipo l'insieme delle classi concrete disponibili né duplicare la logica di scelta a ogni punto di invocazione.

Esempio nel progetto. \texttt{WorkflowTaskRegistry::for(string \$taskType): WorkflowTaskHandler} seleziona l'handler corretto per tipo di task, senza che \texttt{WorkflowTaskRunner} (il chiamante) conosca le classi concrete \texttt{DocumentWorkflowTaskHandler}/\texttt{CommunicationWorkflowTaskHandler}. Senza questo pattern, quella selezione andrebbe duplicata con logica propria a ogni punto di invocazione del runner.

Nota di onestà: non è Factory Method in senso GoF stretto, che richiede una gerarchia di sottoclassi dove ciascuna decide per override quale prodotto concreto creare. \texttt{WorkflowTaskRegistry} è una singola classe con una mappa interna e un lookup a runtime, non una gerarchia — più vicino a un Registry (nel senso di Fowler) che a un Factory Method: stesso intento pratico (il chiamante non conosce le classi concrete), meccanismo diverso.

\begin{lstlisting}[style=phplisting, caption={WorkflowTaskRegistry -- registrazione e lookup dell'handler per tipo di task}]
// app/Mvp/Workflow/Services/WorkflowTaskRegistry.php
class WorkflowTaskRegistry
{
    /** @var array<string, WorkflowTaskHandler> */
    private array $handlers = [];

    public function register(WorkflowTaskHandler $handler): void
    {
        foreach ($handler->taskTypes() as $taskType) {
            $this->handlers[$taskType] = $handler;
        }
    }

    public function for(string $taskType): WorkflowTaskHandler
    {
        return $this->handlers[$taskType]
            ?? throw new \InvalidArgumentException("Task workflow non supportato: {$taskType}");
    }

    /**
     * @return array<int, string>
     */
    public function taskTypes(): array
    {
        return array_keys($this->handlers);
    }
}
\end{lstlisting}

\subsubsection{Singleton}

\textbf{Intent:} garantire che una classe abbia una sola istanza condivisa e fornire un punto di accesso globale ad essa.

Evita la costruzione ripetuta di collaboratori costosi da istanziare (un client HTTP verso un servizio esterno, tipicamente) e centralizza in un solo punto la scelta di quale implementazione concreta soddisfa una data porta.

Esempio nel progetto. I client AWS (\texttt{SfnClient}, il client Bedrock, il client Textract) e i rispettivi adapter sono bindati come singleton nel service provider (costruzione costosa evitata, connessioni riutilizzate), ma bindati all'interfaccia di porta, non alla classe concreta: il binding diventa così anche il punto unico in cui si decide quale adapter soddisfa quale porta. Senza questa scelta, cambiare adapter richiederebbe cercare e modificare ogni type-hint concreto sparso nella codebase.

Nota di onestà: non è un Singleton in senso GoF stretto, che richiede che sia la classe stessa a impedire una seconda istanza (costruttore privato, \texttt{getInstance()} statico). Qui è il container a garantire un'unica istanza per la durata della request: un vincolo di lifecycle esterno alla classe, non una proprietà che la classe impone su se stessa — \texttt{SfnWorkflowEngineAdapter} e gli altri adapter restano classi con un costruttore pubblico ordinario, e nulla impedirebbe a un altro punto del codice di chiamare comunque \texttt{new} e ottenere una seconda istanza.

\begin{lstlisting}[style=phplisting, caption={AppServiceProvider -- binding singleton dei client AWS e delle porte secondarie}]
// app/Providers/AppServiceProvider.php (register())
$this->app->singleton(BedrockRuntimeClient::class, function () {
    return $this->bedrockClient((string) config('services.bedrock.region'));
});

$this->app->singleton(SfnClient::class, function () {
    $config = [
        'version' => 'latest',
        'region' => config('services.workflow.region'),
    ];

    if (filled(config('services.workflow.endpoint'))) {
        $config['endpoint'] = config('services.workflow.endpoint');
    }

    return new SfnClient($config);
});

// [...] SqsClient, TextractClient, WorkflowTaskHeartbeat: stesso schema,
// client costoso o servizio condiviso, un solo binding.

// --- Dominio Documents: porta -> adapter ---
$this->app->singleton(OcrGatewayPort::class, TextractOcrAdapter::class);
$this->app->singleton(DocumentAiGatewayPort::class, BedrockDocumentAiAdapter::class);
$this->app->singleton(DocumentStoragePort::class, FlysystemDocumentStorageAdapter::class);
$this->app->singleton(DocumentRepository::class, EloquentDocumentRepository::class);
$this->app->singleton(SendMessageRendererPort::class, DompdfSendMessageRenderer::class);
$this->app->singleton(DocumentEventDispatcherPort::class, LaravelDocumentEventDispatcher::class);

// Workflow: porta condivisa fra Documents e Communications.
$this->app->singleton(WorkflowEnginePort::class, SfnWorkflowEngineAdapter::class);
\end{lstlisting}

\subsubsection{Observer}

\textbf{Intent:} definire una dipendenza uno-a-molti fra oggetti, così che quando un oggetto cambia stato tutti i suoi dipendenti vengano notificati automaticamente, senza che il soggetto li conosca singolarmente.

Disaccoppia l'esecuzione di una regola di business dalla sua conseguenza collaterale (l'audit) che diventa una reazione indipendente a un evento invece di una chiamata esplicita ripetuta in ogni caso d'uso che ne ha bisogno.

Esempio nel progetto. Applicato simmetricamente a entrambi i domini, con porte separate perché gli eventi sono specifici di dominio: Documents dispatcha 14 eventi (\texttt{DocumentEventDispatcherPort} → \texttt{LaravelDocumentEventDispatcher}, 13 listener), Communications 21 (\texttt{CommunicationEventDispatcherPort} → \texttt{LaravelCommunicationEventDispatcher}, 20 listener; si veda la Sezione 5 per l'elenco delle classi che li dispatchano). In ciascun dominio un evento di completamento pipeline (\texttt{DocumentWorkflowCompleted}, \texttt{CommunicationWorkflowCompleted}) non ha un listener registrato: lo stato che segnala è già persistito direttamente dal caso d'uso che lo dispatcha, non da una reazione all'evento. Ogni listener dipende da un solo collaboratore, \texttt{AuditLogger} (Sezione 5.3).

Senza Observer, ogni nuova reazione a un evento di dominio richiederebbe toccare ogni caso d'uso che genera quell'evento, invece di aggiungere un listener.

\begin{lstlisting}[style=phplisting, caption={Registrazione del listener in AppServiceProvider e reazione in RecordDocumentProcessingStarted}]
// app/Providers/AppServiceProvider.php (boot())
Event::listen(DocumentUploadAccepted::class, RecordDocumentUploadAccepted::class);
Event::listen(DocumentProcessingStarted::class, RecordDocumentProcessingStarted::class);
Event::listen(DocumentProcessingCompleted::class, RecordDocumentProcessingCompleted::class);
Event::listen(DocumentProcessingFailed::class, RecordDocumentProcessingFailed::class);
// [...] altri 9 listener per Documents, 20 per Communications: stesso
// schema, un Event::listen per coppia evento/listener.

// app/Mvp/Documents/Application/Listeners/RecordDocumentProcessingStarted.php
class RecordDocumentProcessingStarted
{
    public function __construct(private readonly AuditLogger $audit) {}

    public function handle(DocumentProcessingStarted $event): void
    {
        $this->audit->record(
            'mvp-document-processing-started',
            resourceType: 'original_document',
            resourceId: (string) $event->documentId,
            metadata: ['status' => ProcessingStatus::Processing->value],
            tenantId: $event->tenantId,
        );
    }
}
\end{lstlisting}

Il caso d'uso che pubblica l'evento (\texttt{ProcessDocumentService::process()}, si veda il Listing precedente) non conosce \texttt{RecordDocumentProcessingStarted}: chiama \texttt{\$this->events->dispatch(new DocumentProcessingStarted(...))} sulla propria porta secondaria, e il dispatcher Laravel invoca tutti i listener registrati per quell'evento.

\subsubsection{Command}

\textbf{Intent:} incapsulare una richiesta come oggetto a sé stante, permettendo di parametrizzare i client con richieste diverse e di trattare ogni richiesta come una singola unità testabile in isolamento.

Trasforma ogni operazione applicativa in un oggetto con una singola responsabilità, testabile passando implementazioni fittizie delle sue dipendenze senza dover avviare l'intero framework che lo circonda.

Esempio nel progetto. Ogni caso d'uso applicativo (Sezione 5) è una classe dedicata a una singola porta primaria, la stessa forma già usata da \texttt{WorkflowTaskHandler::execute()}: un caso d'uso, una responsabilità, testabile passando implementazioni fittizie delle porte secondarie senza avviare Laravel.

\begin{lstlisting}[style=phplisting, caption={DeleteDocumentService -- caso d'uso applicativo con una singola porta primaria (delete())}]
// app/Mvp/Documents/Application/UseCases/DeleteDocumentService.php
class DeleteDocumentService implements DeleteDocumentUseCase
{
    public function __construct(
        private readonly DocumentRepository $documents,
        private readonly DocumentStoragePort $storage,
        private readonly DocumentEventDispatcherPort $events,
        private readonly LoggerInterface $logger,
    ) {}

    public function delete(int $subDocumentId, Actor $actor): void
    {
        $subDocument = $this->documents->findSubDocument($subDocumentId);
        $originalDocumentId = $subDocument->originalDocumentId;
        $original = $this->documents->findOriginalDocument($originalDocumentId);

        if ($original->tenantId !== $actor->tenantId) {
            throw new DocumentNotAuthorizedException;
        }

        $this->documents->deleteSubDocument($subDocumentId);
        $this->deleteStoragePath($subDocument->filePath);

        $this->events->dispatch(new SubDocumentDeleted($subDocumentId, $originalDocumentId, $actor));

        if (! $this->documents->originalDocumentHasRemainingSubDocuments($originalDocumentId)) {
            $this->documents->deleteOriginalDocumentWithWorkflowTasks($originalDocumentId);
            $this->deleteStoragePath($original->filePath);
        }
    }

    private function deleteStoragePath(string $path): void
    {
        try {
            $this->storage->delete($path);
        } catch (\Throwable $e) {
            $this->logger->warning('DeleteDocumentService: storage cleanup failed', ['path' => $path, 'message' => $e->getMessage()]);
        }
    }
}
\end{lstlisting}

Nota di onestà: l'idea "un caso d'uso = una classe con un solo metodo pubblico" non regge alla lettera per le porte che raggruppano più transizioni sullo stesso aggregato: \texttt{CommunicationDraftUseCase} ne ha cinque (\texttt{favorite}/\texttt{unfavorite}/\texttt{update}/\texttt{save}/\texttt{discard}), \texttt{StartCommunicationWorkflowUseCase} e \texttt{UpdateCommunicationCoverUseCase} ne hanno due ciascuna. È una scelta esplicita (raggruppare transizioni correlate sullo stesso aggregato invece di una classe per transizione), non un Command puro: si applica a metà dei casi d'uso, non a tutti.

\subsubsection{Pattern nativi del framework}

Laravel contribuisce due pattern propri, adottati così come sono anziché reimplementati: Eloquent (variante di \textit{Active Record}, POEAA) per il mapping oggetti-tabelle, usato però solo dentro gli adapter di persistenza, mai nel dominio: è proprio ciò che l'Adapter (Sezione 7.1.1) isola. Il Service Container di Laravel fornisce la dependency injection su cui si appoggiano sia il binding a Singleton (Sezione 7.1.5) sia la risoluzione automatica dei costruttori dei casi d'uso. Da non confondere con l'omonimo pattern Facade di Laravel (\texttt{Log::}, \texttt{Storage::}, \texttt{Auth::}: un accesso statico ai binding del container, un'accezione diversa dal GoF Facade applicato ai casi d'uso in Sezione 7.1.3): quelle Facade restano confinate agli adapter secondari (es. \texttt{Storage::} in \texttt{FlysystemDocumentStorageAdapter}, \texttt{Log::} in \texttt{TextractOcrAdapter}): mai usate nel Dominio di Documents e Communications, dove lo stesso script di CI che verifica la Dependency Rule (Sezione 4.7) blocca esplicitamente ogni riferimento a \texttt{Illuminate\textbackslash*}. Nell'Applicazione lo script è più permissivo e non vieta \texttt{Illuminate\textbackslash*} in generale (solo \texttt{Aws\textbackslash*}, i model Eloquent e il namespace dell'altro dominio): quelle tre Facade restano comunque assenti anche lì, ma per disciplina di progetto, non per un vincolo meccanico.

Due pattern sono stati valutati e scartati esplicitamente, non per assenza di un candidato ma perché inapplicabili al problema reale: \textbf{Proxy} (caching o circuit-breaking davanti ai gateway Bedrock/Textract) contraddirebbe una scelta esplicita del progetto: nessun fallimento di un servizio AI deve essere mascherato da un comportamento sostitutivo automatico. \textbf{Abstract Factory} non ha un candidato reale: non esiste nel progetto una famiglia di adapter correlati da costruire coerentemente insieme (gli adapter per LocalStack e per AWS reale non sono famiglie diverse, sono la stessa classe con endpoint e credenziali diversi), e l'unico bisogno di selezione a runtime è già coperto dal Factory Method di \texttt{WorkflowTaskRegistry}.

\subsection{Design Pattern Frontend}

\subsubsection{Facade}

\textbf{Intent:} fornire un'interfaccia unificata a un insieme di interfacce di un sottosistema, semplificando l'uso del sottosistema per il client: lo stesso intento del Facade backend (Sezione 7.1.3), applicato al lato client.

Evita che un ViewModel debba orchestrare direttamente più collaboratori del livello dati (client HTTP, canale SSE, stato condiviso) per compiere una singola azione, riducendo quell'orchestrazione a un solo metodo.

Esempio nel progetto. \texttt{AssistantService} e \texttt{DocumentWorkflowService} (Sezione 6.4) sono Facade sul client HTTP generato, su \texttt{SseClient} e sullo Store condiviso: un ViewModel chiama un solo metodo (\texttt{generate()}, \texttt{upload()}...) senza sapere che dietro c'è una richiesta HTTP, l'apertura di uno stream SSE e un aggiornamento dello stato applicativo. Senza questo pattern, ogni ViewModel dovrebbe orchestrare direttamente client API, \texttt{SseClient} e \texttt{MvpStateStore} per ciascuna azione, duplicando la stessa sequenza di passi in più punti.

\begin{lstlisting}[style=tslisting, caption={AssistantService -- Facade su client HTTP generato, SseClient e MvpStateStore dietro generate()}]
// apps/frontend/src/app/features/assistant/data/assistant.service.ts
generate(payload: CommunicationDraftForm): Observable<CommunicationGenerationProgress> {
  return this.trackGeneration(this.api.generateMvpCommunication(payload));
}

private trackGeneration(
  start$: Observable<StartCommunicationGenerationResponse>
): Observable<CommunicationGenerationProgress> {
  return new Observable<CommunicationGenerationProgress>((observer) => {
    let closeStream: (() => void) | null = null;

    const subscription = start$.subscribe({
      next: (response) => {
        observer.next({
          status: response.message,
          phase: "queued",
          communicationId: response.communicationId
        });

        closeStream = this.sse.connect(response.streamUrl, {
          onEvent: (event, data) => {
            // [...] rilancia sull'observer un evento CommunicationGenerationProgress
            // per ciascun evento SSE ("progress", "text", "cover", "still_running"),
            // e su "done" aggiorna lo Store con lo stato autorevole del backend:
            if (event === "done") {
              const payload = data as { communication?: Communication; state?: MvpState };

              if (payload.state) {
                this.store.setState(payload.state);
              }

              observer.next({
                status: "Bozza generata correttamente.",
                phase: "completed",
                communicationId: response.communicationId,
                communication: payload.communication
              });
              closeStream?.();
              observer.complete();
            }
          },
          onNamedError: (message) => {
            observer.next({
              status: message || "Generazione non disponibile. Controlla lo stato della bozza.",
              phase: "failed",
              communicationId: response.communicationId
            });
            closeStream?.();
            this.store.reload();
            observer.complete();
          },
          onConnectionError: () => {
            closeStream?.();
            this.store.reload();
            observer.complete();
          }
        });
      },
      error: (error: unknown) => observer.error(error)
    });

    return () => {
      subscription.unsubscribe();
      closeStream?.();
    };
  });
}
\end{lstlisting}

Il chiamante (\texttt{AssistantPageViewModel::generate()}, si veda il Listing successivo) vede un solo \texttt{Observable<CommunicationGenerationProgress>}: la richiesta HTTP iniziale, la connessione SSE e l'aggiornamento dello Store restano interamente dietro questo metodo.

\subsubsection{Presentation Model}

\textbf{Intent (Fowler, POEAA):} rappresentare lo stato e il comportamento della view in una classe indipendente dai widget della piattaforma, così che la view sia una proiezione sottile di quello stato invece di contenerlo.

A differenza del Facade, non è un pattern GoF ma architetturale (POEAA, come Repository o Active Record). Separa la logica applicativa di una pagina dal framework che la ospita, rendendola verificabile senza avviare l'infrastruttura di rendering: al prezzo di un file in più per ogni pagina a cui viene applicato.

Esempio nel progetto. Descritto per intero in Sezione 6.2: ogni pagina principale (\texttt{AssistantPage}, \texttt{CopilotPage}, \texttt{OverviewPage}) ha un \texttt{*ViewModel} gemello (classe TypeScript pura, costruita con \texttt{new}, senza \texttt{inject()} né riferimenti al DOM) che il Component costruisce e a cui lega l'intero template.

\begin{lstlisting}[style=tslisting, caption={AssistantPageViewModel -- classe pura costruita con new(), niente inject() né riferimenti al DOM}]
// apps/frontend/src/app/features/assistant/assistant-page.view-model.ts
export class AssistantPageViewModel {
  readonly phase: WritableSignal<CommunicationGenerationPhase | "idle"> = signal("idle");
  readonly isGenerating: Signal<boolean> = computed(
    () => this.phase() === "queued" || this.phase() === "generating-text" || this.phase() === "generating-cover"
  );
  readonly status = signal("In attesa di istruzioni.");
  readonly selectedDraftId: WritableSignal<number | null> = signal(null);
  readonly latestDraft: WritableSignal<GeneratedDraft | null> = signal(null);

  // [...] altri ventotto signal/computed per storico, configurazioni,
  // filtri, presentazione delle metriche, stati di caricamento delle
  // singole azioni.

  readonly error: Signal<string | null> = computed(() => this.store.error() ?? this.store.filteredAssistantError());

  constructor(
    private readonly assistant: AssistantService,
    private readonly store: MvpStateStore
  ) {}

  generate(payload: CommunicationDraftForm): void {
    this.phase.set("queued");
    this.status.set("Generazione in corso.");
    this.selectedDraftId.set(null);
    this.latestDraft.set(null);
    this.rateError.set(null);

    this.assistant.generate(payload).subscribe({
      next: (progress) => this.handleProgress(progress),
      error: (error: unknown) => {
        this.phase.set("failed");
        this.status.set(getApiErrorMessage(error, "Generazione non disponibile."));
      }
    });
  }

  // [...] regenerate(), rateDraft(), uploadCover(), saveDraft(), discard(),
  // saveToHistory(), toggleFavorite(), ...: stessa forma per ogni azione,
  // un metodo che aggiorna i signal e chiama AssistantService (Facade).
}
\end{lstlisting}

Le dipendenze (\texttt{AssistantService}, \texttt{MvpStateStore}) arrivano dal costruttore, non da \texttt{inject()}: la classe è istanziabile con \texttt{new AssistantPageViewModel(...)} in un test senza \texttt{TestBed}, il requisito che ha guidato la scelta di questo pattern al posto del \texttt{@Component} stesso come ViewModel (si veda oltre).

Il minimo richiesto sarebbe il \texttt{@Component} stesso come ViewModel (MVVM in senso lasco): scartato come unica implementazione perché non regge a un test costruito con \texttt{new}. Senza questo pattern, ogni test sulla logica di una pagina richiederebbe \texttt{TestBed}/\texttt{Injector.create()}: il costo che l'intera Sezione 6.2 spiega di voler evitare, e solo dove il beneficio lo giustifica (le tre pagine principali, non ogni componente della SPA).

\end{sloppypar}

% ── 8 ──────────────────────────────────────────────────────────────────────
\section{Database}

\begin{sloppypar}

Il sistema non ha una tabella \texttt{users}: l'identità (tenant, attore) è risolta da configurazione o da header fidati a monte della richiesta, a seconda della modalità (Sezione 3), non da una tabella anagrafica locale: coerentemente, ogni tabella di dominio porta un \texttt{tenant\_id} testuale invece di una chiave esterna verso un'entità utente. Le sette tabelle di dominio descritte in questa sezione escludono le tabelle infrastrutturali di Laravel (\texttt{sessions}, \texttt{cache}, \texttt{jobs}, \texttt{password\_reset\_tokens}), prive di rilevanza per il modello dei dati applicativo.

\subsection{Schema ER}

Le relazioni con vincolo di integrità referenziale dichiarato sono solo due: lo split produce i sotto-documenti (\texttt{original\_documents} 1---N \texttt{sub\_documents}, \textit{on delete cascade}) e l'estrazione produce i dati strutturati (\texttt{sub\_documents} 1---1 \texttt{extracted\_data}, \textit{on delete cascade}). Le altre relazioni visibili nel diagramma sono polimorfiche e non vincolate a livello di database: \texttt{workflow\_tasks} si riferisce a un \texttt{original\_documents} o a una \texttt{communications} tramite la coppia \texttt{subject\_type}/\texttt{subject\_id} (Sezione 8.4), mentre \texttt{audit\_events} si riferisce a un \texttt{original\_documents}, una \texttt{communications}, un \texttt{sub\_documents} o una \texttt{prompt\_configurations} tramite \texttt{resource\_type}/\texttt{resource\_id} (i quattro soli valori usati dai listener che scrivono l'audit trail, Sezione 7.1.6) --- nessuna tabella di dominio è quindi realmente isolata dal resto: coerentemente con l'assenza di una tabella utenti (Sezione 8), ogni tabella resta comunque coperta dal proprio \texttt{tenant\_id}, non da una foreign key.

\begin{figure}[H]
    \centering
    \includegraphics[angle=90, height=1\textheight]{ImmaginiSpecificaTecnica/Database/DiagrammaER.drawio.png}
    \caption{Schema ER}
\end{figure}

\subsection{Schema Relazionale}

A differenza dello Schema ER (Sezione 8.1), qui compaiono i tipi di colonna effettivi e le foreign key dichiarate esplicitamente: le due sole relazioni con vincolo di integrità referenziale reale (\texttt{sub\_documents.original\_document\_id} e \texttt{extracted\_data.sub\_document\_id}, entrambe \textit{on delete cascade}) sono frecce continue, mentre le quattro relazioni polimorfiche restano tratteggiate, senza vincolo dichiarato nel database. Il vincolo di unicità su \texttt{prompt\_configurations} (\texttt{tenant\_id}, \texttt{name}) è riportato come nota nella tabella stessa, non come relazione.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.85\textheight]{ImmaginiSpecificaTecnica/Database/schema-relazionale.png}
    \caption{Schema Relazionale}
\end{figure}

\subsection{Schema ERD (Entity-Relationship Diagram)}

Stesse tabelle, stesse colonne e stessi vincoli della Sezione 8.2, qui ridisegnati con la cardinalità indicata direttamente sulle linee di collegamento invece che a parole: un pallino vuoto indica cardinalità opzionale (0), una barra indica cardinalità obbligatoria (1), il ramo aperto indica ``molti''.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.85\textheight]{ImmaginiSpecificaTecnica/Database/schema-erd.png}
    \caption{Schema ERD}
\end{figure}

\subsection{Descrizione tabelle}

\subsubsection{communications}

Aggregato radice del modulo AI Assistant Generativo: l'intero ciclo di vita di una bozza di comunicazione, dal prompt iniziale fino allo stato finale, vive su questa unica riga. Accumula sia lo stato editoriale deciso dall'operatore (\texttt{status}, \texttt{is\_favorite}, \texttt{rating}) sia lo stato tecnico della pipeline asincrona di generazione (\texttt{generation\_status}, \texttt{cover\_status}, i riferimenti all'esecuzione Step Functions), perché l'entità di dominio \texttt{Communication} (Sezione 5.2) le governa entrambe nello stesso posto invece di separarle su tabelle diverse.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/communications.png}
    \caption{Tabella communications}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{tenant\_id}, \texttt{created\_by}: scoping multi-tenant e attore, risolti da header fidati (Sezione 3), non da una foreign key verso una tabella utenti.
    \item \texttt{prompt}, \texttt{tone}, \texttt{style}: input della generazione.
    \item \texttt{generated\_title}, \texttt{generated\_body}, \texttt{image\_prompt}: output testuale e prompt visivo prodotto per la copertina.
    \item \texttt{status} (\textit{draft}/\textit{approved}/\textit{discarded}), \texttt{is\_favorite}.
    \item \texttt{rating} (1–5, nullable), \texttt{rating\_comment}, \texttt{rated\_at}, \texttt{rated\_by}.
    \item \texttt{generation\_status} (\textit{pending}/\textit{processing}/\textit{completed}/\textit{failed}), \texttt{workflow\_execution\_arn}, \texttt{workflow\_started\_at}/\texttt{completed\_at}/\texttt{failed\_at}, \texttt{workflow\_failure\_reason}, \texttt{error\_message}.
    \item \texttt{cover\_image\_path}, \texttt{cover\_image\_mime}, \texttt{cover\_image\_size}, \texttt{cover\_image\_source}, \texttt{cover\_status} (\textit{pending}/\textit{processing}/\textit{ready}/\textit{failed}/\textit{removed}), \texttt{cover\_error}.
\end{itemize}

\subsubsection{original\_documents}

Aggregato radice del modulo AI Co-Pilot: il file caricato dall'utente, il testo e le pagine riconosciute dall'OCR, e lo stato del workflow Step Functions che lo elabora fino allo split in sotto-documenti. I metadati \texttt{manual\_*} (tipo documento, azienda, mese/anno di riferimento), se impostati in fase di upload, prevalgono sempre su quelli estratti successivamente dall'AI (Sezione 5.1, \texttt{UploadDocumentService}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/original_documents.png}
    \caption{Tabella original\_documents}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{tenant\_id}, \texttt{created\_by}.
    \item \texttt{file\_path}, \texttt{original\_filename}.
    \item \texttt{manual\_document\_type}, \texttt{manual\_company\_name}, \texttt{manual\_reference\_month}, \texttt{manual\_reference\_year}: metadati dichiarati in upload, mai sovrascritti dall'AI.
    \item \texttt{processing\_status} (\textit{pending}/\textit{processing}/\textit{completed}/\textit{failed}), \texttt{error\_message}.
    \item \texttt{s3\_bucket}, \texttt{s3\_key}: posizione reale su S3, distinta da \texttt{file\_path} quando Textract reale è abilitato (Sezione 5.1, \texttt{StartDocumentWorkflowService}).
    \item \texttt{workflow\_execution\_arn}, \texttt{workflow\_started\_at}/\texttt{completed\_at}/\texttt{failed\_at}, \texttt{workflow\_failure\_reason}.
    \item \texttt{textract\_job\_id}, \texttt{ocr\_text}, \texttt{ocr\_pages} (jsonb: testo per pagina con relativa confidenza, usato per assegnare gli intervalli di pagina ai sotto-documenti), \texttt{ocr\_confidence\_avg}.
\end{itemize}

\subsubsection{sub\_documents}

Un segmento del documento originale prodotto dallo split AI, associato a un singolo destinatario individuato. Governa la propria revisione (\texttt{review\_status}) e il proprio stato di invio (\texttt{send\_status}), oltre alle eventuali sovrascritture manuali del messaggio precompilato che l'operatore invia fuori piattaforma (Sezione 5.1, \texttt{SendMessageService}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/sub_documents.png}
    \caption{Tabella sub\_documents}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{original\_document\_id}: FK verso \texttt{original\_documents}, \textit{on delete cascade}.
    \item \texttt{file\_path}, \texttt{start\_page}, \texttt{end\_page}.
    \item \texttt{send\_status} (\textit{pending}/\textit{sent}), \texttt{send\_recipient\_override}, \texttt{send\_subject\_override}, \texttt{send\_body\_override}.
    \item \texttt{review\_status} (\textit{needs\_review}/\textit{auto\_validated}/\textit{quarantined}/\textit{manually\_validated}).
    \item \texttt{error\_message}.
\end{itemize}

\subsubsection{extracted\_data}

I campi strutturati che l'AI estrae per un singolo sotto-documento (nominativo, azienda, data, tipo documento), in relazione 1-1 con \texttt{sub\_documents}. \texttt{ai\_payload} conserva lo snapshot integrale e non tipizzato dell'output del modello: la revisione manuale che corregge le colonne tipizzate non cancella così il dato originale prodotto dall'AI.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/extracted_data.png}
    \caption{Tabella extracted\_data}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{sub\_document\_id}: FK univoca verso \texttt{sub\_documents}, \textit{on delete cascade}.
    \item \texttt{employee\_first\_name}, \texttt{employee\_last\_name}, \texttt{company\_name}, \texttt{recipient\_email}, \texttt{fiscal\_code}, \texttt{employee\_id}.
    \item \texttt{document\_date}, \texttt{document\_type}, \texttt{description}.
    \item \texttt{confidence\_score} (0–100, nullable): combina la confidenza media OCR con la completezza dei campi chiave trovati (Sezione 5.1, \texttt{ExtractSubDocumentFieldsService}).
    \item \texttt{field\_confidences} (json, nullable): confidenza scomposta per singolo campo chiave, non solo il valore aggregato di \texttt{confidence\_score}. Introdotta perché la confidenza restituita da Textract è quasi sempre alta di per sé: il segnale utile in pratica non era il punteggio OCR complessivo, ma la presenza o assenza dei singoli campi chiave, che riduceva l'esito a poche combinazioni discrete; scomporre la confidenza per campo rende quel segnale esplicito invece di appiattirlo in un unico numero.
    \item \texttt{ai\_payload} (json).
\end{itemize}

\subsubsection{workflow\_tasks}

Tabella unica dei task di callback dai worker asincroni verso Step Functions/SQS, condivisa da entrambi i domini invece di duplicata per modulo: la presa in carico atomica e la deduplicazione sul token vivono in un solo posto, e il dominio del task è identificato dalla coppia polimorfica \texttt{subject\_type}/\texttt{subject\_id} invece di una foreign key dedicata.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.35\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/workflow_tasks.png}
    \caption{Tabella workflow\_tasks}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{subject\_type}, \texttt{subject\_id}: riferimento polimorfico a \texttt{OriginalDocument} o \texttt{Communication}, senza vincolo di integrità referenziale dichiarato.
    \item \texttt{task\_type}, \texttt{task\_token\_hash} (univoca).
    \item \texttt{status}, \texttt{input\_payload}, \texttt{output\_payload}, \texttt{error\_message}.
    \item \texttt{started\_at}, \texttt{completed\_at}, \texttt{failed\_at}.
\end{itemize}

\subsubsection{prompt\_configurations}

Combinazioni di prompt, tono e stile salvate dall'utente per essere riutilizzate come preset del form di generazione, indipendenti da qualunque generazione effettiva (Sezione 5.2, \texttt{PromptConfigurationService}). Il nome è univoco per tenant, protetto da un vincolo di unicità a livello di database oltre che dalla logica applicativa.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.32\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/prompt_configurations.png}
    \caption{Tabella prompt\_configurations}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{tenant\_id}, \texttt{created\_by}.
    \item \texttt{name}, \texttt{prompt}, \texttt{tone}, \texttt{style}.
\end{itemize}

\subsubsection{audit\_events}

Log di audit trasversale a entrambi i domini, popolato dai listener degli eventi di dominio (Sezione 7.1.6, Observer) invece che da chiamate dirette sparse nei singoli casi d'uso. Ogni riga registra chi ha compiuto un'azione, su quale risorsa, correlata alla richiesta HTTP che l'ha generata.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.38\textwidth]{ImmaginiSpecificaTecnica/Database/Tabelle/audit_events.png}
    \caption{Tabella audit\_events}
\end{figure}

Colonne
\begin{itemize}\raggedright
    \item \texttt{event\_type}.
    \item \texttt{tenant\_id}, \texttt{actor\_id}, \texttt{actor\_email}.
    \item \texttt{resource\_type}, \texttt{resource\_id}: colonne testuali, non una foreign key: identificano la risorsa coinvolta senza un vincolo relazionale.
    \item \texttt{request\_id}, \texttt{correlation\_id}: propagati dagli stessi header di correlazione usati dallo stream SSE (Sezione 6.4).
    \item \texttt{metadata} (json).
\end{itemize}

\end{sloppypar}

% ─────────────────────────────────────────────────────────────────────────────
\section{Requisiti Funzionali Soddisfatti}

\begin{sloppypar}

Questo capitolo riporta lo stato di implementazione di ciascun requisito funzionale identificato nell'Analisi dei Requisiti, verificato direttamente sul codice sorgente del sistema alla data di redazione di questo documento. I requisiti funzionali si dividono in obbligatori e opzionali a seconda della loro importanza per il progetto: ne sono stati identificati 109, di cui 94 obbligatori e 15 opzionali, derivati dall'analisi dei casi d'uso (Analisi dei Requisiti, Sezione 4.1). Tutti i requisiti obbligatori risultano soddisfatti.

\subsection{Completamento requisiti}

\begin{itemize}
    \item \textbf{Requisiti Obbligatori}: 94/94 requisiti funzionali soddisfatti.
    \item \textbf{Requisiti Opzionali}: 11/15 requisiti funzionali soddisfatti.
\end{itemize}

I quattro requisiti opzionali non ancora soddisfatti alla data di redazione:
\begin{itemize}
    \item \textbf{RF4-OP}: nessun parametro di lunghezza della bozza né nella richiesta di generazione (\texttt{GenerateCommunicationCommand}) né nel form di generazione.
    \item \textbf{RF16-OP}: nessun metodo di ripristino del contenuto generato dall'AI dopo una modifica manuale; \texttt{CommunicationDraftService::update()} (Sezione 5.2) sovrascrive titolo e corpo senza conservare una copia del contenuto originale.
    \item \textbf{RF20-OP}: nessuna funzionalità di inoltro via e-mail per una comunicazione generata; l'invio precompilato via e-mail esiste solo sul modulo Co-Pilot (\texttt{SendMessageService}, Sezione 5.1).
    \item \textbf{RF107-OP}: il campo e-mail del destinatario, se assente, è mostrato come casella vuota anziché con un indicatore esplicito di dato non disponibile, a differenza degli altri campi dello stesso pannello (nome, azienda, data, tipologia, descrizione, confidenza), che lo hanno.
\end{itemize}

\subsection{Tabella di tracciamento dei requisiti}

\renewcommand{\arraystretch}{1.3}
\rowcolors{2}{gray!8}{white}
{\footnotesize
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{4pt}
\begin{longtable}{|p{1.8cm}|p{8.7cm}|p{2.4cm}|p{2.1cm}|}
    \hline
    \rowcolor{gray!20}
    \textbf{Codice} & \textbf{Descrizione} & \textbf{Stato} & \textbf{Priorità} \\
    \hline
    \endfirsthead
    \hline
    \rowcolor{gray!20}
    \textbf{Codice} & \textbf{Descrizione} & \textbf{Stato} & \textbf{Priorità} \\
    \hline
    \endhead
    \multicolumn{4}{l}{\textit{AI Assistant Generativo}} \\
    \hline
    RF1-OB & Il sistema deve permettere al Redattore di inserire il prompt testuale per la generazione del contenuto. & Implementato & Obbligatorio \\
    \hline
    RF2-OB & Il sistema deve permettere al Redattore di selezionare il tono comunicativo della bozza da generare. & Implementato & Obbligatorio \\
    \hline
    RF3-OB & Il sistema deve permettere al Redattore di selezionare lo stile editoriale della bozza da generare. & Implementato & Obbligatorio \\
    \hline
    RF4-OP & Il sistema deve permettere al Redattore di selezionare la lunghezza desiderata per la bozza da generare. & Non implementato & Opzionale \\
    \hline
    RF5-OB & Il sistema deve avviare la generazione del contenuto tramite AI a partire dal prompt e dai parametri configurati. & Implementato & Obbligatorio \\
    \hline
    RF6-OB & Il sistema deve visualizzare un'anteprima della bozza generata. & Implementato & Obbligatorio \\
    \hline
    RF7-OB & Il sistema deve visualizzare il titolo della bozza generata nell'anteprima. & Implementato & Obbligatorio \\
    \hline
    RF8-OB & Il sistema deve visualizzare il testo della bozza generata nell'anteprima. & Implementato & Obbligatorio \\
    \hline
    RF9-OB & Il sistema deve visualizzare l'immagine di copertina della bozza generata nell'anteprima. & Implementato & Obbligatorio \\
    \hline
    RF10-OB & Il sistema deve permettere al Redattore di modificare manualmente il titolo della bozza. & Implementato & Obbligatorio \\
    \hline
    RF11-OB & Il sistema deve permettere al Redattore di modificare manualmente il testo della bozza. & Implementato & Obbligatorio \\
    \hline
    RF12-OB & Il sistema deve permettere al Redattore di sostituire l'immagine di copertina della bozza con un file caricato localmente. & Implementato & Obbligatorio \\
    \hline
    RF13-OB & Il sistema deve permettere al Redattore di eliminare l'immagine di copertina della bozza. & Implementato & Obbligatorio \\
    \hline
    RF14-OB & Il sistema deve permettere al Redattore di richiedere la rigenerazione della bozza mantenendo i parametri di configurazione. & Implementato & Obbligatorio \\
    \hline
    RF15-OB & Il sistema deve permettere al Redattore di eliminare la bozza generata corrente. & Implementato & Obbligatorio \\
    \hline
    RF16-OP & Il sistema deve permettere al Redattore di annullare le modifiche manuali apportate alla bozza, ripristinando il contenuto originariamente generato. & Non implementato & Opzionale \\
    \hline
    RF17-OB & Il sistema deve permettere il salvataggio della bozza nello storico. & Implementato & Obbligatorio \\
    \hline
    RF18-OP & Il sistema deve permettere al Redattore di visualizzare un'anteprima del documento prima dell'esportazione definitiva. & Implementato & Opzionale \\
    \hline
    RF19-OB & Il sistema deve permettere la pubblicazione del documento generato e la sua esportazione in formato PDF. & Implementato & Obbligatorio \\
    \hline
    RF20-OP & Il sistema deve permettere al Redattore di inoltrare il documento finalizzato tramite posta elettronica. & Non implementato & Opzionale \\
    \hline
    RF21-OB & Il sistema deve fornire al Redattore la libreria personale con lo storico delle generazioni passate e i prompt salvati. & Implementato & Obbligatorio \\
    \hline
    RF22-OB & Il sistema deve permettere la ricerca e il filtraggio dello storico delle generazioni e dei prompt salvati. & Implementato & Obbligatorio \\
    \hline
    RF23-OB & Il sistema deve permettere al Redattore di filtrare lo storico delle generazioni tramite ricerca per parola chiave. & Implementato & Obbligatorio \\
    \hline
    RF24-OB & Il sistema deve permettere al Redattore di filtrare lo storico delle generazioni per tono comunicativo. & Implementato & Obbligatorio \\
    \hline
    RF25-OB & Il sistema deve permettere al Redattore di filtrare lo storico delle generazioni per stile editoriale. & Implementato & Obbligatorio \\
    \hline
    RF26-OB & Il sistema deve permettere al Redattore di filtrare lo storico delle generazioni per data. & Implementato & Obbligatorio \\
    \hline
    RF27-OB & Il sistema deve permettere al Redattore di salvare la configurazione corrente del prompt nella libreria personale, assegnandole un nome identificativo. & Implementato & Obbligatorio \\
    \hline
    RF28-OB & Il sistema deve permettere il caricamento e il riutilizzo di una configurazione prompt salvata. & Implementato & Obbligatorio \\
    \hline
    RF29-OB & Il sistema deve permettere al Redattore di contrassegnare una generazione come preferita nella libreria personale. & Implementato & Obbligatorio \\
    \hline
    RF30-OB & Il sistema deve permettere al Redattore di rimuovere una generazione dai preferiti nella libreria personale. & Implementato & Obbligatorio \\
    \hline
    RF31-OP & Il sistema deve permettere l'eliminazione di elementi dallo storico e dalla libreria dei prompt. & Implementato & Opzionale \\
    \hline
    RF32-OB & Il sistema deve permettere al Redattore di assegnare un rating numerico alla bozza generata. & Implementato & Obbligatorio \\
    \hline
    RF33-OB & Il sistema deve permettere al Redattore di aggiungere un commento testuale qualitativo alla valutazione della bozza. & Implementato & Obbligatorio \\
    \hline
    RF34-OB & Il sistema deve fornire all'Analista dati una dashboard con metriche dell'AI Assistant, con possibilità di esportare un report riepilogativo. & Implementato & Obbligatorio \\
    \hline
    RF35-OB & Il sistema deve visualizzare nella dashboard le statistiche di utilizzo del modulo AI Assistant. & Implementato & Obbligatorio \\
    \hline
    RF36-OB & Il sistema deve visualizzare nella dashboard il rating medio delle bozze generate. & Implementato & Obbligatorio \\
    \hline
    RF37-OB & Il sistema deve permettere all'Analista dati di consultare i feedback testuali ricevuti dagli utenti. & Implementato & Obbligatorio \\
    \hline
    RF38-OB & Il sistema deve permettere all'Analista dati di applicare filtri dinamici ai dati visualizzati nella dashboard analitica. & Implementato & Obbligatorio \\
    \hline
    RF39-OB & Il sistema deve permettere all'Analista dati di filtrare i dati della dashboard per intervallo di date. & Implementato & Obbligatorio \\
    \hline
    RF40-OB & Il sistema deve permettere all'Analista dati di filtrare i dati della dashboard per tono comunicativo. & Implementato & Obbligatorio \\
    \hline
    RF41-OB & Il sistema deve permettere all'Analista dati di filtrare i dati della dashboard per stile editoriale. & Implementato & Obbligatorio \\
    \hline
    \multicolumn{4}{l}{\textit{AI Co-Pilot per Consulenti del Lavoro}} \\
    \hline
    RF42-OB & Il sistema deve permettere al Consulente del lavoro di caricare un singolo documento per l'analisi AI automatica. & Implementato & Obbligatorio \\
    \hline
    RF43-OB & Il sistema deve permettere al Consulente del lavoro di caricare documenti con classificazione e metadati inseriti manualmente. & Implementato & Obbligatorio \\
    \hline
    RF44-OB & Il sistema deve permettere la selezione della tipologia del documento durante il caricamento manuale. & Implementato & Obbligatorio \\
    \hline
    RF45-OB & Il sistema deve permettere l'inserimento del mese di riferimento del documento durante il caricamento manuale. & Implementato & Obbligatorio \\
    \hline
    RF46-OB & Il sistema deve permettere l'inserimento dell'anno di riferimento del documento durante il caricamento manuale. & Implementato & Obbligatorio \\
    \hline
    RF47-OB & Il sistema deve permettere l'inserimento dell'azienda di riferimento del documento durante il caricamento manuale. & Implementato & Obbligatorio \\
    \hline
    RF48-OB & Il sistema deve permettere al Consulente del lavoro di visualizzare lo storico dei documenti analizzati. & Implementato & Obbligatorio \\
    \hline
    RF49-OB & Il sistema deve permettere il filtraggio dello storico documenti tramite criteri multipli. & Implementato & Obbligatorio \\
    \hline
    RF50-OB & Il sistema deve permettere il filtraggio dello storico documenti tramite barra di ricerca testuale. & Implementato & Obbligatorio \\
    \hline
    RF51-OB & Il sistema deve permettere il filtraggio dello storico documenti per stato di scaricamento. & Implementato & Obbligatorio \\
    \hline
    RF52-OB & Il sistema deve permettere il filtraggio dello storico documenti per livello di confidenza dell'analisi AI. & Implementato & Obbligatorio \\
    \hline
    RF53-OB & Il sistema deve permettere il filtraggio dello storico documenti per mese e anno. & Implementato & Obbligatorio \\
    \hline
    RF54-OB & Il sistema deve permettere al Consulente del lavoro di visualizzare i dettagli di un singolo documento dallo storico. & Implementato & Obbligatorio \\
    \hline
    RF55-OB & Il sistema deve visualizzare la porzione di documento relativa al dipendente (versione splittata). & Implementato & Obbligatorio \\
    \hline
    RF56-OB & Il sistema deve visualizzare il documento originale completo (non splittato). & Implementato & Obbligatorio \\
    \hline
    RF57-OB & Il sistema deve visualizzare il nome e cognome del dipendente associato al documento. & Implementato & Obbligatorio \\
    \hline
    RF58-OB & Il sistema deve visualizzare l'azienda associata al documento. & Implementato & Obbligatorio \\
    \hline
    RF59-OB & Il sistema deve visualizzare il nome del file del documento. & Implementato & Obbligatorio \\
    \hline
    RF60-OB & Il sistema deve visualizzare la data del documento. & Implementato & Obbligatorio \\
    \hline
    RF61-OB & Il sistema deve visualizzare il numero di pagine del documento. & Implementato & Obbligatorio \\
    \hline
    RF62-OB & Il sistema deve visualizzare la tipologia del documento. & Implementato & Obbligatorio \\
    \hline
    RF63-OB & Il sistema deve visualizzare la breve descrizione del documento. & Implementato & Obbligatorio \\
    \hline
    RF64-OB & Il sistema deve visualizzare il livello di confidenza in percentuale del documento. & Implementato & Obbligatorio \\
    \hline
    RF65-OP & Il sistema deve visualizzare lo stato del documento, comprensivo di stato di revisione e stato di scaricamento. & Implementato & Opzionale \\
    \hline
    RF66-OP & Il sistema deve visualizzare l'e-mail del destinatario del documento. & Implementato & Opzionale \\
    \hline
    RF67-OP & Il sistema deve visualizzare il codice fiscale del destinatario. & Implementato & Opzionale \\
    \hline
    RF68-OP & Il sistema deve visualizzare la matricola del dipendente. & Implementato & Opzionale \\
    \hline
    RF69-OP & Il sistema deve visualizzare la data e ora di caricamento del documento. & Implementato & Opzionale \\
    \hline
    RF70-OB & Il sistema deve permettere al Consulente del lavoro di modificare il nome e cognome del dipendente associato al documento. & Implementato & Obbligatorio \\
    \hline
    RF71-OB & Il sistema deve permettere la modifica dell'azienda associata al documento. & Implementato & Obbligatorio \\
    \hline
    RF72-OB & Il sistema deve permettere la modifica della data del documento. & Implementato & Obbligatorio \\
    \hline
    RF73-OB & Il sistema deve permettere la modifica della tipologia del documento. & Implementato & Obbligatorio \\
    \hline
    RF74-OB & Il sistema deve permettere la modifica della breve descrizione del documento. & Implementato & Obbligatorio \\
    \hline
    RF75-OP & Il sistema deve permettere la modifica dell'indirizzo e-mail del destinatario. & Implementato & Opzionale \\
    \hline
    RF76-OP & Il sistema deve permettere la modifica del codice fiscale del destinatario. & Implementato & Opzionale \\
    \hline
    RF77-OP & Il sistema deve permettere la modifica della matricola del dipendente. & Implementato & Opzionale \\
    \hline
    RF78-OB & Il sistema deve permettere al Consulente del lavoro di annullare le modifiche dei dati del singolo documento. & Implementato & Obbligatorio \\
    \hline
    RF79-OB & Il sistema deve permettere al Consulente del lavoro di eliminare il singolo documento. & Implementato & Obbligatorio \\
    \hline
    RF80-OB & Il sistema deve generare automaticamente il contenuto del messaggio precompilato per il documento da spedire. & Implementato & Obbligatorio \\
    \hline
    RF81-OB & Il sistema deve visualizzare il destinatario del messaggio precompilato generato automaticamente. & Implementato & Obbligatorio \\
    \hline
    RF82-OB & Il sistema deve visualizzare l'oggetto del messaggio precompilato generato automaticamente. & Implementato & Obbligatorio \\
    \hline
    RF83-OB & Il sistema deve visualizzare il testo del messaggio precompilato generato automaticamente. & Implementato & Obbligatorio \\
    \hline
    RF84-OB & Il sistema deve permettere la conferma e lo scaricamento del messaggio precompilato. & Implementato & Obbligatorio \\
    \hline
    RF85-OB & Il sistema deve permettere al Consulente del lavoro di modificare il destinatario del messaggio precompilato. & Implementato & Obbligatorio \\
    \hline
    RF86-OB & Il sistema deve permettere la modifica dell'oggetto del messaggio precompilato. & Implementato & Obbligatorio \\
    \hline
    RF87-OB & Il sistema deve permettere la modifica del testo del messaggio precompilato. & Implementato & Obbligatorio \\
    \hline
    RF88-OB & Il sistema deve fornire all'Analista dati una dashboard dedicata con metriche di monitoraggio del modulo AI Co-Pilot. & Implementato & Obbligatorio \\
    \hline
    RF89-OB & Il sistema deve visualizzare nella dashboard del Co-Pilot il numero totale di documenti analizzati. & Implementato & Obbligatorio \\
    \hline
    RF90-OB & Il sistema deve visualizzare nella dashboard del Co-Pilot la percentuale di classificazioni corrette. & Implementato & Obbligatorio \\
    \hline
    RF91-OB & Il sistema deve visualizzare nella dashboard del Co-Pilot i documenti sotto soglia di confidenza. & Implementato & Obbligatorio \\
    \hline
    RF92-OB & Il sistema deve visualizzare nella dashboard del Co-Pilot la percentuale di destinatari riconosciuti automaticamente. & Implementato & Obbligatorio \\
    \hline
    RF93-OB & Il sistema deve visualizzare nella dashboard del Co-Pilot il tempo medio di analisi dei documenti. & Implementato & Obbligatorio \\
    \hline
    \multicolumn{4}{l}{\textit{Gestione Errori}} \\
    \hline
    RF94-OB & Il sistema deve notificare il Redattore in caso di formato file non valido durante la sostituzione dell'immagine di copertina. & Implementato & Obbligatorio \\
    \hline
    RF95-OB & Il sistema deve notificare l'utente in caso di indirizzo e-mail non valido durante l'inoltro del documento. & Implementato & Obbligatorio \\
    \hline
    RF96-OB & Il sistema deve notificare il Redattore quando la ricerca nello storico delle generazioni non produce risultati. & Implementato & Obbligatorio \\
    \hline
    RF97-OB & Il sistema deve notificare il Redattore quando il commento testuale supera il limite di caratteri consentito. & Implementato & Obbligatorio \\
    \hline
    RF98-OB & Il sistema deve notificare l'Analista dati quando i dati analitici non sono disponibili per i criteri selezionati. & Implementato & Obbligatorio \\
    \hline
    RF99-OB & Il sistema deve rilevare i formati file non supportati durante l'upload e notificare il Consulente del lavoro. & Implementato & Obbligatorio \\
    \hline
    RF100-OB & Il sistema deve notificare l'utente quando nessun documento corrisponde ai criteri di visualizzazione dello storico. & Implementato & Obbligatorio \\
    \hline
    RF101-OB & Il sistema deve gestire il caso in cui il nome e cognome del dipendente non siano disponibili, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF102-OB & Il sistema deve gestire il caso in cui l'azienda non sia disponibile, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF103-OB & Il sistema deve gestire il caso in cui la data del documento non sia disponibile, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF104-OB & Il sistema deve gestire il caso in cui la tipologia del documento non sia disponibile, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF105-OB & Il sistema deve gestire il caso in cui la breve descrizione non sia disponibile, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF106-OB & Il sistema deve gestire il caso in cui il livello di confidenza non sia disponibile, visualizzando un opportuno indicatore. & Implementato & Obbligatorio \\
    \hline
    RF107-OP & Il sistema deve gestire il caso in cui l'e-mail del destinatario non sia disponibile, visualizzando un opportuno indicatore. & Non implementato & Opzionale \\
    \hline
    RF108-OP & Il sistema deve notificare il Consulente del lavoro in caso di codice fiscale non valido durante la modifica. & Implementato & Opzionale \\
    \hline
    RF109-OB & Il sistema deve notificare il Redattore quando il testo del prompt è mancante o insufficiente per avviare la generazione. & Implementato & Obbligatorio \\
    \hline
    \caption{Tabella di tracciamento dei requisiti funzionali}
\end{longtable}
}

\end{sloppypar}

\end{document}
